# ArtNetEsp — Architectural Review & Refactoring Plan

Senior-level review of the firmware as of v2026.1.2 (branch `big-refactor`), and a phased plan to get to a clean, portable, modern-C++ codebase without breaking devices in the field.

Guiding constraints honored throughout:

- **Field compatibility**: JSON config schema (type strings `BINARY`/`DIMMER`/`SERVO`/`REPEATER`), REST routes, and OTA must keep working for already-deployed devices until a deliberate major-version break.
- **Every phase ends green**: each phase builds all 11 environments and is independently flashable/testable. No "big bang" rewrite.
- **The ESP32 core-1 DMX sender pattern is load-bearing** (copy under mutex → release → blocking TX). It gets improved, never casually rewritten.

---

## Part 1 — Findings

### 1.1 Correctness bugs and undefined behavior (fix first, on the current structure)

These are concrete defects found by reading the code, not style opinions. Several explain known symptoms from the README TODO list.

| # | Issue | Where | Consequence |
|---|-------|-------|-------------|
| B1 | **Dangling reference capture in the Art-Net dispatch lambda.** `[&]` captures `init()`'s `universe` *parameter* by reference; the lambda runs long after `init()` returned. | `src/artnetHandler.h:44` | UB on every received frame. Harmless today only because all `frame(univ, …)` overloads ignore the universe — which is also *why* "Repeater ignores universe" can't be fixed without fixing this. |
| B2 | **Same bug in the API:** the `/reset-wifi` handler captures the `connect` *pointer parameter* of `setupApi()` by reference and dereferences it after the function returned. | `src/api.h:68-74` | UB; likely related to "BUG: Reboot doesn't always work correctly on Sonoff Basic" (README TODO). |
| B3 | **Out-of-bounds read `data[-1]`.** Repeater registers `channel = 0`, 512 channels; the dispatch loop reads `data[i - 1]` starting at `i = 0`. | `src/artnetHandler.h:54-58`, `src/device/repeater.h:32` | OOB read each frame, plus 512 pointless virtual `get()`/`set()` calls per frame per repeater (data already delivered via `frame()`). |
| B4 | **DMX channel truncated to `uint8_t`.** Config stores `uint16_t channel` (1–512), but `Device::channel`, all device constructors, and `set()/get()` use `uint8_t`. | `src/config.h:56` vs `src/device/device.h:10,17` | Any device configured on channel ≥ 256 silently wraps to the wrong channel. |
| B5 | **Repeater drops DMX channel 512 (off-by-one).** Buffer is `data[512]` where slot 0 is the start code, and `write()` rejects `channel >= 512`; ESP8266 side inits ESPDMX with 511. The correct packet size is 513 (start code + 512 slots). | `src/dmx/dmx.h:17`, `src/dmx/dmx32.cpp:75`, `src/dmx/dmx8266.cpp:9` | A full 512-channel universe loses its last channel on both platforms. |
| B6 | **Uninitialized members read before write.** `Device::lastChange` is never initialized (no ctor sets it); `Strobe::value`, `adjustedActiveValue`, `adjustedInactiveValue` likewise; `Config::universe` is garbage if JSON lacks the key. | `src/device/device.h:11`, `src/device/strobe.h:26-31`, `src/config.h:129` | UB; spurious "DMX timeout" blackout right after boot; `flip()` before any DMX frame writes a garbage PWM value to the pin. |
| B7 | **Null-pointer dereference on button press.** `dmx_devices[0]->isEnabled()` runs whenever `config.dmx.size() > 0`, but slot 0 is `NULL` if the first entry's type is `DISABLED`/unknown — or any non-`BINARY` type on Sonoff builds. | `src/main.cpp:80-90` | Crash/reboot on button press for valid-but-unsupported configs. |
| B8 | **Device count not clamped against `MAX_DMX_DEVICES`.** `configFromJson()` adds *every* JSON entry; `artnet.init(…, config.dmx.size())` then iterates `devices[k]` past the end of `dmx_devices[MAX_DMX_DEVICES]`. | `src/config.cpp:136-155`, `src/main.cpp:188`, `src/artnetHandler.h:48` | A config with more entries than the platform max reads past the array — garbage `Device*`, crash. |
| B9 | **`Config::save()` overwrites `default.json`.** If `/config/config.json` doesn't exist at boot, `_fileName` stays `/config/default.json` and the first `POST /config` writes there. `config.json` is never created; factory defaults are destroyed. | `src/config.cpp:5-17,40-47` | "Reset to defaults" semantics silently broken on every fresh device. |
| B10 | **Missing vtable anchor / not-quite-pure virtual.** `Device::getNumberOfChannels()` is declared, never defined, and not `= 0`. The build links only because the optimizer elides the unused `Device` vtable. | `src/device/device.h:39` | Any `-O0`/debug build fails to link; fragile by construction. Should be pure. Also: **no virtual destructor** on `Device`. |
| B11 | **Cross-context use-after-free window.** On ESP32, AsyncWebServer handlers run in the `async_tcp` task. `POST /config` → `cleanupDmx()` deletes `DmxChannel` objects while the OLED `Ticker` callback and `loop()` read `config->dmx[i]` concurrently. | `src/api.h:38-56`, `src/config.cpp:89-96`, `src/hw/oledDisplay.h:140,178` | Race; worst case crash during reconfiguration on OLED boards. |
| B12 | **Blocking captive portal inside `loop()`.** ~~On WiFi loss, `Connect::loop()` calls `connect()` → `wifiManager->autoConnect()`, which blocks (possibly forever, in portal mode). The portal also `reset()`s the shared server, so REST/OTA handlers are gone after a runtime reconnect cycle.~~ **Partially fixed** (item 11/Phase 4, see its "Done" note below): `Connect::loop()` no longer calls `connect()`/`autoConnect()` on a runtime drop — it does non-blocking `WiFi.reconnect()` instead, so `loop()` never blocks and the shared server is never `reset()` post-boot. The remaining half — `Connect::connect()`'s cold-boot/long-press `wifiManager->autoConnect()` still depends on `alanswx/ESPAsyncWiFiManager` — is planned for replacement by **Phase 8 item 0** (in-house captive portal). | `src/connect.cpp` (now: `connect()` only) | Cold-boot/long-press portal still pulls in the stale `alanswx/ESPAsyncWiFiManager` dependency; runtime WiFi blips no longer freeze devices (fixed). |
| B13 | **~20 ms of I2C inside a `Ticker` callback.** `display->display()` flushes the whole framebuffer over I2C from timer context (SYS context on ESP8266) every second. The commented-out `attach_scheduled` shows this was already fought once. | `src/hw/oledDisplay.h:114-115` | WiFi jitter/WDT risk on ESP8266; blocks the shared `esp_timer` task on ESP32. |
| B14 | **Per-channel mutex churn + frame tearing in the repeater path.** `DmxRepeater::frame()` calls `dmx.write()` 512 times; each take/give of `dataMutex` (≈20k lock ops/s at 40 fps). The sender task can transmit a frame that is half-old/half-new since the 512 writes aren't atomic as a set. | `src/device/repeater.h:39-46`, `src/dmx/dmx32.cpp:72-83` | Wasted CPU on core 0 and no frame coherence on the DMX line. Needs a bulk `writeFrame()` under one lock. |
| B15 | **`DmxProxy` is a per-device value member wrapping a singleton hardware resource.** Two `REPEATER` entries → `dmx_driver_install()` twice + two core-1 sender tasks on one UART. | `src/device/repeater.h:16`, `src/dmx/dmx32.cpp:37-70` | UART contention/undefined output. Hardware ports must be owned once. |
| B16 | **Blocking serial logging in the DMX hot path.** Every `set()` on relay/dimmer/servo does Arduino-`String` concatenation + `Serial.println` (≈2–3 ms per line at 115200) — per channel change, at up to 44 fps. | `src/device/strobe.cpp:33`, `src/device/relay.h:56-57`, `src/device/dmxServo.h:112` | Visible fade stutter and frame jitter under real console traffic. |
| B17 | **Strobe speed sub-channel is unreachable.** `set()` listens on `channel - 1`, but the dispatcher only delivers `getChannel() … getChannel()+getNumberOfChannels()-1` (and `getNumberOfChannels()` returns 1). Also wraps at channel 0. This is the root cause of "Fix Stroboscope" in the TODO. | `src/device/strobe.cpp:39-44`, `src/artnetHandler.h:54` | Dead feature; confirms the dispatch design needs rework, not the Strobe code alone. |
| B18 | **`flip()`-to-full-brightness is dead code.** `valueOverride` is computed on flip but `update()` never reads it. | `src/device/strobe.cpp:143-158` vs `47-58` | "Manual flip should bring the full light on" doesn't. |
| B19 | **Inverted `isEnabled()` for active-LOW relays.** Returns the raw pin `value`, so an active-LOW relay that is ON reports disabled (status LED shows the opposite state). | `src/device/relay.h:76-79` | Wrong LED feedback on active-low hardware. |
| B20 | **Silence blackout fights the button.** `Device::handle()` re-blacks-out every 5 s of DMX silence (and `frame()` resets the timer so it repeats forever, logging each time). A relay flipped ON by button turns itself OFF ≤5 s later if no console is running. | `src/device/device.h:20-28` | Surprising behavior in exactly the standalone-test scenario the button exists for; README TODO "make blackout optional" is the fix vehicle. |
| B21 | **`#include "version.h"` vs actual `include/Version.h`.** Builds only on case-insensitive filesystems (macOS). `board.h` spells it correctly. | `src/config.h:18`, `src/main.cpp:27` | Linux/CI builds fail. Must be fixed before CI exists. |
| B22 | **Zombie boot on FS-mount failure.** `setup()` `return`s early; `loop()` then runs `button.check()`/`artnet.loop()` against half-initialized state. | `src/main.cpp:104-113` | Undefined behavior instead of a deliberate safe mode. |
| B23 | **`default.json` carries board-specific pins.** `ledPin: 2`, `buttonPin: 0` in the shared `data/` image override the per-board `-D LED_PIN/DEFAULT_BUTTON_PIN` defaults at runtime on every board that flashes `uploadfs`. | `data/config/default.json`, `src/config.cpp:162-171` | e.g. `esp32-devkitc-v4` (LED 14, button 27) gets LED 2 / button 0 until manually reconfigured. Defaults belong to the board layer, not the shared data image. |
| B24 | **`StatusLed` drives PWM with out-of-range magic (1000 on an 8-bit range) and a baked-in active-LOW assumption.** Works today only via clamping behavior of two different `analogWrite` implementations. | `src/hw/statusLed.h:19,62-69` | Wrong/undefined brightness on active-HIGH wiring; semantics by accident. |
| B25 | **PlatformIO's LDF "leaks" `esp_dmx` into ESP8266 builds.** LDF's default `chain` mode textually scans `#include` lines without evaluating `#ifdef`s, so `dmx32.cpp`'s `#ifdef ESP32`-guarded body (→ `dmx.h` → `#include <esp_dmx.h>`) still registers `esp_dmx` as a dependency on every ESP8266 env — even `sonoff_basic`/`sonoff_s31`, where `SONOFF_BASIC` excludes `dmx.h` from `main.cpp` entirely. `esp_dmx` needs ESP-IDF headers (`driver/uart.h`, `freertos/*`) absent from the ESP8266 Arduino core, so LDF then fails to build it. | `platformio.ini` (all 6 ESP8266 envs), `src/dmx/dmx32.cpp:1`, `src/dmx/dmx.h:7-13` | `pio run` failed for every ESP8266 env, not just Sonoff. Fixed in Phase 0 via `lib_ignore = esp_dmx` on each ESP8266 env — should be hoisted into `[common_esp8266]` once Phase 0 item 3 lands. |
| B26 | **`lolin_s2_mini` fails to build out of the box.** `mathieucarbou/ESPAsyncWebServer@^3.4.5`'s `AsyncWebServer::state() const` calls non-`const` `AsyncServer::status()` (from the transitive `AsyncTCP@3.3.2`) — a `-fpermissive`-eligible "passing 'const AsyncServer' as 'this' argument discards qualifiers" hard error across 8 translation units (`AsyncJson`, `AsyncWebServerRequest`, `AsyncWebHeader`, `WebHandlers`, `AsyncEventSource`, `AsyncWebSocket`, `Middleware`, `WebRequest`). **Confirmed pre-existing**: control-tested against the pre-Phase-0 `platformio.ini` (unpinned `platform = espressif32`, no `-std=` override at all) — identical failure, so it predates and is independent of the Phase 0 `gnu++17` unification (item 4). | `platformio.ini` (`[env:lolin_s2_mini]`), `.pio/libdeps/lolin_s2_mini/ESPAsyncWebServer/src/ESPAsyncWebServer.h:1691` | `pio run -e lolin_s2_mini` has never produced firmware on a clean checkout — not a Phase 0 regression, `pio run` is 11/12 both before and after. Real fix belongs with the §1.3.7 webserver-fork unification (deferred — needs `lolin_s2_mini` hardware to validate a fork swap or exact-version pin). **Fixed in Phase 7 item 1** — see Done note. |
| B27 | **`POST /config` + immediate `POST /reboot` can lose the most recent save.** `tools/hil_smoke.py`'s `check_config_roundtrip()` does `POST {universe: 2->3}` (confirmed via GET), then `POST {universe: 3->2}` (confirmed via GET), each followed by only a 0.2s sleep, then `main()` immediately does `POST /reboot`. **Two observed manifestations**: (1) during item 1's `--full` run, post-reboot `GET /config` returned `default.json`'s shape entirely (`host=Art-<chipid>`, `universe=0`, `dmx=[]`, `wifi=[]`) — `.hil/last-boot.log` showed `Loading config from /config/config.json` / `Failed to read file`, with reset reason `USB_UART_CHIP_RESET` (a *second*, hardware reset from `boot_check`'s serial reconnect landing shortly after the soft `ESP.restart()`). (2) during item 2's `hil_smoke.py --soak 0` run, post-reboot `GET /config` returned the **correct** `host`/`dmx` but `universe=3` (the *first* POST's value, not the second/reverted `2`) — i.e. one save behind, config.json itself still readable. Isolated manual repros (`POST` ×2 + `POST /reboot`, no `boot_check` afterward, with both 0.2s and 1s delays) persisted the final value correctly every time. | `tools/hil_smoke.py` (`check_config_roundtrip()` + `main()`'s immediate `/reboot`), `config.cpp`'s `applyPendingUpdate()`/`save()`, LittleFS on `seeed_xiao_esp32s3`, `.hil/last-boot.log` | **Confirmed pre-existing / unrelated to Phase 7 item 1's AsyncWebServer bump or item 2's ArtNet bump** — neither touches `config.cpp`/filesystem/reboot paths, and isolated manual repros are clean on both builds. Manifestation (2) narrows the theory: the *second* POST's `applyPendingUpdate()`/`save()` may not have completed/flushed to flash by the time `POST /reboot`'s handler calls `ESP.restart()`, 0.2s after the GET that confirmed the in-memory value. Manifestation (1)'s full wipe additionally needs a hardware reset (`boot_check`'s DTR/RTS toggle) landing during/just-after that incomplete write. Bench config restored both times via `POST /config` + confirmed across a clean soft reboot. Not blocking Phase 7; candidate fixes for later: add a settle delay before `POST /reboot` in `hil_smoke.py`'s `main()` (band-aid), or make `config.cpp`'s `save()` synchronous/confirmed-flushed before `POST /reboot`'s handler returns (real fix). |
| B28 | **Arduino-ESP32 core 3.x's base image is noticeably larger than core 2.x's, eating flash headroom on the two 4MB ESP32 boards.** Item 3's pioarduino/core-3.0.7 switch (no source/feature changes — `[common_esp32]`'s platform line, `pwm.cpp`'s LEDC rewrite, and an ESP32Servo re-pin only) moved `esp32-devkitc-v4` from 979289/1310720=74.7% flash to 1187277/1310720=90.6%, and `lolin_s2_mini` from ~70.7% (item 1's post-fix figure) to 1150754/1310720=87.8% — roughly +16/+17 points of the 1.25MB app partition, just from the newer IDF 5.1/core 3.0.7 runtime (bigger WiFi/TLS/IDF baseline). The 3.3MB-flash ESP32-S3 boards stay comfortable (~35%). | `platformio.ini` (`[common_esp32].platform`) | Not a regression introduced by this refactor's own code — same effect would occur on `main` with any core-3.x bump. ~123KB headroom remains on `esp32-devkitc-v4`/`lolin_s2_mini`, enough for near-term work, but **future ESP32 feature additions should budget against ~88-91% as the new baseline on these two boards**, not the previous ~75%. If headroom becomes tight, options: a larger partition scheme (less SPIFFS/LittleFS, e.g. `min_spiffs.csv`), or `CONFIG_COMPILER_OPTIMIZATION_SIZE`/IDF size-optimization flags. |
| B29 | **`Connect::reset()` (`WiFi.disconnect(true, true)` + 100ms delay) doesn't reliably erase NVS-stored STA credentials before `POST /reset-wifi`'s immediate `ESP.restart()`.** On the XIAO bench, `POST /reset-wifi` → `disconnectAndRestart()` → `cn->reset()` (`WiFi.disconnect(true,true); delay(100);`) → `ESP.restart()`, followed by a `boot_check` hardware reset, came back up reconnected to the *original* saved network (no portal/AP ever appeared) - `Connect::connect()`'s new `tryStationConnect()` succeeded immediately, with none of the "starting setup portal" log lines. | `src/connect.cpp` (`reset()`, pre-existing/unchanged by Phase 8 item 0), `POST /reset-wifi` (`webApi.cpp`) | Pre-existing - `reset()`'s code is byte-for-byte the same as before this item (ESPAsyncWiFiManager's `connect()` called the same `WiFi.disconnect`). Likely `eraseap=true`'s flash write to the WiFi NVS partition doesn't complete within the 100ms delay before `ESP.restart()` (same family of issue as B27's "write not flushed before restart"). Means `POST /reset-wifi` may not reliably force the captive-portal-on-next-boot behavior users/README expect from "reset WiFi" - candidate fix: increase the delay, or call `esp_wifi_stop()`/verify erasure via `esp_wifi_get_config()` before restarting. Not blocking Phase 8 item 0 (its new portal code is otherwise build-verified and HIL-verified on the happy path); flagged for a future item. |

Smaller correctness notes, fixed in passing: `config.update()` always returns `true` so the reload branch in `api.h:46-50` is dead; `WiFi.hostname()` is the ESP8266 spelling — verify what it actually does on ESP32 (`setHostname` is the native API) (`src/main.cpp:119`); `DmxRepeater::handle()`'s `lastRefreshTime += interval` catch-up can burst after a stall (`src/device/repeater.h:50-53`); `heltec_wifi_kit_8` env sets no `board_build.filesystem` — verify the produced `uploadfs` image is LittleFS like the code expects.

### 1.2 Structural anti-patterns

1. **Includes inside namespaces.** `#include <LinkedList.h>` inside `namespace art` (`src/config.h:31-33`) and `#include "hw/board.h"` inside `namespace art` (`src/config.cpp:2-4`). This only compiles because include guards happen to have fired at global scope first — a change in include order would wrap third-party/Arduino headers in `art::`. Remove; never include inside a namespace.
2. **Header-only modules with non-inline, out-of-class definitions and missing guards.** `api.h`, `hw/logger.h`, `hw/board.h`, `hw/oledDisplay.h` have **no include guard at all**; `relay.h`, `dmxServo.h`, `repeater.h`, `artnetHandler.h`, `oledDisplay.h`, `api.h` define functions/globals (`Ticker apiTicker`, `getAngleValue`, `StatusDisplay::setStatus64`, …) that violate ODR the moment a second translation unit includes them. The codebase survives on the convention "only `main.cpp` includes these."
3. **Global-variable coupling across modules.** `connect.cpp` drives `extern StatusLed *status` declared in `connect.h:16` and owned by `main.cpp`; `dmx32.cpp:6-9` has file-scope mutable `int transmitPin…` globals with collision-prone names. Dependencies should be injected, not reached for.
4. **Raw owning pointers, no RAII, no virtual destructor.** `new` everywhere, never `delete` (acceptable for boot-once objects, but then make them values/`unique_ptr` and say so); `LinkedList<DmxChannel*>` owns by convention; `Device` deleted through base would be UB.
5. **Three names for every concept.** Enum `Dimmable` / class `Strobe` / JSON `"DIMMER"` / README "DIMMER"; enum `Binary` / class `DmxRelay` / JSON `"BINARY"` / README heading "RELAY"; `DmxChannel` is actually a *device config*, not a channel; `DmxProxy` is a *DMX output port*. Naming is the cheapest readability fix available here.
6. **Two parallel data paths into devices** — per-channel `set()` with a `get()`-based change-detect hack, *plus* bulk `frame(universe, data, size)` — and every device receives both on every frame. The change-detect (`data[i-1] != get(i)`) only works because `get()` ignores its argument for 1-channel devices; it can never work for multi-channel devices (which is what blocks NeoPixel, the multi-channel dimmer, and the strobe sub-channel).
7. **Inconsistent device lifecycle.** Relay does `pinMode` in `start()`; Strobe in its constructor; Repeater spawns a FreeRTOS task from its constructor (during the config switch in `setup()`); Servo attaches in `start()`. And `start()` is called by… `ArtnetHandler::init()` (`src/artnetHandler.h:32-34`) — the network class initializes output hardware.
8. **Dead code and drift.** Unused `getRotationalValue()` (`dmxServo.h:65-91`), empty `#ifdef ESP32` block (`dmxServo.h:38-40`), unused `valueOverride`, unused `TICK_DURATION`, commented-out experiments (`main.cpp:107,139-140`, `oledDisplay.h:114`). Removed in Phase 0: dead `-DBUTTON_PIN=0` flag (nothing read `BUTTON_PIN`; code uses `DEFAULT_BUTTON_PIN`), unused `durydevelop/OLED` lib in the heltec env, and `me-no-dev/ESPAsyncUDP` (confirmed unused — only a comment/URL reference inside `ESPAsyncWiFiManager.h`, the ArtNet lib uses `WiFiUDP`).
9. **Layering violation in config.** `configToJson()` reads `WiFi.SSID()/RSSI()` (`src/config.cpp:192-193`) — the persistence model serializes live network state. Info/status belongs in the API layer.
10. **Magic numbers & macro constants.** `CONFIG_BUFFER_SIZE`, `DMX_SILENCE_TIMEOUT`, `40` ms in two places (`repeater.h:10` and `dmx32.cpp` field — drift risk), `0.5f` ticks, `1000`/`200` PWM, `5`/`10`/`127` defaults inline in the JSON parser.
11. **Mixed formatting and logging.** 2-space (`main.cpp`) vs 4-space (everything else); `LOG(String + String)` vs raw `Serial.print` chains (`relay.h:37-42`); no `.clang-format`.

### 1.3 Portability & conditional-compilation problems

1. **`#ifdef` sprawl.** 27 preprocessor conditionals in `main.cpp` alone. The ESP8266-vs-ESP32 WiFi-include dance is copy-pasted in four files; `ESP_FS` is defined independently in `main.cpp` and `config.h`. There is no platform layer — every module knows about every chip.
2. **`SONOFF_BASIC` is a board name used as a feature system.** It excludes whole modules (`dmx/`, strobe, servo, repeater) and is tested in 6+ places. Adding "a board without servo but with DMX" today means inventing another `SONOFF_X`-style macro. Needs granular capability flags (`FEATURE_DMX_PORT`, `FEATURE_SERVO`, `FEATURE_PWM_DIMMER`, `FEATURE_OLED`, `FEATURE_OTA`) derived in one place from a board profile.
3. **Pin configuration split across two weakly-coupled mechanisms.** `board.h` `#ifndef` defaults vs scattered `-D` in `platformio.ini`; `lolin_s2_mini` silently inherits defaults (documented as Gotcha #3 in AGENTS.md — it should not need documenting; it should be impossible).
4. **Divergent C++ standards.** espressif8266 (core 3.x) compiles gnu++17; espressif32 (core 2.x) compiles gnu++11. The same headers compile under two different language versions depending on target.
5. **Platform-specific PWM is broken on ESP32.** The dead third-party `erropix/ESP32 AnalogWrite@0.2` (2020) is pulled in for `analogWrite`, the configured `pwmFreq` is silently ignored on ESP32 (`main.cpp:138-144` TODO), and this dependency is what blocks moving to Arduino core 3.x (which has native `analogWrite`/LEDC). A 20-line `Pwm` wrapper over LEDC removes the lib, honors `pwmFreq`, and unblocks the platform upgrade.
6. **Filesystems differ per platform** (LittleFS on 8266, SPIFFS on ESP32) for no architectural reason. SPIFFS is deprecated in arduino-esp32. Unify on LittleFS behind the platform layer (with an explicit migration decision — see Risks).
7. **Dependency chaos across the 11 envs** (see `platformio.ini`): three different AsyncWebServer sources — `ottowinter/ESPAsyncWebServer-esphome` (8266 envs), `mathieucarbou/ESPAsyncWebServer` (s2_mini only), and *unpinned transitive* (all other ESP32 envs); `ESP32Servo ^0.11.0` vs `^3.0.6` (major-version skew between lolin32/devkitc and XIAO!); `Adafruit SSD1306 ^2.4.3` vs `^2.5.10`; `BusIO ^1.7.2` vs `^1.16.0`. Platforms themselves are unpinned (`platform = espressif32` floats). No `[common]` section — every env repeats `lib_deps`, `extra_scripts`, `monitor_speed`. *Phase 0 update*: `[common]`/`[common_esp8266]`/`[common_esp32]` introduced, platforms pinned, SSD1306/BusIO aligned; the `ESP32Servo` and AsyncWebServer-fork unifications remain — `lolin_s2_mini`'s fork is independently broken regardless (B26), so that fork swap and the `lolin_s2_mini` fix are now one combined hardware-gated task.
8. **`board.h` unconditionally includes `Wire.h`/`SPI.h`** for all non-Sonoff builds, dragging I2C/SPI into boards with no display.
9. **Servo libs differ per platform** (`ESP32Servo` vs `Servo`) — fine, but the `#ifdef` lives in the device class instead of a platform header.

### 1.4 Modern C++ gaps (C++17 is available on every target once §1.3.4 is fixed)

- `enum class DmxType` (currently a plain enum that implicitly converts; `LOG(config.dmx[i]->type)` prints raw ints).
- Default member initializers everywhere a member exists (kills the entire B6 class of bugs); constructor init-lists instead of assignment bodies.
- `std::array<std::unique_ptr<Device>, MAX_DMX_DEVICES>` (or fixed-capacity vector) instead of raw `Device*[]`; factory function returning `std::unique_ptr<Device>`.
- `std::vector<DeviceConfig>` *by value* instead of `LinkedList<DmxChannel*>` (removes a dependency, the O(n) `operator[]`, and manual `delete` loops).
- `override`/`final` on every override (none today — `Strobe::handle()` is the only `override` in the codebase); `virtual ~Device() = default`; `= 0` for the interface.
- `constexpr`/`inline constexpr` constants instead of `#define`; `static constexpr char[]` (or PROGMEM) instead of eight `const String` members per `Config` instance.
- `uint16_t` channel types end-to-end; `nullptr` over `NULL`; `snprintf` over `sprintf`.
- Explicit lambda captures (`[this]`, by-value for locals) — outlaw `[&]` on long-lived callbacks (root cause of B1/B2).
- A tiny RAII lock guard for the FreeRTOS mutex in `dmx32.cpp`.
- Keep it embedded-sane: no exceptions, no RTTI-dependent code, no `std::function` in hot paths, no dynamic allocation after `setup()` (already true in spirit — make it a stated rule).

---

## Part 2 — Target architecture

```
src/
├── main.cpp                 # ~20 lines: construct App, app.setup(), app.loop()
├── app/
│   ├── app.h/.cpp           # owns all subsystems as values/unique_ptr; explicit wiring & boot order
│   ├── deviceFactory.h/.cpp # DmxType -> unique_ptr<Device> (replaces the switch in main)
│   └── safeMode.h/.cpp      # FS-failure / panic boot path (LED pattern + diagnostic AP)
├── core/                    # platform-free, natively unit-testable (no Arduino.h)
│   ├── configModel.h        # DeviceConfig, HardwareConfig, NetworkConfig — value types, defaulted members
│   ├── configCodec.h/.cpp   # ArduinoJson <-> model (ArduinoJson runs on native; testable)
│   ├── dmxTypes.h           # enum class DmxType + to/from string; channel math; DMX constants (513!)
│   └── dimmerLogic.h        # pure strobe/dimmer timing state machine (testable)
├── devices/
│   ├── device.h             # pure interface: uint16 channels, virtual dtor, single dispatch path
│   ├── relay.h/.cpp  dimmer.h/.cpp  servoDevice.h/.cpp  repeater.h/.cpp
├── net/
│   ├── artnetService.h/.cpp # receive + dispatch (slices frame per device span; universe-aware)
│   ├── webApi.h/.cpp        # REST handlers; owns "info" serialization (moves WiFi.SSID out of config)
│   └── wifiService.h/.cpp   # non-blocking reconnect state machine; portal only at boot
├── platform/                # the ONLY place allowed to #if ESP8266/ESP32
│   ├── platform.h           # chip id, restart, heap, hostname, filesystem accessor (one ESP_FS)
│   ├── pwm.h/.cpp           # LEDC on ESP32 (honors pwmFreq), analogWrite on 8266
│   ├── dmxPort.h            # DmxPort interface (renamed DmxProxy) + writeFrame(span) bulk API
│   ├── dmxPort32.cpp        # core-1 sender task (existing pattern, bulk-locked)
│   └── dmxPort8266.cpp      # ESPDMX wrapper
├── hw/
│   ├── statusLed.h          # polarity-correct, range-correct
│   └── oledDisplay.h/.cpp   # rendering only; updates driven from loop(), not Ticker
└── boards/
    ├── board.h              # selects boards/<X>.h from one BOARD_* define; final fallback defaults
    ├── lolin32.h, esp32_devkitc_v4.h, lolin_s2_mini.h, …   # ALL pins + FEATURE_* per board
    └── features.h           # capability flags; SONOFF profile = a set of features, not a code fork
```

Key design decisions:

1. **One dispatch path.** `Device` gets a single `void onDmx(uint16_t universe, const uint8_t* data, uint16_t size)` taking the slice for its declared span (`firstChannel()`, `channelCount()`); the dispatcher does bounds-checking and slicing once, correctly. Per-device change detection becomes the device's own business. This single change unblocks: strobe speed channel, multi-channel dimmer, NeoPixel, per-device universe (repeater TODO), and deletes B3/B17.
2. **The `#ifdef` firewall.** Grep-enforceable rule: `#if defined(ESP8266|ESP32)` may appear only under `src/platform/` and `boards/`. Everything else compiles identically for all targets.
3. **Capabilities, not board names, gate features.** `#ifdef FEATURE_SERVO` etc.; `sonoff_basic` becomes a board profile that turns features off. New boards are a header + an env section, nothing else.
4. **Ownership is explicit.** `App` owns subsystems by value or `unique_ptr`; `DmxPort` is owned once by `App` and handed to the repeater (fixes B15); nothing reaches for externs.
5. **Silence-blackout is policy, not hard-coded behavior**: base-class helper, per-device default, `"blackout": true|false` config flag (delivers a README TODO; fixes the B20 interaction by suspending blackout while manually flipped).
6. **Boot order stays sacred** and gets encoded in `App::setup()` with the WiFi-before-`server.begin()` constraint asserted in one commented place instead of a `!!!!!` comment.

---

## Part 3 — Phased plan

Each phase = one PR-sized unit, all 11 envs built (`pio run`), plus the on-device smoke tests listed. Phases 1–2 deliberately precede file moves so bug fixes stay reviewable as small diffs. From Phase 0.5 onward, "bench test" means the HIL harness: **T1** (`tools/verify.sh --quick`) after every significant change, **T2** (`--full`) at the end of each phase — see Phase 0.5 for the tier definitions.

### Phase 0 — Safety net (no behavior change) — S
1. Fix `version.h` → `Version.h` includes (B21) — prerequisite for any Linux CI.
2. Add include guards / `#pragma once` to `api.h`, `logger.h`, `board.h`, `oledDisplay.h`.
3. `platformio.ini`: introduce `[common]`, `[common_esp8266]`, `[common_esp32]` with shared `lib_deps`/`extra_scripts`/`monitor_speed` via `${...}` interpolation; align skewed lib versions (SSD1306 → `^2.5.10`, BusIO → `^1.16.0`, both confirmed to resolve identically to the old constraints); **pin** `platform =` versions (`espressif8266@4.2.1`, `espressif32@6.6.0`); remove dead deps (`durydevelop/OLED`, dead `-DBUTTON_PIN=0`, `ESPAsyncUDP` — audited, confirmed unused, removed). **Done** for all 12 envs (`pio run` → 11/12, see item 4 + B26 for the 12th).
   - **Deferred** (out of Phase 0's no-behavior-change mandate — unifying these blind across 4 ESP32 boards with no bench hardware risks trading "doesn't compile" for "compiles but misbehaves at runtime"): `ESP32Servo` major-version unification (`^0.11.0` vs `^3.0.6`) and the 3-way AsyncWebServer-fork split (§1.3.7). Bundle with the B26 fix as a future hardware-gated task.
4. Unify the language level: `-std=gnu++17` (+ matching `build_unflags = -std=gnu++11`) on ESP32 envs. **Done**, applied uniformly via `[common_esp32]` to all 6 ESP32 envs including `lolin_s2_mini` — no per-env exception needed, since B26 is a control-tested pre-existing failure independent of this flag.
5. Add `.clang-format` (match dominant 4-space Allman-ish style), `.editorconfig`; one pure-formatting commit. **Done** — `clang-format -i` (Apple clang-format 17, via Xcode toolchain) across all 20 files in `src/`; only 8 needed changes (12 already matched the new style), all whitespace/indentation only — `config.h`/`config.cpp`'s `namespace art { ... }` body dedented one level (`NamespaceIndentation: None`), `main.cpp`'s 2-space indent → 4-space, one stray `(){}`→`() {}` in `artnetHandler.h`. No logic changes (diffs reviewed). Verified: `d1_mini_oled` + `esp32-devkitc-v4` + `sonoff_basic` build clean, bench T1 (`tools/verify.sh --quick`) passes (`BOOT_RESULT=ok`, v2026.1.7).
6. GitHub Actions: matrix build of all 11 envs (with `submodules: recursive`), `cppcheck`/`clang-tidy` job (advisory at first). **Done** — `.github/workflows/ci.yml`: `build` job is a 12-env matrix (`fail-fast: false`, `submodules: recursive`, `~/.platformio` cached per-env keyed on `platformio.ini`'s hash); `lolin_s2_mini` runs with `continue-on-error: true` and a comment pointing at B26, so it stays visible without blocking the other 11. Separate `lint` job runs `cppcheck` over `src/` (`continue-on-error: true`, `missingInclude`/`unusedFunction` suppressed for Arduino-framework noise) — advisory only. `clang-tidy` deferred: needs a `compile_commands.json` per env (`pio run -t compiledb`), which is a heavier lift than this item warrants on its own; revisit alongside Phase 1.
   *Note: the version-increment pre-script bumps `version` on every build — CI's checkout is ephemeral and nothing commits it back, so no special handling needed (documented inline in the workflow).*
7. **Boot sentinel:** last line of `setup()` logs `BOOT OK v<version>` — the deterministic success marker the Phase 0.5 harness keys on (heuristics like "Connected :)" are too fragile). Add `monitor_filters = esp32_exception_decoder` to the ESP32 `[common_esp32]` section for interactive debugging.

**Verify:** `pio run` (all envs) before/after produce working firmware — **11/12 both before and after** (`lolin_s2_mini` is pre-existing-broken, B26; not a regression). Flashed `seeed_xiao_esp32s3` bench + `tools/verify.sh --full` passed clean (build → flash → boot → REST contract → Art-Net E2E → 4 min soak, no heap-leak trend) after the consolidation — no functional diff.

### Phase 0.5 — Hardware-in-the-loop harness (black-box; needs only the Phase 0 sentinel) — M

The bench device becomes the regression gate for every phase that follows. The harness is **deterministic CLI scripts** (exit-code driven) so any agent or human can run one command after a change and trust the answer. It characterizes today's 2026.1.2 behavior at the network/serial boundary — no firmware seams required.

**Bench device: `seeed_xiao_esp32s3`** (XIAO ESP32-S3 — the repeater board from `assets/Xiao-Repeater.jpg`; DMX TX = GPIO3, LED = GPIO21, button = GPIO0 = the XIAO's `B`/BOOT button). Key property: **native USB-CDC, no USB-UART bridge** — the serial port disappears and re-enumerates on every reset, which shapes the harness design below. All HIL commands default to this env via `tools/hil.env`.

**Bench setup (one-time onboarding):**
1. `pio device list` → the XIAO enumerates as `/dev/cu.usbmodem*`; record port + `env=seeed_xiao_esp32s3` in `tools/hil.env`.
2. `pio run -e seeed_xiao_esp32s3 -t upload && pio run -e seeed_xiao_esp32s3 -t uploadfs` (uploadfs once — default config + web UI). *Until B23 lands, `default.json` overrides the LED to pin 2, so the XIAO's built-in LED (GPIO21) won't blink — harmless: the harness keys on serial + HTTP, not the LED. The `B` button doubles as the device button (GPIO0) for manual flip/long-press tests.*
3. Join the captive portal once to store WiFi credentials. **Creds live in NVS and survive reflashes — never run `pio run -t erase` in the routine loop** (that wipes WiFi creds *and* the filesystem; it's a recovery step only, followed by re-onboarding).
4. Once an RS485 adapter is wired to GPIO3, the repeater soak becomes a real-line test exercising the core-1 sender task (R1) on the bench — the single riskiest code path gets hardware coverage.

**Deliverables (`tools/`, all stdlib + pyserial; run with PlatformIO's bundled interpreter `~/.platformio/penv/bin/python` so nothing needs installing):**

| Tool | Job |
|------|-----|
| `boot_check.py` | Resets the board (USB-CDC DTR/RTS sequence), watches serial for N s (default 30). **Pass** = `BOOT OK` sentinel + zero crash signatures + no boot-loop evidence; parses and prints `DEVICE_IP=` from the boot log for the network stages. Writes `.hil/last-boot.log`. Exit codes: 0 ok / 1 crash / 2 timeout / 3 no-port (see recovery note). Built for native USB: reopens the port with retries and counts re-enumerations across the watch window. |
| `artnet_send.py` | Minimal ArtDmx sender (UDP 6454, trivial packet): single frame, ramp, or sustained flood at a given fps. |
| `hil_smoke.py` | REST contract: `GET /heap`, `GET /config` schema check, `POST /config` round-trip on a scratch field, `POST /reboot` → re-run boot check. ArtNet end-to-end: set a configured channel via `artnet_send`, assert the device's serial echo of the `set()` within 1 s. Soak mode: flood at 44 fps × N min, assert windowed-min `/heap` is non-decreasing. |
| `decode_backtrace.py` | Feeds `Backtrace: 0x…` lines through the toolchain's `addr2line` against `.pio/build/<env>/firmware.elf` — failures come back as symbolized stacks, not hex. |
| `verify.sh [env] [--quick\|--full]` | Orchestrator; `env` defaults to the bench device from `tools/hil.env`: build → flash → `boot_check` (`--quick`, ~90 s) → plus `hil_smoke` and a 3–5 min soak (`--full`). On crash, auto-runs the decoder and tails the log. |

**Crash signatures `boot_check.py` fails on** (ESP32 now, ESP8266 set included for when an 8266 joins the bench): `Guru Meditation Error`, `abort() was called`, `assert failed`, `Backtrace:`, `Task watchdog got triggered`, `Brownout detector was triggered`, `CORRUPT HEAP`; 8266: `Exception (`, `wdt reset`, `Soft WDT reset`, `Stack smashing`.

**Boot-loop detection on native USB:** ROM `rst:0x…` banners can be missed entirely on USB-CDC because the port vanishes during reset, so the XIAO check uses firmware-level evidence instead — the early `Starting...` marker (or a second `BOOT OK`) appearing more than once in one watch window, and/or repeated port re-enumeration, fails the check.

*Triage notes:* `Brownout` usually means USB power/cable, not code — labeled separately so we don't chase ghosts. The worst native-USB failure mode is **silent**: firmware that crashes before the USB stack comes up never enumerates at all. `boot_check` reports that distinctly (exit 3, "no port"), and recovery needs a physical hand — hold `B` (BOOT), tap `R` (reset) to enter the ROM bootloader, reflash. The harness says so explicitly when it needs you; an agent can't press the buttons.

**Verification tiers (the cadence for "after every significant change"):**

| Tier | When | Command | Time |
|------|------|---------|------|
| T0 | every build-affecting edit | `pio run -e seeed_xiao_esp32s3` (compile only) | seconds |
| T1 | every significant change / each Phase-1 bug commit | `tools/verify.sh --quick` | ~90 s |
| T2 | end of each phase, before merge | `tools/verify.sh --full` + `pio run` (all 11 envs) | ~10 min |

**Operational constraints:** the serial port is an exclusive resource — flashing, monitoring, and soaking must be serialized (the orchestrator owns the port; never flash while a soak holds it). Long soaks run in a background shell; everything else is foreground because the next step depends on the result.

**Verify (of the harness itself):** run T2 against unmodified 2026.1.2 — it must pass clean; then deliberately break boot (early `abort()` on a scratch branch) — it must fail with a decoded stack. A harness that has never failed isn't trusted.

**Known harness flake:** `tools/verify.sh --quick` occasionally reports `BOOT_RESULT=crash # boot-loop` (`STARTING_COUNT=2`) immediately after `pio run -t upload`, on firmware that boots fine. Cause: the XIAO's native USB-CDC re-enumerates on every reset, and the tail of the post-upload auto-reboot's "Starting..." can land in the OS receive buffer before `boot_check.py` opens the port and issues its own DTR/RTS reset, so both boots get counted. `boot_check.py` now calls `ser.reset_input_buffer()` before resetting (mitigates but doesn't fully eliminate it - it's USB-enumeration timing, not a buffering bug per se). If `--quick` reports a boot-loop with no actual crash signature and a clean-looking log, re-run `tools/boot_check.py` (no reflash needed) or re-run `--quick`; this is a harness artifact, not a regression to chase.

### Phase 1 — Correctness fixes on the current structure — M
One commit per bug, in this order (independent unless noted):
1. B10: `virtual ~Device() = default`, `getNumberOfChannels() = 0`, `override` everywhere. **Done** — `device.h` gets the virtual destructor and a pure-virtual `getNumberOfChannels()` (`Device` is now formally abstract; confirmed no code constructs a bare `Device`). All four subclasses (`DmxRelay`, `DmxServo`, `DmxRepeater`, `Strobe`) mark every overridden method `override` (the pre-existing `Strobe::handle() override` was already correct). `Strobe::start(uint8_t)` remains a declared-but-undefined overload — pre-existing, out of scope for B10. Verified: d1_mini_oled + esp32-devkitc-v4 + sonoff_basic build clean; bench T1 passes (`BOOT_RESULT=ok`, v2026.1.10, IP/WiFi/ArtNet/Repeater all correct).
2. B6: default member initializers across `Device`, `Strobe`, `Config`. **Done** — surgical fix targeting exactly the read-before-write members identified in the bug table: `Device::lastChange = 0` (so `millis() - lastChange` doesn't underflow on a fresh object and trigger a spurious "DMX timeout" blackout in the first `handle()` call), `Strobe::value/adjustedActiveValue/adjustedInactiveValue = 0` (so `get()` and a pre-DMX `flip()` see deterministic zeros instead of garbage PWM values), `Config::universe = 0` (so `artnet.init(config.universe, ...)` gets 0 instead of garbage if `universe` is ever absent from a config JSON — `default.json` does set it, but the `POST /config` partial-update path doesn't guarantee it). `Device::universe`/`channel` (always set by every subclass ctor) and the rest of `Strobe`'s/`Config`'s always-initialized members are left as-is — broader "defaults everywhere + ctor init-lists" modernization remains a §1.4/Phase 5 item, kept separate to avoid overlap. Verified: d1_mini_oled + esp32-devkitc-v4 + sonoff_basic build clean (2026.1.11); bench T1 passes (`BOOT_RESULT=ok`, v2026.1.11, IP/WiFi/ArtNet/Repeater all correct).
3. B1/B2: lambda captures — `[this]` / by-value; never `[&]` on stored callbacks. **Done** — `artnetHandler.h`'s Art-Net dispatch lambda (B1) changed `[&]` → `[this]`, and the body's bare `universe` (which was actually binding to `init()`'s *parameter*, shadowing the `this->universe` member, and dangling once `init()` returned) is now explicitly `this->universe` — a stable member set once in `init()`. `api.h`'s `/reset-wifi` handler (B2) changed `[&]` → `[connect]`, copying the `Connect*` pointer value into the closure instead of holding a dangling reference to `setupApi()`'s `connect` parameter. The other `[&]` captures in `api.h` (`/config` GET/POST handlers) were checked and left as-is: they only capture `config`, which is itself a reference parameter bound to the long-lived global `Config` — capturing a reference by reference re-binds to the same underlying object, not to the parameter slot, so there's no dangling-reference risk there. Verified: d1_mini_oled + esp32-devkitc-v4 + sonoff_basic build clean (2026.1.12); bench T1 passes (`BOOT_RESULT=ok`, v2026.1.12, IP/WiFi/ArtNet/Repeater all correct).
4. B4: `uint16_t` channels end-to-end (config → ctor → `set`/`get` → dispatch loop). **Done** — `Device::channel` and `getChannel()` are now `uint16_t`; `Device::set()`/`get()` and every override (`DmxRelay`, `DmxServo`, `Strobe`) plus all three constructors take `uint16_t channel`/`dmxChannel` instead of `uint8_t`. `config.dmx[i]->channel` (already `uint16_t` in `config.h`) now flows into the constructors without truncation. `DmxRepeater` needed no changes (doesn't override `set`/`get`, hardcodes `channel = 0`). The `artnetHandler.h` dispatch loop (`int i`, already 32-bit) needed no type change — it was the `get(uint8_t)`/`set(uint8_t,...)` parameter types that truncated `i` for channels ≥ 256, which is now fixed. Verified: d1_mini_oled + esp32-devkitc-v4 + sonoff_basic build clean (2026.1.13); bench T1 passes (`BOOT_RESULT=ok`, v2026.1.13, IP/WiFi/ArtNet/Repeater all correct).
5. B3: dispatcher skips the per-channel loop for frame-consuming devices (`channelCount() == 512` interim test; properly typed in Phase 5) and clamps `i >= 1`. **Done** — `artnetHandler.h`'s dispatch lambda now `continue`s past the per-channel `get()`/`set()` loop entirely for any device reporting `getNumberOfChannels() == 512` (currently only `DmxRepeater`, whose `frame()` already copied the whole packet via `DmxProxy::write()` - the per-channel pass was 512 redundant no-op virtual calls plus the `data[-1]` OOB read at `i=0`). For all other devices, the loop body now has an `if (i < 1) continue;` guard, so a misconfigured `channel: 0` (e.g. `configFromJson`'s `channel["channel"] | 0` default) can no longer produce `data[-1]`. `512` is written as a literal with a comment (matches `DMX_CHANNELS` in `dmx/dmx.h`, which isn't includable from `artnetHandler.h` under `SONOFF_BASIC` - Gotcha #4); a shared/typed constant is a Phase 5 item. Verified: d1_mini_oled + esp32-devkitc-v4 + sonoff_basic build clean (2026.1.14); bench T1 passes (`BOOT_RESULT=ok`, v2026.1.14, IP/WiFi/ArtNet/Repeater all correct, repeater init unaffected). A live-frame repeater throughput test is deferred to B5, which explicitly calls for one.
6. B8: clamp config parsing to `MAX_DMX_DEVICES` at load; pass the clamped count everywhere. **Done** — `configFromJson()` (`config.cpp`) now stops `dmx.add()` once `dmx.size() >= MAX_DMX_DEVICES`, with a `LOG` warning if the JSON `dmx` array has more entries than that ("only the first N will be used"), so `config.dmx.size()` can never exceed `MAX_DMX_DEVICES` after `load()`. `main.cpp::setup()` introduces a `deviceCount = min(config.dmx.size(), MAX_DMX_DEVICES)` local (belt-and-braces given the config-side clamp) and reuses it for the `dmx_devices[]` instantiation loop, the `longName` device-summary loop, and the `artnet.init(..., dmx_devices, deviceCount)` call — previously all three used `config.dmx.size()` directly, which is exactly the unclamped value `artnetHandler.h:48` iterates `devices[k]` over (the OOB-read path B8 names). `handleEvent`'s button-flip loop and `loop()`'s `handle()` loop already had `&& i < MAX_DMX_DEVICES` guards and needed no change. Verified: d1_mini_oled + esp32-devkitc-v4 + sonoff_basic build clean (2026.1.15); bench T1 passes (`BOOT_RESULT=ok`, v2026.1.15, IP/WiFi/ArtNet/Repeater all correct).
7. B7: null-check `dmx_devices[0]` in the button handler. **Done** — `handleEvent`'s `kEventPressed` case (`main.cpp`) changed `if (config.dmx.size() > 0)` to `if (config.dmx.size() > 0 && dmx_devices[0])` before calling `dmx_devices[0]->isEnabled()`. `dmx_devices[0]` is `NULL` whenever `config.dmx[0]->type` falls into the `switch`'s `default:` case in `setup()` (an unrecognized/`Disabled` type, or any non-`Binary` type on `SONOFF_BASIC` builds where `Servo`/`Dimmable`/`Repeater` aren't compiled in) — previously this was a guaranteed null-pointer dereference (crash/reboot) on every button press for such a config. The fix is a pure guard; the flip loop just above it (`for (... && i < MAX_DMX_DEVICES; i++) if (dmx_devices[i]) dmx_devices[i]->flip();`) was already correctly null-checked and needed no change. Verified: d1_mini_oled + esp32-devkitc-v4 + sonoff_basic build clean (2026.1.16); bench T1 passes (`BOOT_RESULT=ok`, v2026.1.16, IP/WiFi/ArtNet/Repeater all correct).
8. B9: `save()` always writes `/config/config.json`; `load()` falls back to default. **Done** — removed the `_fileName` member entirely; it was set once in the constructor based on `ESP_FS.exists(CONFIG_FILE)` at boot and then used by both `load()` and `save()`, so on a fresh device (no `/config/config.json` yet) `_fileName == DEFAULT_FILE` and the first `POST /config` would `remove()` and overwrite `/config/default.json` - destroying the factory-default config permanently, with no `config.json` ever created. `save()` now always targets `CONFIG_FILE` (guarded with `ESP_FS.exists()` before `remove()`, so the very-first save on a fresh device doesn't log a spurious "Failed to delete file"). `load()` now computes its source file itself on every call - `CONFIG_FILE` if present, else `DEFAULT_FILE` with the existing "Config file does not exist, using default" log - rather than relying on constructor-time state, so the fallback is correct however many times `load()` is called. Verified: d1_mini_oled + esp32-devkitc-v4 + sonoff_basic build clean (2026.1.17); bench T1 passes (`BOOT_RESULT=ok`, v2026.1.17, IP/WiFi/ArtNet/Repeater all correct). A live `POST /config` on a freshly-erased filesystem (confirming `config.json` gets created and `default.json` survives) wasn't exercised on the bench this session - the fix is mechanical (every `_fileName` use-site replaced with the correct constant) and covered by the build+boot pass.
9. B5 + B14 + B15: `DmxPort` buffer 513, channels 1–512 valid; add `writeFrame(const uint8_t*, uint16_t)` doing one `memcpy` under one lock (preserve copy→release→send pattern in the task); repeater uses it; make the port a single shared instance. **Done** —
   - **B5** (`dmx.h`/`dmx32.cpp`): `DMX_CHANNELS` (512, the channel count - unchanged, still used by `getNumberOfChannels()` and the artnetHandler.h `==512` check) is now distinct from a new `DMX_BUFFER_SIZE = DMX_CHANNELS + 1 = 513` (start code + 512 channel slots), used for `data[]`/`localBuf[]` sizing and every `dmx_write`/`dmx_send_num`/`memcpy`/`memset` call in `dmx32.cpp`. `DmxProxy::write()`'s bound changed from `channel < DMX_CHANNELS` to `channel <= DMX_CHANNELS`, so channel 512 (`data[512]`) is now reachable - on ESP32 all 512 channels round-trip correctly. On ESP8266 (`dmx8266.cpp`), the vendored ESPDMX library has a hardcoded `uint8_t dmxData[512]` global - `chanSize=512` lets `update()` transmit `dmxData[0..511]` (start code + channels 1-511, one more than before), but `write(512,...)` would index `dmxData[512]`, one past the array, corrupting the adjacent `chanSize` global. `init()` now requests `DMX_CHANNELS` (512, was 511) and `write()` explicitly rejects `channel >= DMX_CHANNELS`, so channel 512 stays a documented no-op on ESP8266 (library limitation, not our off-by-one) while channels 1-511 now work (previously only 1-510 were actually transmitted).
   - **B14** (`dmx.h`/`dmx32.cpp`/`dmx8266.cpp`/`repeater.h`): added `DmxProxy::writeFrame(const uint8_t *frameData, uint16_t size)`. On ESP32 it's one `xSemaphoreTake`/`memcpy(&data[1], frameData, size)`/`xSemaphoreGive` for the whole frame (size clamped to 512), replacing 512 individual `write()` calls (512 lock/unlock pairs -> 1) and eliminating the tearing risk (the sender task can no longer observe a half-old/half-new buffer mid-update). On ESP8266 (no mutex to economize on) it loops `write(i+1, frameData[i])`, which naturally excludes channel 512 via `write()`'s bound. `DmxRepeater::frame()` is now just `dmx.writeFrame(data, size); Device::frame();`. The core-1 sender task's copy-under-mutex-then-release-before-blocking-I/O ordering (AGENTS Gotcha #5) is unchanged.
   - **B15** (`dmx.h`/`repeater.h`): `DmxProxy` gained `static DmxProxy &instance()` (function-local `static DmxProxy proxy`, header-only singleton accessor). `DmxRepeater::dmx` changed from a value member to `DmxProxy &dmx`, initialized from `DmxProxy::instance()` in the ctor init-list. `DmxProxy::init()` (both platforms) is now idempotent via a new `initialized` flag, returning immediately on a second call. Together: if `config.dmx` ever has two `REPEATER` entries, both `DmxRepeater`s resolve to the *same* `DmxProxy`, and only the first `init()` call installs the driver / starts the core-1 sender task - no double `dmx_driver_install()`, no second sender task on the same UART.

   Verified: d1_mini_oled (compiles `dmx8266.cpp`) + esp32-devkitc-v4 (compiles `dmx32.cpp`) + sonoff_basic (confirms `dmx/` stays excluded under `SONOFF_BASIC`, unaffected) all build clean (2026.1.18). Bench: `tools/verify.sh seeed_xiao_esp32s3 --full` with `VERIFY_SOAK_MINUTES=0` - the initial `boot_check` hit the documented USB-CDC re-enumeration flake (line ~195: `STARTING_COUNT=2`/`BOOT_OK_COUNT=1`, log otherwise clean, no backtrace) and `verify.sh` exited early per its `on_crash` path; a standalone `tools/boot_check.py` re-run (no reflash) immediately returned `BOOT_RESULT=ok` v2026.1.18, confirming the harness-flake diagnosis. `tools/hil_smoke.py` (soak skipped via `--soak 0`) then ran end-to-end and **all checks passed**: `GET /heap`, `GET /config schema` (`dmx=['REPEATER']`), `POST /config` round-trip (exercises B9's save()/load() too), reboot + post-reboot boot_check, and **Art-Net E2E sent a full 512-channel ArtDmx frame to the REPEATER device - core-1 sender task survived with no crash** (`SMOKE_RESULT=pass`). Confirming that DMX channel 512 specifically reaches the RS485 wire still requires an external RS485/DMX analyzer (`hil_smoke.py`'s own "use RS485 fixture" note) - not available to this session; the buffer-sizing and dispatch-path fix is otherwise fully exercised.
10. B16: logging discipline — introduce leveled `LOG_DEBUG/INFO/WARN` in `logger.h` (compile-time threshold per env); hot-path `set()` logs become `LOG_DEBUG`, default threshold INFO. **Done** — `logger.h` gained `LOG_LEVEL_NONE/WARN/INFO/DEBUG` (0-3) and a `LOG_LEVEL` macro defaulting to `LOG_LEVEL_INFO` (override per-env via `-D LOG_LEVEL=LOG_LEVEL_DEBUG` in `build_flags`); `LOG_WARN(x)`/`LOG_INFO(x)`/`LOG_DEBUG(x)` expand to `Serial.println(x)` when `LOG_LEVEL` is high enough, else expand to *nothing* - the existing `LOG`/`LOGGER` macros are unchanged for everything else. Converted the three hot-path `set()` logs the bug table names to `LOG_DEBUG`: `strobe.cpp:33,41` (`Strobe::set()`'s Dimmer and Dimmer-Strobe branches), `relay.h:56-57` and `dmxServo.h:112-113` (each collapsed two `Serial.print`/`println` calls into one `LOG_DEBUG("...: " + String(value))`, matching the concatenation style `strobe.cpp` already used; both files gained `#include "../hw/logger.h"`). `relay.h`'s `flip()` log ("Relay: FLIP: ") and the one-time constructor logs (`strobe.cpp:5`, `relay.h`/`dmxServo.h` pin/channel/universe banners) aren't in the per-frame `set()` path and were left as plain `Serial.print`/`println`. Verified the elimination is real, not just "compiles clean": at the default `LOG_LEVEL` (INFO), `strings` on `d1_mini_oled`'s `firmware.elf` shows none of `"Relay: "`, `"Servo: "`, `"Dimmer: "`, `"Dimmer Strobe: "` present (while the unrelated `"Relay: FLIP: "` remains) - both the `Serial.println` call *and* its `String` concatenation are fully compiled out; rebuilding the same env with `PLATFORMIO_BUILD_FLAGS="-D LOG_LEVEL=LOG_LEVEL_DEBUG"` makes all four strings reappear, confirming the per-env override path. Build matrix (d1_mini_oled/esp32-devkitc-v4/sonoff_basic) clean (2026.1.19); bench T1 passes (`BOOT_RESULT=ok`, v2026.1.19, IP/WiFi/ArtNet/Repeater all correct). The "QLC+ fade smoothness" bench check from the Verify line isn't directly observable on this bench - the XIAO is configured with a `REPEATER` device only, no `DIMMER` - so the binary-level before/after comparison above stands in for it; a live fade-stutter check needs a DIMMER-configured board.
11. B12: `wifiService` reconnect state machine — runtime loss → periodic non-blocking `WiFi.reconnect()` (lights keep running); captive portal only at cold boot. *(Behavior change vs README — deliberate; document.)* **Done** — `connect.h` gained `bool wifiConnected = true` tracking the last-known WiFi state across `loop()` ticks. `Connect::loop()` (`connect.cpp`) no longer calls `connect()`/`wifiManager->autoConnect()` on a runtime drop: on the first tick `WiFi.isConnected()` is false it logs "WiFi connection lost, reconnecting in background" once and sets `StatusLed::Connecting`, then every `REFRESH_INTERVAL` (1s) while still down it calls non-blocking `WiFi.reconnect()` (returns immediately, retries with the previously-used credentials) - `loop()` itself never blocks, so `artnet.loop()`, every `dmx_devices[i]->handle()`, `button.check()`, etc. keep running and lights hold their last value. On reconnect it logs "WiFi reconnected" + the new IP once and restores `StatusLed::Connected`. `Connect::connect()` (the blocking `wifiManager->autoConnect()` / captive-portal path) is unchanged except it now sets `wifiConnected = true` on success - it remains the *only* path that can open the captive portal, and it's cold-boot-only as before (`setup()` is the only caller). This also resolves the second half of B12's symptom ("portal resets the shared AsyncWebServer, REST/OTA lost after a runtime reconnect") as a side effect: since the portal is never invoked again after boot, nothing ever calls `server->reset()` on the shared `AsyncWebServer` post-boot. README's WiFi section ("WiFi connection is constantly being checked every second...") rewritten to document the new behavior - runtime loss reconnects non-blockingly in the background with everything else still running, and the Captive Portal is cold-boot-only. Verified: build matrix (d1_mini_oled/esp32-devkitc-v4/sonoff_basic, all compile `connect.cpp`) clean (2026.1.20); bench T1 passes (`BOOT_RESULT=ok`, v2026.1.20) with a clean boot log - no spurious "WiFi connection lost"/"WiFi reconnected" lines appear during normal cold-boot `autoConnect()`, confirming `wifiConnected`'s initial state and the post-`connect()` set are correct. The Verify line's "WiFi-router-off-mid-fade" check needs power-cycling the bench's WiFi router/AP - shared infrastructure outside this session's reach (same category as B5's RS485-analyzer and B16's DIMMER-board notes) - so it wasn't run live; `WiFi.reconnect()`'s non-blocking contract and the absence of any blocking call on the disconnected-loop path were instead confirmed by code inspection and the clean build.
12. B13: OLED refresh from `loop()` via millis-schedule; Ticker only sets a "due" flag (or is removed). **Done** — `oledDisplay.h`'s `Ticker _ticker` (1Hz, calling `setStatus()` - which ends with `display->display()`, a ~20ms I2C flush - directly from timer/ISR context) is removed entirely, along with `staticTick()`, the dead `TICK_DURATION` constant, and the commented-out `attach_scheduled` line it was paired with (the "this was already fought once" evidence B13 names). `StatusDisplay` gains a `loop()` method and a `DISPLAY_REFRESH_INTERVAL = 1000`/`lastRefreshTime` millis-schedule pair, following the same pattern as `Connect::loop()`/`DmxRepeater::handle()`; `main.cpp::loop()` calls `statusDisplay->loop()` (under the existing `#ifdef OLED_SSD1306`) after `connect.loop()`. `setStatus()` itself is unchanged - its `ticksForPage`/`ticksForMessage` counters are counted in calls-to-`setStatus()`, and `loop()` calls it at the same ~1Hz cadence the Ticker did, so `TICKS_PER_PAGE`/`TICKS_PER_MESSAGE` (page-every-2s, message-for-4s) behave the same. Net effect: the I2C transfer now happens on the main task/loop (like every other device's `handle()`), not from `Ticker`'s timer-ISR context (SYS context on ESP8266; the shared `esp_timer` task on ESP32) - removing the WiFi-jitter/soft-WDT and other-timers-blocked risk B13 names. One landmine found along the way: naming the new member `REFRESH_INTERVAL` (matching `Connect`'s) initially broke every ESP8266+Servo build (`d1_mini_oled`, `heltec_wifi_kit_8`) with `Servo.h:57:37: error: expected unqualified-id before numeric constant` - ESP8266 `Servo.h` `#define`s `REFRESH_INTERVAL` to `20000` (its own PWM refresh period) as an unscoped global macro, and since `dmxServo.h`/`Servo.h` is included earlier in `main.cpp` than `oledDisplay.h`, the macro substituted into the new declaration (`static const unsigned long 20000 = 1000;`). Renamed to `DISPLAY_REFRESH_INTERVAL` with a comment explaining why, and the collision is gone. Verified: build matrix (d1_mini_oled/esp32-devkitc-v4/sonoff_basic) **plus** both other OLED envs (`heltec_wifi_kit_8` - 32px ESP8266, `seeed_xiao_esp32s3_oled` - 64px ESP32-S3) all build clean (2026.1.21); bench T1 passes (`BOOT_RESULT=ok`, v2026.1.21). The bench (`seeed_xiao_esp32s3`) has no OLED, so `OLED_SSD1306` is undefined there and the new `loop()`/`setStatus()` code isn't exercised at runtime by T1 - a live "does the page-cycling/I2C-on-main-task actually look right on a screen" check needs an OLED-equipped board (`d1_mini_oled`/`heltec_wifi_kit_8`/`seeed_xiao_esp32s3_oled`), same category as B5's RS485-analyzer, B16's DIMMER-board, and B12's router-power-cycle notes.
13. B11: `POST /config` handler only parses + stages; apply/save happens on the next `loop()` iteration (single-threaded apply; removes the cross-task mutation). **Done** — `Config` (`config.h`/`.cpp`) gained a fixed `char _pendingJson[CONFIG_BUFFER_SIZE]` buffer + `volatile bool _hasPending`, plus (ESP32 only) a `portMUX_TYPE _pendingMux`. `stageUpdate(JsonVariant)` `serializeJson()`s the incoming payload into `_pendingJson` and sets `_hasPending`; `applyPendingUpdate()` (early-returns if nothing's pending) `deserializeJson()`s it back into a local `StaticJsonDocument<CONFIG_BUFFER_SIZE>`, clears `_hasPending`, then runs the *original* `update()`/`save()`-or-`load()`/`serialize()`+`LOG()` sequence unchanged. The buffer+flag handoff is wrapped in `portENTER_CRITICAL`/`portEXIT_CRITICAL(&_pendingMux)` on ESP32 (cheap cross-core spinlock, same family of primitive `dmx32.cpp` already relies on for `dataMutex`); ESP8266 has no such section at all, since ESPAsyncTCP callbacks there run via `schedule_function()` from `loop()`'s own context - the cross-task race B11 names is ESP32-specific, exactly as the bug table says ("On ESP32, AsyncWebServer handlers run in the `async_tcp` task"). `api.h`'s `POST /config` handler (the `async_tcp`-task lambda) now does only `config.stageUpdate(json)` and responds immediately - `202 {"status":"pending"}` on success, `500 {"error":"update too large"}` if the payload doesn't fit `_pendingJson` (in practice unreachable: anything that doesn't fit `CONFIG_BUFFER_SIZE` couldn't be `save()`d either, per Gotcha #7). `main.cpp::loop()` calls `config.applyPendingUpdate()` as its first statement, before the `config.dmx[i]`-reading device loop, button handler, and (since B13) `StatusDisplay::loop()` - so `cleanupDmx()`/`cleanupWiFi()`/the `LinkedList` rebuild now run on the same single task as every reader of `config.dmx`/`config.wifi`, eliminating the UAF/race window entirely (covering both halves of the original bug-table entry, including the `oledDisplay.h:140,178` half that B13 had already moved onto the main task as a side effect). `update()`'s always-`true` return (making the `load()`-on-failure branch dead) is pre-existing and left as-is - out of scope here. README's `POST /config` section documents the new `202`/`{"status":"pending"}` response and that clients should `GET /config` afterward to see the applied result. Verified: build matrix (d1_mini_oled/esp32-devkitc-v4/sonoff_basic) clean (2026.1.22); bench T1 passes (`BOOT_RESULT=ok`, v2026.1.22, IP `192.168.4.26`). The Verify line's "POST /config under ArtNet load" check **was run live** (unlike the deferred checks for B5/B12/B13/B16): with `tools/artnet_send.py --flood --duration 5` (44 fps to the REPEATER) running, `curl -X POST /config` echoing the bench's current `host`/`hw`/`dmx` back returned `202 {"status":"pending"}` immediately; `.hil/last-boot.log` (captured via `boot_check.py --no-reset`) shows, on the very next `loop()` tick, `PUT /config` → `Applying staged config update` → `Updated config` → `Saving config...`/`Config saved` → the full post-apply config dump (`_needReboot: true`, expected per `update()`'s unconditional `_dirty = true` - pre-existing, unrelated to B11); `BOOT_RESULT=timeout` (no crash, no reboot - the correct outcome for a no-reset watch window) and a follow-up `GET /config`/`GET /heap` (252792 B free) confirm the device stayed healthy and the new config persisted, with the ArtNet flood running throughout.
14. B19, B24: relay `isEnabled()` polarity; `StatusLed` gets explicit on/off duty values within range and respects `_onValue`. **Done** — **B19** (`relay.h`): `DmxRelay::isEnabled()` returned the raw pin `value` (0/1 = LOW/HIGH), which only matches "is the relay active" for active-HIGH wiring (`active_value == HIGH`, so `value == active_value` whenever `value` is truthy). For active-LOW wiring (`active_value == LOW`), an ON relay drives the pin LOW (`value == 0`) and `isEnabled()` returned `false` - exactly inverted, so `main.cpp`'s button handler (which calls `status->set(StatusLed::ON)` or `status->set(StatusLed::OFF)` based on `dmx_devices[0]->isEnabled()`) shows the opposite LED state from the relay's actual output. Fixed to `return value == active_value;`, the polarity-independent "is the pin currently driven to its active level" check; for active-HIGH wiring this is equivalent to the old `return value` (no behavior change there), for active-LOW it's now correct. `flip()` (`value = !value`) needed no change - it already toggles the raw 0/1 pin value, which always lands on the other of `{active_value, inactive_value}` since those are always `{LOW, HIGH} = {0, 1}` in some order. `Strobe::isEnabled()` was checked too: it returns a semantic `enabled` bool set/cleared by `flip()`/`start()`/`stop()`, independent of pin polarity - a different, already-correct design, not the B19 bug, left as-is. **B24** (`statusLed.h`): `STATUS_PWM_OFF = 1000` and `STATUS_PWM_ON = 200` are replaced with three named, in-range (0-255) constants - `PWM_MAX = 255` (the 8-bit `analogWrite` duty range: ESP8266 via `main.cpp`'s `analogWriteRange(255)`, ESP32 via the `ESP32 AnalogWrite` library's `analogWrite(pin, value, valueMax = 255)` default) and `STATUS_BRIGHTNESS_ON = 55` / `STATUS_BRIGHTNESS_OFF = 0` - plus a new private `setBrightness(uint8_t brightness)` that maps a 0-255 "logical brightness" to the actual `analogWrite` duty via `_onValue == HIGH ? brightness : PWM_MAX - brightness`. `analogWrite`'s duty is time spent at logic HIGH, so for an active-LOW LED (`_onValue == LOW`, the only value `main.cpp` ever constructs `StatusLed` with: `new StatusLed(config.hardware.ledPin, LOW)`) more HIGH time means *dimmer*, hence the inversion. `set()`'s `Connected`/`ON` and `OFF` cases now call `setBrightness(STATUS_BRIGHTNESS_ON)` / `setBrightness(STATUS_BRIGHTNESS_OFF)` instead of `analogWrite(_ledPin, STATUS_PWM_ON/OFF)` directly. Checked the vendored `ESP32 AnalogWrite` source (`analogWrite.cpp`: `duty = ((levels-1)/valueMax) * min(value, valueMax)`) to confirm the bug table's "works only via clamping" claim - `min(1000, 255) == min(255, 255)`, so the old out-of-range `1000` produced the *identical* duty to `255` on both platforms, by accident. For the active-LOW case (the only wiring in use today), the new path produces byte-identical `analogWrite` arguments to the old code: `setBrightness(55) -> analogWrite(_ledPin, 255-55=200)` (was `STATUS_PWM_ON=200`) and `setBrightness(0) -> analogWrite(_ledPin, 255-0=255)` (was `STATUS_PWM_OFF=1000`, clamped to `255`) - so this is a pure generalization (now also correct for hypothetical active-HIGH wiring) with no behavior change on any currently-deployed board. `Connecting`/`CaptivePortal` (Ticker-driven blink, doesn't call `analogWrite`) and `stop()`/the unreachable `default:` case are unchanged - outside the bug table's cited lines (`statusLed.h:19,62-69`). Verified: build matrix (d1_mini_oled/esp32-devkitc-v4/sonoff_basic) clean (2026.1.23); bench T1 passes (`BOOT_RESULT=ok`, v2026.1.23, IP `192.168.4.26`) - the boot log shows the `Status::Connecting`(0) -> `Status::Connected`(2) transitions exercising the new `setBrightness()` path with no crash. A live relay-flip + LED-brightness bench check (the natural end-to-end test for both bugs together) needs a `BINARY`-type device configured; the bench is REPEATER-only (`Art32-3BD8 REPEATER #0`) - same hardware-dependent category as B16's DIMMER-board and B12's router-power-cycle notes - so the byte-identical-`analogWrite`-arguments argument above stands in for the active-LOW case, which is the only polarity actually deployed.
15. B18/B17 minimal: wire `valueOverride` into `update()`; leave full strobe redesign to Phase 5 (root cause is dispatch). **Done** — **B18** (`strobe.cpp`): `Strobe::update()`'s active branch wrote `analogWrite(pin, adjustedActiveValue)` directly, so `flip()`'s computed `valueOverride` (set to `adjustedMaxValue` - full brightness - when flipping ON with `value == 0`, per the "Manual flip should bring the full light on" comment) was never applied: `update()` re-wrote `adjustedActiveValue` (still `0`, since no DMX had set it) right over it, so a manual flip with no console running did nothing. Fixed minimally: `update()`'s active branch now writes/logs `valueOverride` instead of `adjustedActiveValue`; `set()`'s Dimmer branch (`dmxChannel == channel`) gains one line, `valueOverride = adjustedActiveValue`, keeping the two in sync during normal DMX-driven operation so this is a no-op there. `flip()` itself needed no change - its existing `valueOverride = value > 0 ? adjustedActiveValue : adjustedMaxValue` (flip ON) / `valueOverride = adjustedInactiveValue` (flip OFF) was already correct, just previously discarded. Net effect: flip-to-ON with `value == 0` now drives the pin to `adjustedMaxValue` (full brightness) until the next DMX frame on the dimmer channel calls `set()` again and resyncs `valueOverride = adjustedActiveValue`, returning control to the console - matching README's "DIMMER: button enables/disables -> On/Off" (2021.4) intent and the in-code comment. `update()`'s inactive (`else`) branch is untouched (`adjustedInactiveValue`, used when `enabled == false` and when `state == inactiveState` during stroboscope cycling) - it was already correct, not part of B18. **B17** (the `channel - 1` stroboscope sub-channel, unreachable per the dispatcher's `getChannel()..getChannel()+getNumberOfChannels()-1` range with `getNumberOfChannels() == 1`) is unchanged, as item 15 specifies - its root cause is the dispatch/multi-channel design (§1.2 structural anti-pattern 6), deferred to Phase 5; `Strobe::set()`'s `else if (dmxChannel == channel - 1)` branch and `Strobe::handle()`'s strobe-interval logic remain dead code, same as before. Verified: build matrix (d1_mini_oled/esp32-devkitc-v4/sonoff_basic) clean (2026.1.24) - `sonoff_basic` confirms `strobe.cpp` stays excluded under `SONOFF_BASIC` and is unaffected. Bench T1 passes (`BOOT_RESULT=ok`, v2026.1.24, IP `192.168.4.26`). The bench (`seeed_xiao_esp32s3`) is configured REPEATER-only with no `DIMMER` device, so `Strobe::set()`/`update()`/`flip()` aren't exercised at runtime by T1 - same hardware-dependent category as B16's DIMMER-board and B12's router-power-cycle notes; this 3-line change confined to two adjacent methods is otherwise verified by the clean build/link.
16. B22: FS-failure → `safeMode()` loop (LED pattern + serial message) instead of falling through. **Done** — `main.cpp` gained `void safeMode(const String &reason)`, defined just before `setup()`. The FS-mount failure branch (`if (!ESP_FS.begin(...))`) now calls `safeMode(F("Cannot init the filesystem"))` instead of `LOG(...); return;`. `safeMode()` itself never returns: `pinMode(LED_PIN, OUTPUT)`, then forever logs `"FATAL: " + reason + " - halted in safe mode"` and blinks `LED_PIN` at 10Hz (10× `digitalWrite(LED_PIN, !digitalRead(LED_PIN)); delay(50);` per log line) - a pattern deliberately distinct from `StatusLed`'s existing `Connecting` (5Hz, 0.1s ticker) and `CaptivePortal` (1Hz, 0.5s ticker) blinks, so it reads as "something is more wrong than usual." Since `safeMode()` doesn't return, `setup()` doesn't return either, so `loop()` is never entered - this is the actual fix, not the LED/log dressing. The old early `return;` left `loop()` running against `config` (empty `LinkedList`s - harmless), but also `status`, `dmx_devices[]`, and (when `OLED_SSD1306`) `statusDisplay` - all global pointers still `nullptr` since their `new` calls happen later in `setup()`. `loop()` unconditionally calls `statusDisplay->loop()` under `OLED_SSD1306`, and `button.check()`/`handleEvent` can reach `status->set(...)` - both null-pointer dereferences. There's no useful subset of `loop()` that's safe to run with the filesystem gone (it holds WiFi credentials, DMX config, and the web UI), so "never call `loop()`" is the only correct fix - patching each null-deref individually would just be chasing the same root cause B22 names. `LED_PIN` (from `hw/board.h`, already `#include`d at `main.cpp:1`) is used directly rather than `config.hardware.ledPin`/`StatusLed`; `HardwareConfig`'s in-class default (`ledPin = LED_PIN`) would actually also resolve correctly even before `config.load()`, but the raw macro avoids any dependency on `Config`/`StatusLed` construction succeeding, keeping `safeMode()` self-contained. README's "Indicator LED -> Blinking patterns" list gained a bullet for the new pattern, with the `pio run -t uploadfs` remedy. Verified: build matrix (d1_mini_oled/esp32-devkitc-v4/sonoff_basic) clean (2026.1.25). Bench T1 passes (`BOOT_RESULT=ok`, v2026.1.25, IP `192.168.4.26`). The bench's filesystem mounts successfully on every boot, so `safeMode()` is never invoked at runtime by T1 - same build/link-only-verified category as B18/B17; T1's normal-boot pass confirms the new function and the changed `if` body compile and link cleanly without disturbing the successful-FS-mount path (the condition is false, so `safeMode()` is simply skipped, exactly as before).
17. B23: strip `hw` pins from `data/config/default.json` (board defaults then apply); keep `freq`. **Done** — removed `"ledPin": 2` and `"buttonPin": 0` from `data/config/default.json`'s `hw` object; `"freq": 600` and `"longPressDelay": 5000` (neither board-specific - no env overrides `DEFAULT_PWM_FREQ`/`DEFAULT_BUTTON_LONG_PRESS`) are unchanged. `data/` is a single shared filesystem image flashed via `uploadfs` to **all 11** environments, but `ledPin`/`buttonPin` are physical-wiring facts that vary per board via `-D LED_PIN=.../-D DEFAULT_BUTTON_PIN=...` in `platformio.ini` (e.g. `lolin32`: LED 4/button 16, `esp32-devkitc-v4`: LED 14/button 27, `esp32-s3-devkitc-1`: LED 4/button 16 - none of these are `2`/`0`). `configFromJson()` (`config.cpp`) does `hardware.ledPin = doc["hw"]["ledPin"] | LED_PIN` only *inside* `if (!doc["hw"]["ledPin"].isNull())` - the `| LED_PIN` fallback never fires while the key is present, so a hardcoded `2` in `default.json` silently overwrote every such board's correct compile-time `LED_PIN`/`DEFAULT_BUTTON_PIN` on first boot (the exact scenario the bug table names for `esp32-devkitc-v4`). With the keys absent, those `if` blocks are skipped entirely on `load()` from `default.json`, so `hardware.ledPin`/`hardware.buttonPin` simply keep the value they already had since `Config`'s construction - `HardwareConfig`'s in-class defaults `ledPin = LED_PIN` / `buttonPin = DEFAULT_BUTTON_PIN`, i.e. the correct per-board macro (B6 had already put these in-class defaults in place; B23 is what makes them actually reach a freshly-`uploadfs`'d device). `Config::cleanup()` (called from `load()`) doesn't touch `hardware.ledPin`/`buttonPin`/`longPressDelay` either, so nothing else clobbers them in between. A user can still set either field explicitly via `POST /config`'s `hw.ledPin`/`hw.buttonPin` - `update()`→`configFromJson()` honors the same `if (!isNull())` keys, and `save()`/`configToJson()` unconditionally round-trips all four `hw.*` fields into `/config/config.json`, so an explicit choice persists across reboots exactly as before; only the *unconfigured, freshly-flashed* default changes. README's `GET /config`/`POST /config` examples (lines ~159-164, ~191-196) and `tools/hil_smoke.py`'s `REQUIRED_HW` list are schema illustrations/key-presence checks independent of `default.json`'s content - `configToJson()` always emits all four `hw.*` keys regardless, so neither needed changes. Verified: `data/config/default.json` is valid JSON (`python3 -m json.tool`/`json.load` clean); no `extra_scripts`/build step reads `data/config/default.json` (grep clean), so this is a pure runtime-data change with nothing to recompile - no firmware build matrix or `Version.h`/`version` bump applies. The fix's actual effect (correct `ledPin`/`buttonPin` on a *freshly-erased* device's first boot) can't be exercised on the shared XIAO bench without `uploadfs` + erasing `/config/config.json`, which would also discard the bench's saved REPEATER config and WiFi-stored-in-NVS onboarding from earlier in this session - riskier than the build-only-verified category used for B18/B17/B22, so it's left to code-trace + JSON-validity above. The change is purely additive-by-omission (deleting two keys whose presence was the bug), with no code path that newly depends on their absence beyond the `isNull()` checks already in place.
18. B20: suspend silence-blackout while a device is manually flipped on; log timeout once, not every 5 s. **Done** — `Device` (`device.h`) gains two protected flags: `silenceLogged` (have we already logged "DMX timeout" for this silence period) and `manualOverride` (has `flip()` changed the device's state since the last real DMX frame, so the periodic blackout should stand down). `handle()`'s timeout branch now logs `"DMX timeout"` only `if (!silenceLogged)` (setting it `true` immediately after), still calls the no-arg `frame()` (which resets only `lastChange`, deliberately *not* `silenceLogged` — resetting it here would re-arm the log on the very next tick and defeat "once"), and then calls `set(channel, 0)` only `if (!manualOverride)`. The net effect: the first silence tick logs once and blacks out as before; every ~5 s thereafter it silently re-blacks-out (unchanged, harmless/idempotent cadence — not what the bug complains about) unless `manualOverride` is set, in which case the blackout is skipped entirely until DMX resumes. The two-arg `frame(univ, data, size)` — the "a real Art-Net frame arrived" signal — resets both `silenceLogged = false` and `manualOverride = false`, so normal per-period logging and blackout resume the moment a console reconnects. `DmxRelay::flip()` (`relay.h`) gains the one line `manualOverride = true;`, the only subclass touched: it's the device type the bug names ("a relay flipped ON by button turns itself OFF ≤5 s later"), `DmxRelay` doesn't override `handle()` (uses `Device::handle()` directly), and setting the flag on *either* direction of flip is safe — if the relay ends up OFF, `set(channel, 0)`'s would-be output (`inactive_value`, since `0 > threshold` is never true for a `uint8_t` threshold) matches the flipped-OFF state anyway, so skipping it changes nothing observable. `DmxServo::flip()` is `// do nothing` (a manual flip doesn't change Servo state at all, pre-existing and out of scope here), so it needs no `manualOverride` write; `DmxServo` still doesn't override `handle()`, so it gets the "log once" half of the fix for free. `DmxRepeater::handle()` is fully self-contained (its own 40 ms refresh loop, never calls `Device::handle()`) and `DmxRepeater::frame(univ,data,size)` calls the no-arg `Device::frame()` (pre-existing, unchanged) — both new flags stay permanently `false` and inert for repeaters. **Out of scope:** `Strobe::handle()` (`strobe.cpp:86-124`) has its own separate, structurally-identical silence-timeout (`lastChange != 0 && millis()-lastChange > DMX_SILENCE_TIMEOUT`, logging `"DMX Silence Timeout"` every ~5 s via `LOG`, no manual-override suspension) — it does not call `Device::handle()` and isn't part of the lines the bug table cites (`device.h:20-28`); fixing it the same way is deferred to the Phase 5 "silence-blackout is policy" redesign (line 133), consistent with B17/B18's Strobe-specific deferrals. README's "Make blackout on DMX timeout optional" TODO (line 271) stays unchecked — this fix doesn't add a config toggle (that's the Phase 5 item); it only fixes the two interactions B20 names: button-fights-blackout and per-5s log spam. Verified: build matrix (d1_mini_oled/esp32-devkitc-v4/sonoff_basic) clean (2026.1.26). Bench T1 passes (`BOOT_RESULT=ok`, v2026.1.26, IP `192.168.4.26`). The Verify line's B20-specific bench check ("button flip via the XIAO `B` button with console stopped") can't be exercised on this bench: it's configured REPEATER-only, `DmxRepeater` doesn't override `flip()` (inherits the base no-op `{}`) and isn't subject to `Device::handle()`'s silence path at all, so there's no `DmxRelay` instance on this hardware to flip — same hardware-dependent-deferral category as B18/B17/B23, documented here rather than performed.

**Verify:** build matrix (at minimum the Gotcha-#12 trio: d1_mini_oled, esp32-devkitc-v4, sonoff_basic); HIL T1 on the XIAO after each bug commit, plus targeted bench checks: QLC+ fade smoothness (B16), button flip via the XIAO `B` button with console stopped (B20), WiFi-router-off-mid-fade test (B12), POST /config under ArtNet load (B11 — the OLED-race half waits for an OLED board on the bench), 512-channel frame on the repeater with channel 512 patched (B5; RS485 on XIAO GPIO3).

### Phase 2 — Naming & taxonomy (mechanical, JSON/REST unchanged) — S
- `Strobe` → `PwmDimmer` (`devices/dimmer.*`); `DmxChannel` → `DeviceConfig`; `DmxProxy` → `DmxPort`; `enum class DmxType { Disabled, Relay, Dimmer, Servo, Repeater }`; legacy JSON strings (`"BINARY"`, `"DIMMER"`, …) preserved in the codec, with new canonical aliases (`"RELAY"`) accepted on input.
- Kill the third name everywhere (README "RELAY" heading vs `"BINARY"` type string gets an explicit alias note).
- Decide the namespace: everything project-owned under `artnode::` (or keep `art::` but apply it to all modules, not just config). Fix the includes-inside-namespace hazard (§1.2.1) while touching these files.

**Namespace decision:** keep `art::` — it's the existing, working namespace for `Config`/`DmxType`/`WiFiNet`/`HardwareConfig`; `artnode::` would mean renaming an already-correct namespace for purely cosmetic reasons. The "apply to all modules" half of the bullet (wrapping `Device`/`DmxRelay`/`PwmDimmer`/`DmxPort`/`ArtnetHandler`/`Connect`/etc. in `art::`) is a separate, larger sweep with no functional payoff on its own — deferred rather than bolted onto this "S"-sized phase; it can land incrementally as Phase 3-5 already touch those files for other reasons, or as its own quick-win pass. This phase only fixes the **includes-inside-namespace hazard** (§1.2 item 1) in `config.h`/`config.cpp`, since that's a two-line hygiene fix independent of any broader namespace sweep. `DmxPort` (was `DmxProxy`, `dmx/`) and `PwmDimmer` (was `Strobe`, `device/`) stay in the global namespace, alongside their un-renamed siblings (`Device`, `DmxRelay`, `DmxServo`, `DmxRepeater`) — consistent with their current scope, avoiding a half-namespaced `device/`+`dmx/`.

One commit per rename, in this order:
1. Fix includes-inside-namespace (§1.2 item 1): move `#include <LinkedList.h>` (`config.h:33`) and `#include "hw/board.h"` (`config.cpp:2-4`) outside `namespace art { ... }`.
2. `DmxProxy` → `DmxPort` (`src/dmx/dmx.h`, `dmx32.cpp`, `dmx8266.cpp`, `device/repeater.h`) — pure rename, global namespace.
3. `DmxChannel` → `DeviceConfig` (`config.h`, `config.cpp`, `main.cpp`) — pure rename, stays `art::DeviceConfig`; JSON key `"dmx"` and all field names unchanged.
4. `enum DmxType`: `Binary` → `Relay`, `Dimmable` → `Dimmer` (`config.h`, `main.cpp`'s device-instantiation switch). `dmxTypeToString()` keeps emitting the legacy wire strings (`"BINARY"`/`"DIMMER"`/...) per the R5 compat contract; `dmxTypeFromString()` additionally accepts `"RELAY"` as an alias for `Relay` (alongside the still-accepted `"BINARY"`). README: add an explicit RELAY/BINARY alias note next to the `POST /config` type list (making line 176's `DIMMER | RELAY | SERVO | REPEATER` comment finally accurate).
5. `Strobe` → `PwmDimmer`; `device/strobe.h`/`strobe.cpp` → `device/dimmer.h`/`dimmer.cpp` (`main.cpp`'s include + instantiation). Check off README's `- [ ] Rename Strobe class (it is meaningless)` TODO (line 272) — this is exactly what it asked for; the separate Stroboscope/Flip behavioral TODOs (lines 262-263) are unaffected and stay open.

**Verify:** build matrix (Gotcha-#12 trio) after each commit; bench T1 once at the end of the phase, plus `GET /config` byte-compatible with Phase 1 output and a Phase-1-era `config.json` still loading (old `"BINARY"`/`"DIMMER"` strings must still parse via the now-renamed enum values).

**Done (item 1):** Turned out to be two unrelated includes with very different stories:
- `config.cpp`'s `#include "hw/board.h"` inside `namespace art {}` was genuinely dead: `config.h` (included at the top of `config.cpp`, outside any namespace) already pulls in `hw/board.h` at global scope, and `board.h` is `#pragma once`-guarded, so this second include was a silent no-op. Removed outright.
- `config.h`'s `#include <LinkedList.h>` inside `namespace art {}` is **not** the hazard it looked like - it's load-bearing. Moving it to global scope (and dropping the `art::` qualifier from `art::LinkedList<DmxChannel*> dmx`/`wifi`) builds clean in isolation, but `main.cpp` also (transitively, via `ESPAsyncWebServer.h` -> `StringArray.h`) brings in `ESPAsyncWebServer-esphome`'s own global-scope `template<class T, template<class> class Item> class LinkedList` (2 template params). Two global `::LinkedList` templates with different arities can't coexist - confirmed by an actual build failure (`error: redeclared with 1 template parameter`, plus a cascade of "incomplete type" errors from `main.cpp`/`oledDisplay.h` once `config.dmx`'s type silently became the *other* `LinkedList`). Putting ivanseidel's `LinkedList<T>` inside `namespace art` is precisely what avoids this collision (`art::LinkedList` vs. global `::LinkedList`). Left in place, `art::LinkedList<DmxChannel*> dmx`/`wifi` unchanged, and added a comment at the include explaining why it must stay there so a future pass doesn't repeat this.

Build matrix (d1_mini_oled/esp32-devkitc-v4/sonoff_basic): all SUCCESS, v2026.1.26->v2026.1.27.

**Done (item 2):** `DmxProxy` -> `DmxPort`, pure rename across `src/dmx/dmx.h`, `dmx32.cpp`, `dmx8266.cpp`, `device/repeater.h` - class name, all `DmxProxy::` method-definition prefixes, the `instance()` singleton accessor and its local (`proxy` -> `port`), the `DmxRepeater::dmx` reference member, and every comment referencing the old name (including the "(B15)" singleton-pattern notes). Global namespace, unchanged. No JSON/REST surface involved.

Build matrix (d1_mini_oled/esp32-devkitc-v4/sonoff_basic): all SUCCESS.

**Done (item 3):** `DmxChannel` -> `DeviceConfig`, pure rename, `art::DeviceConfig` (stays in `art::`). Only 4 occurrences total, all in `config.h`/`config.cpp` - the typedef (`config.h:67`), the `art::LinkedList<DeviceConfig *> dmx` member (`config.h:140`), and two local-variable sites in `config.cpp` (`configFromJson`'s `new DeviceConfig()` builder loop, renamed `dmxChannel`->`deviceConfig` for consistency; and `configToJson`'s `dmx.get(i)` accessor). `main.cpp` never named the type directly (it only accesses fields through `config.dmx[i]->...`), so it needed no changes. JSON key `"dmx"` and all field names (`channel`/`type`/`threshold`/`pulse`/`multiplier`/`pin`/`level`) unchanged.

Build matrix (d1_mini_oled/esp32-devkitc-v4/sonoff_basic): all SUCCESS.

**Done (item 4):** `enum DmxType`: `Binary`->`Relay`, `Dimmable`->`Dimmer` (`config.h`); `main.cpp`'s device-instantiation switch updated (`case art::DmxType::Relay:`/`Dimmer:`). `dmxTypeToString()` (output/codec) is unchanged in behavior - still emits `"BINARY"`/`"DIMMER"`/`"SERVO"`/`"REPEATER"`/`"DISABLED"` per R5, just via the renamed enum values and a renamed-for-consistency `DMX_DIMMER` string constant (still `"DIMMER"`). `dmxTypeFromString()` (input/codec) now accepts a new `DMX_RELAY = "RELAY"` constant as an alias for `Relay`, alongside the still-accepted legacy `"BINARY"` - so a Phase-1-era config with `"type": "BINARY"` and a brand-new config with `"type": "RELAY"` both parse to the same enum value. README: added a note in the RELAY section explaining the `"BINARY"`/`"RELAY"` relationship (kills the "third name" - RELAY heading vs BINARY string - per item 2's bullet), and the existing `POST /config` type-list comment (`DIMMER | RELAY | SERVO | REPEATER`, line 176) is now accurate for input.

Build matrix (d1_mini_oled/esp32-devkitc-v4/sonoff_basic): all SUCCESS.

**Done (item 5):** `Strobe` -> `PwmDimmer`, `src/device/strobe.h`/`strobe.cpp` -> `src/device/dimmer.h`/`dimmer.cpp` (`git mv` to preserve history). Header guard `STROBE_H`->`DIMMER_H`, class declaration and all 12 method definitions (constructor, `get`/`set`/`update`/`setInterval`/`setDuration`/`setPin`/`handle`/`start`/`stop`/`flip`/`isEnabled`), the `#include`, and the two log strings naming the class (`"New Strobe: ..."`->`"New Dimmer: ..."`, `"Strobe Started"`->`"Dimmer Started"`) all renamed. `main.cpp`: `#include "device/strobe.h"`->`#include "device/dimmer.h"`, `new Strobe(...)`->`new PwmDimmer(...)` with identical constructor args. `DEFAULT_STROBE_PULSE` macro, `config.h`'s `pulse`/`multiplier` field comments ("Strobe pulse length"/"Strobe period multiplier"), and `dimmer.cpp`'s "Strobe" debug log/comment for the secondary (channel-1) flash feature are all left as-is per item 2's namespace-decision note - these name the strobe/flash *sub-feature* that `PwmDimmer` still implements, not the class. README: checked off `- [x] Rename Strobe class (it is meaningless)` (line 274); the separate Stroboscope/Flip TODOs (lines 264-265) are unaffected and stay open. No JSON/REST surface touched.

Build matrix (d1_mini_oled/esp32-devkitc-v4/sonoff_basic): all SUCCESS.

**Phase 2 verified:** bench T1 (`tools/verify.sh seeed_xiao_esp32s3 --quick`) on the HIL device - `BOOT_RESULT=ok`, v2026.1.27. `GET /config` matches hil_smoke.py's schema contract (`REQUIRED_TOP`/`REQUIRED_INFO`/`REQUIRED_DMX_FIELDS`), with the configured `dmx[0]` still reporting `"type": "REPEATER"`. R5 compat contract exercised live via POST/GET round-trips on `dmx[0].type`: `"BINARY"` -> `Relay` -> `"BINARY"` (legacy string still parses and round-trips, confirming a Phase-1-era `config.json` loads correctly through the renamed enum), `"DIMMER"` -> `Dimmer` -> `"DIMMER"`, `"RELAY"` (new alias) -> `Relay` -> `"BINARY"` (alias collapses to the canonical wire string), `"REPEATER"` -> `Repeater` -> `"REPEATER"` (unaffected, confirmed after settling the staged-update delay). Device rebooted afterward to restore a clean `_needReboot: false` state with the repeater device re-instantiated correctly.

### Phase 3 — Platform layer — M
- Create `src/platform/`; move every `#if ESP8266/ESP32` from `main.cpp`, `config.*`, `connect.*`, `oledDisplay.h`, device headers into it. Single `ESP_FS`/filesystem accessor; hostname set via one platform call (resolves the `WiFi.hostname` question on ESP32).
- `Pwm` class: LEDC on ESP32 (channel allocation + `pwmFreq` honored — fixes the silent TODO at `main.cpp:139`), `analogWrite/analogWriteFreq` on 8266. Drop `erropix/ESP32 AnalogWrite`.
- Unify LittleFS on ESP32 (`board_build.filesystem = littlefs` + code), **gated by the migration decision in Risks (R2)**.
- Servo include/`#ifdef` moves behind `platform/servo.h`.

**R2 decision (2026-06-12):** option (a) - accept. ESP32 switches straight to LittleFS with no migration shim; already-deployed ESP32 devices lose `/config/config.json` on first OTA to this version and fall back to `default.json`/captive portal (re-pair WiFi, re-POST `/config` - ~5 lines of JSON). README gets an upgrade note for ESP32 users. R2 closed.

One commit per item, in this order:
1. Filesystem + hostname platform consolidation: `src/platform/filesystem.h` - single `#if defined(ESP8266)/#else` block defining `ESP_FS` (`LittleFS`/`SPIFFS`, per-platform choice unchanged for now - item 3 flips the ESP32 side) and its includes, replacing the three duplicated copies in `config.h`, `connect.h`, `main.cpp`. A small `src/platform/wifi.h` wraps hostname assignment behind one call, replacing the raw `WiFi.hostname(config.host)` in `main.cpp:137` (resolves whether `setHostname` is needed on ESP32).
2. `Pwm` platform class (`src/platform/pwm.h`/`.cpp`): per-pin setup/write/frequency, backed by LEDC on ESP32 (fixes the silently-ignored `pwmFreq` - the commented-out `analogWriteFrequency` TODO in `main.cpp`'s `setup()`, ESP32 branch) and `analogWrite`/`analogWriteFreq`/`analogWriteRange` on ESP8266. Replaces direct `analogWrite()` calls in `hw/statusLed.h` and `device/dimmer.h`/`.cpp`, and the ESP8266-only global PWM setup in `main.cpp`. Drops `erropix/ESP32 AnalogWrite` from `platformio.ini` and the `#include <analogWrite.h>` in `main.cpp`/`statusLed.h`/`dimmer.h`.
3. ESP32 -> LittleFS (R2 = accept, above): flip `platform/filesystem.h`'s ESP32 branch to `LittleFS`, add `board_build.filesystem = littlefs` to all 6 ESP32 envs (`lolin32`, `esp32-devkitc-v4`, `lolin_s2_mini`, `esp32-s3-devkitc-1`, `seeed_xiao_esp32s3`, `seeed_xiao_esp32s3_oled`) in `platformio.ini`. README: upgrade note for ESP32 users (config reset, re-pair via portal/REST).
4. Servo platform header: `src/platform/servo.h` wraps `#ifdef ESP32 #include <ESP32Servo.h> #else #include <Servo.h> #endif`, replacing the inline block in `device/dmxServo.h`.

Scope note: this phase targets the four named consolidations above, not an exhaustive sweep of every `#ifdef ESP32`/`#if defined(ESP8266)` in the tree - e.g. `config.cpp`'s `portENTER_CRITICAL`/`portMUX_TYPE` blocks (B11, genuinely different RTOS primitives, no shared abstraction) and `oledDisplay.h`'s ESP8266-only `<ESP8266WiFi.h>` include are left as-is.

**Verify:** build matrix; dimmer PWM frequency measured on a scope/LED flicker on both platforms; FS read/write + OTA on ESP32 after the LittleFS switch.

**Done (item 1):** New `src/platform/filesystem.h` consolidates the duplicated `#if defined(ESP8266)/#else` FS+WiFi-header block - previously three near-identical copies in `config.h`, `main.cpp`, and a WiFi-header-only subset in `connect.h` - into a single `ESP_FS` definition (`LittleFS`/ESP8266, `SPIFFS`/ESP32, per-platform choice unchanged for now - item 3 flips the ESP32 side). The planned `src/platform/wifi.h` hostname wrapper was **not** created: investigated `WiFi.hostname(config.host)` (`main.cpp`, in `setup()`) against both frameworks' sources and found it's already portable as-is - `WiFiGenericClass::hostname` (arduino-esp32, aliased to `setHostname`) and `LwipIntf::hostname` (arduino-esp8266) both implement the identical `bool hostname(const String&)` signature. No behavioral difference to wrap, so a comment documenting this (and why no `#ifdef` is needed) was added at the call site instead of an abstraction with nothing to abstract.

Build matrix (d1_mini_oled/esp32-devkitc-v4/sonoff_basic): all SUCCESS, v2026.1.27->v2026.1.28.

**Done (item 2):** New `src/platform/pwm.h`/`pwm.cpp` - a `Pwm` class with two static methods, `init(freq)` and `write(pin, value)`. ESP8266: a thin wrapper - `init()` calls `analogWriteFreq`/`analogWriteRange(255)` (relocated unchanged from `main.cpp`'s global setup), `write()` calls `analogWrite()`. ESP32: LEDC-based, replacing `erropix/ESP32 AnalogWrite` - `write()` lazily allocates one LEDC channel per pin on first use (`ledcSetup`/`ledcAttachPin` at 8-bit resolution and the frequency from `init()`, then `ledcWrite`), capped at 8 channels (the limit on ESP32-S2/S3, safe across all ESP32 variants for this project's pin counts). This **fixes the silently-ignored `pwmFreq` on ESP32**: the old code's ESP32 branch was a commented-out `analogWriteFrequency` TODO that never ran, so `erropix/ESP32 AnalogWrite` always used its own default frequency regardless of `config.hardware.pwmFreq`; now `Pwm::init(config.hardware.pwmFreq)` (called from `main.cpp::setup()`, same "Devices" spot as the old block) sets the actual LEDC frequency used by every dimmer/status-LED pin. Call sites: `hw/statusLed.h::setBrightness` and both `analogWrite` calls in `device/dimmer.cpp::update()` now call `Pwm::write()`. Dropped `erropix/ESP32 AnalogWrite@^0.2` from `platformio.ini`'s `common_esp32.lib_deps`, and the `#ifdef ESP32 #include <analogWrite.h> #endif` blocks in `main.cpp`, `hw/statusLed.h`, and `device/dimmer.h`.

Build matrix (d1_mini_oled/esp32-devkitc-v4/sonoff_basic): all SUCCESS.

**Done (item 3):** `src/platform/filesystem.h`'s ESP32 branch flipped from `<SPIFFS.h>`/`SPIFFS` to `<LittleFS.h>`/`LittleFS` (`ESP_FS` now resolves to `LittleFS` on both platforms - same macro, same `FS`/`File` interface, so `config.cpp`/`api.h`/`main.cpp`'s `ESP_FS.exists/open/remove/begin/serveStatic` calls are unchanged). `board_build.filesystem = littlefs` added to `platformio.ini` for all 6 ESP32 envs (`lolin32`, `esp32-devkitc-v4`, `lolin_s2_mini`, `esp32-s3-devkitc-1`, `seeed_xiao_esp32s3`, `seeed_xiao_esp32s3_oled`) so the partition table's data partition is formatted/mounted as LittleFS instead of SPIFFS. Also fixed the previously-flagged `heltec_wifi_kit_8` gap (ESP8266, same `board_build.filesystem = littlefs` line) - the code has always used LittleFS unconditionally on ESP8266 via `platform/filesystem.h`, but this env was missing the setting; same correctness class as the ESP32 audit, one line, fixed alongside it. README: new "Upgrading" section (TOC + between OTA and TODO) documents the R2-accepted config loss for already-deployed ESP32 devices on first update to v2026.1.28+ - corrected post-HIL-verify (see "Phase 3 verified" below): WiFi reconnects automatically via the ESP32's own NVS-stored credentials (no captive portal), what's actually lost is `config.json`'s `host`/`universe`/`hw.*`/`dmx[]`, and recovery is reflash firmware+filesystem then re-POST `/config` + reboot.

Build matrix: full `pio run` (12 envs, cross-cutting `platformio.ini` change touching 7 envs) - 11/12 SUCCESS, `lolin_s2_mini` FAILED (pre-existing B26, unrelated `ESPAsyncWebServer`/`AsyncTCP` `const`-qualifier error, not a regression). `lolin32` also failed on the first pass (4s, SCons `.sconsign`/`.d` "No such file or directory" - a transient directory-creation race right after `Pwm`'s `lib_deps` change triggered a from-scratch rebuild of this env, `.pio/build/lolin32/` didn't exist afterward); a solo `pio run -e lolin32` retry succeeded cleanly (15s), confirming it wasn't a code issue.

**Done (item 4):** `src/platform/servo.h` wraps `#ifdef ESP32 #include <ESP32Servo.h> #else #include <Servo.h> #endif`, replacing the inline block in `device/dmxServo.h` (lines 4-8). Pure include consolidation, same pattern as item 1's `filesystem.h` - no behavioral change. Covered by the same full build matrix as item 3 (every non-`SONOFF_BASIC` env compiles `dmxServo.h`): 11/12 SUCCESS (`lolin_s2_mini` pre-existing B26, as above).

**Phase 3 verified:** build matrix covered per-item above (11/12 SUCCESS, `lolin_s2_mini` pre-existing B26). Bench T1 on `seeed_xiao_esp32s3` - the only board exercising the SPIFFS->LittleFS transition on real hardware: flashed v2026.1.28 (`-t upload`) onto a device previously running v2026.1.27/SPIFFS with `host=Art32-3BD8`, `dmx[0]={REPEATER}`, `hw.freq=600`. Boot log confirms `ESP_FS.begin(true)` auto-formats the now-unreadable SPIFFS partition to empty LittleFS and mounts successfully (no `safeMode()`/FATAL halt); `/config/config.json` and `/config/default.json` both log `does not exist`. **WiFi reconnected automatically** - `*WM: Try to connect with saved credentials` -> same IP `192.168.4.26`, no captive portal - confirming WiFi credentials live in the ESP32's own NVS, independent of the LittleFS data partition (also visible pre-flash: the bench's `config.json` had `"wifi": []` while connected to "MOPE"). Post-flash `GET /config` confirms the actual loss scope: `dmx=[]`, `host` reverted to default `Art-a3da3bd8`, `hw.ledPin`/`buttonPin` reverted to board defaults (21/0, matching `platformio.ini`'s `-DLED_PIN=21` for this env - the bench's prior config had these set to 1/2); `hw.freq`/`universe` happened to already equal their defaults (600/0). `-t uploadfs` restored `/www/*` (`GET /` -> 200) and `/config/default.json`. Restored the bench's REPEATER via `POST /config` (`host=Art32-3BD8`, `dmx=[REPEATER]`, `hw.freq=600`) + `POST /reboot`, then ran `hil_smoke.py` end-to-end - all PASS, including `GET /config schema` reporting `dmx=['REPEATER']` *after* a hard reset (LittleFS write+read+persistence-across-reboot confirmed) and Art-Net E2E (512ch frame to the REPEATER, core-1 DMX task alive, no crash); `SMOKE_RESULT=pass`. Found and fixed a pre-existing, Phase-3-unrelated bug in `hil_smoke.py`'s round-trip check (separate commit) surfaced by this run: it asserted `POST /config`'s response echoes `{"universe": ...}`, but per the documented async contract the response is `{"status": "pending"}` with the new value confirmed via a follow-up `GET`. README's "Upgrading" section corrected to match these findings (WiFi unaffected/no captive portal; `config.json`'s `host`/`universe`/`hw.*`/`dmx[]` reset to firmware defaults; recommends reflashing both firmware and filesystem images). PWM-frequency change (item 2, LEDC on ESP32) was not verified on a scope/with an LED - no such fixture in this harness; build-level correctness is the only verification available for that item.

### Phase 4 — Board layer — S/M
- `boards/<env>.h` per environment carrying *all* pins and `FEATURE_*` flags; `platformio.ini` passes exactly one `-D BOARD_<NAME>`; `boards/board.h` dispatches. `lolin_s2_mini` gets an explicit header (kills AGENTS Gotcha #3).
- `features.h`: `SONOFF_BASIC` becomes `BOARD_SONOFF_*` profiles that set `FEATURE_*=0`; all `#ifndef SONOFF_BASIC` in code becomes `#if FEATURE_DMX_PORT`, `#if FEATURE_SERVO`, etc. `lib_ignore` stays per-env.
- `Wire/SPI` includes move under `FEATURE_OLED`.

One commit per item, in this order:
1. Board dispatcher + per-board headers: new `src/boards/` directory, one header per `platformio.ini` environment - `d1_mini_oled.h`, `d1_mini.h`, `nodemcuv2.h`, `heltec_wifi_kit_8.h`, `sonoff_basic.h`, `sonoff_s31.h`, `lolin32.h`, `esp32-devkitc-v4.h`, `lolin_s2_mini.h`, `esp32-s3-devkitc-1.h`, `seeed_xiao_esp32s3.h`, `seeed_xiao_esp32s3_oled.h` (12, matching `platformio.ini`'s 12 `[env:...]` sections - AGENTS.md's "11 environments" count, stale even before this phase, is corrected here too). Each header explicitly defines that board's `LED_PIN`, `DEFAULT_BUTTON_PIN`, and - for the 6 ESP32 envs - `DMX_TX_PIN`/`DMX_RX_PIN`/`DMX_ENABLE_PIN`/`DMX_PORT` (today's `board.h` 17/16/21/1 fallback values, made explicit everywhere); the 3 OLED boards (`d1_mini_oled`, `heltec_wifi_kit_8`, `seeed_xiao_esp32s3_oled`) also define `SCREEN_WIDTH`/`SCREEN_HEIGHT`/`OLED_RESET` (heltec keeps 128x32/`OLED_RESET=16`/`LED_BUILTIN=2`, the other two get 128x64/`OLED_RESET=-1`); `esp32-devkitc-v4.h` keeps its `LED_BUILTIN=2` override. **`lolin_s2_mini.h` is the one that matters most**: it writes down DMX pins 17/16/21/1 explicitly, closing AGENTS Gotcha #3 (today it silently inherits these from `board.h`'s `#ifndef` defaults with no indication anyone chose them for this board). `src/boards/board.h` is the new dispatcher: `#if defined(BOARD_X) #include "boards/x.h" #elif ... #else #error`, then the handful of genuinely board-independent fallbacks that remain (`DEFAULT_BUTTON_LONG_PRESS=5`, `DEFAULT_PWM_FREQ=600`, generic 128x64/`OLED_RESET=-1`/`SCREEN_ADDRESS=0x3C` for OLED boards that didn't override, `CHIP_ARC`/`ESP_getChipId()`/`CHIP_ID`), plus the existing `<Arduino.h>`/`"Version.h"` includes carried over unchanged. `src/hw/board.h` is deleted; its 4 include sites (`main.cpp:1`, `config.h:8`, `device/dimmer.h:6`, `dmx/dmx32.cpp:4`) become `"boards/board.h"` (relative path adjusted for `dmx32.cpp`). `platformio.ini`: every env's `build_flags` gains exactly one `-D BOARD_<NAME>` (e.g. `BOARD_ESP32_DEVKITC_V4`, `BOARD_SONOFF_S31`) and loses its `-DDMX_TX_PIN=...`/`-DDMX_RX_PIN=...`/`-DDMX_ENABLE_PIN=...`/`-DLED_PIN=...`/`-DDEFAULT_BUTTON_PIN=...`/`-DLED_BUILTIN=...`/`-DSCREEN_ADDRESS=...`/`-DARDUINO_ESP8266_HELTEC_WIFI_KIT_8=1` flags (now in the matching board header; `ARDUINO_ESP8266_HELTEC_WIFI_KIT_8` is fully retired - its only consumer was `hw/board.h`, now deleted). `-DSONOFF_BASIC`/`-DOLED_SSD1306=1` are *not* touched yet (items 2-3 still consume them) - this item is purely additive/renaming, build matrix behaviorally identical before/after. AGENTS.md Gotcha #3 rewritten to describe the new dispatch (the `board.h`<->`platformio.ini` coupling no longer exists - each board is one self-contained header) and the environment table's "11" corrected to 12.
2. `src/boards/features.h`: a small, dependency-free header (no includes) mapping item 1's per-env `BOARD_<NAME>` macros to four `FEATURE_*` flags: `FEATURE_DMX_PORT`/`FEATURE_SERVO`/`FEATURE_DIMMER` = `0` if `BOARD_SONOFF_BASIC` or `BOARD_SONOFF_S31` is defined, else `1`; `FEATURE_OLED` = `1` if `BOARD_D1_MINI_OLED`/`BOARD_HELTEC_WIFI_KIT_8`/`BOARD_SEEED_XIAO_ESP32S3_OLED`, else `0`. Purely additive (new file, nothing includes it yet) - no behavior change, build matrix identical.
3. Convert the `SONOFF_BASIC`/`OLED_SSD1306` checks enumerated in this session's grep to `FEATURE_*`, adding `#include "boards/features.h"` where missing:
   - `src/dmx/dmx.h` and `src/dmx/dmx8266.cpp`: `#ifndef SONOFF_BASIC` -> `#if FEATURE_DMX_PORT` (`dmx8266.cpp` keeps its outer `#ifdef ESP8266`).
   - `src/device/repeater.h`: `#ifndef SONOFF_BASIC` -> `#if FEATURE_DMX_PORT`.
   - `src/device/dmxServo.h`: `#ifndef SONOFF_BASIC` -> `#if FEATURE_SERVO`.
   - `src/device/dimmer.h`/`dimmer.cpp`: **new** self-guard `#if FEATURE_DIMMER ... #endif` around the whole file (previously unguarded, relying solely on `main.cpp`'s conditional `#include`) - brings `PwmDimmer` in line with the other three device headers' self-guard pattern.
   - `src/main.cpp`: the combined `#ifndef SONOFF_BASIC` block at lines 27-31 (3 includes) splits into three `#if FEATURE_SERVO` / `#if FEATURE_DIMMER` / `#if FEATURE_DMX_PORT` blocks (one `#include` each); the `switch` at lines 166-176 splits the same way, one `#if`/`#endif` pair per `case`. The four `#ifdef OLED_SSD1306` sites (lines 46, 61, 145, 227) -> `#if FEATURE_OLED`.
   - `src/hw/oledDisplay.h`: `#ifdef OLED_SSD1306` (line 3, wraps the whole file) -> `#if FEATURE_OLED`.
   - `platformio.ini`: drop `-DSONOFF_BASIC` (sonoff_basic, sonoff_s31) and `-DOLED_SSD1306=1` (d1_mini_oled, heltec_wifi_kit_8, seeed_xiao_esp32s3_oled) - fully superseded by `BOARD_<NAME>` -> `features.h`.
   - This is the behavioral cutover: after this item, `SONOFF_BASIC` and `OLED_SSD1306` have zero remaining references in `src/`. AGENTS.md Gotcha #4 rewritten around `FEATURE_DMX_PORT`/`FEATURE_SERVO`/`FEATURE_DIMMER` (same exclusion set, new names); its "wrap new device types in `#ifndef SONOFF_BASIC`" advice becomes "...in `#if FEATURE_*`".
4. Wire/SPI under `FEATURE_OLED`: `src/boards/board.h`'s `#include <Wire.h> #include <SPI.h>` (carried from old `hw/board.h:11-12` by item 1, still gated on the by-then-dead `SONOFF_BASIC` until this item) becomes `#if FEATURE_OLED`. `sonoff_basic`/`sonoff_s31`'s `lib_ignore: Wire, SPI, Servo` in `platformio.ini` is unchanged (plan says `lib_ignore` stays per-env) - belt-and-suspenders keeping these libraries out of the dependency graph even though `FEATURE_OLED=0`/`FEATURE_SERVO=0` already mean the referencing headers aren't compiled.

Scope note: this phase doesn't touch `MAX_DMX_DEVICES` (config.h, `#ifdef ESP32` - AGENTS Gotcha #6, genuinely platform- not board-scoped) or any `config.hardware` runtime-configurable field - those are Phase 5/6 territory.

**Verify:** build matrix **including both sonoff envs**; `pio run -e sonoff_basic` flash-size unchanged or smaller; relay smoke test on a Sonoff. Note: the only HIL bench available is `seeed_xiao_esp32s3`/`_oled` (ESP32-S3, `FEATURE_DMX_PORT=1`, repeater-configured) - "relay smoke test on a Sonoff" and the sonoff flash-size comparison are build-level-only on this hardware, same documented-limitation treatment as Phase 3's PWM-frequency/scope check.

**Done (item 1):** New `src/boards/` directory: `board.h` (dispatcher) plus one header per `platformio.ini` environment - `d1_mini_oled.h`, `d1_mini.h`, `nodemcuv2.h`, `heltec_wifi_kit_8.h`, `sonoff_basic.h`, `sonoff_s31.h`, `lolin32.h`, `esp32-devkitc-v4.h`, `lolin_s2_mini.h`, `esp32-s3-devkitc-1.h`, `seeed_xiao_esp32s3.h`, `seeed_xiao_esp32s3_oled.h`. Each defines `LED_PIN`/`DEFAULT_BUTTON_PIN` and, for the 6 ESP32 envs, `DMX_TX_PIN`/`DMX_RX_PIN`/`DMX_ENABLE_PIN`/`DMX_PORT`; the 3 OLED boards (`d1_mini_oled`, `heltec_wifi_kit_8`, `seeed_xiao_esp32s3_oled`) also define `SCREEN_WIDTH`/`SCREEN_HEIGHT`/`SCREEN_ADDRESS`/`OLED_RESET` (heltec: 128x32/`OLED_RESET=16`; the other two: 128x64/`OLED_RESET=-1`). **`lolin_s2_mini.h` writes down DMX pins 17/16/21/1 explicitly**, with a comment noting these were previously silent `hw/board.h` `#ifndef` fallbacks - closing AGENTS Gotcha #3. `boards/board.h` dispatches via `#if defined(BOARD_X) #include "boards/x.h" #elif ... #else #error`, then keeps the board-independent leftovers (`DEFAULT_BUTTON_LONG_PRESS=5`, `DEFAULT_PWM_FREQ=600`, `CHIP_ARC`/`ESP_getChipId()`/`CHIP_ID`, `<Arduino.h>`/`"Version.h"`, and - unchanged for now, item 4's target - the `#ifndef SONOFF_BASIC` `<Wire.h>`/`<SPI.h>` block). `src/hw/board.h` deleted; its 4 include sites (`main.cpp:1`, `config.h:8`, `device/dimmer.h:6`, `dmx/dmx32.cpp:4` as `"../boards/board.h"`) now point to `boards/board.h`. `platformio.ini`: every env's `build_flags` gained exactly one `-D BOARD_<NAME>` and lost its per-pin/`LED_BUILTIN`/`SCREEN_ADDRESS`/`ARDUINO_ESP8266_HELTEC_WIFI_KIT_8=1` flags (now in the matching header; the HELTEC macro is fully retired - its only consumer was `hw/board.h`). `-DSONOFF_BASIC`/`-DOLED_SSD1306=1` retained per plan (items 2-3 still consume them).

**Deviation from plan - `ELEGANTOTA_USE_ASYNC_WEBSERVER`:** this flag was initially moved into the 10 affected board headers alongside the others, which broke the link (`undefined reference to ElegantOTAClass::begin(AsyncWebServer*, const char*, const char*)`). ElegantOTA's own `.cpp` is a separate translation unit that only sees `platformio.ini`'s global `build_flags`, not app-header `#define`s - the library compiled without the `AsyncWebServer` overload while `main.cpp` (seeing the macro via `boards/board.h`) called it. Fixed by reverting `-DELEGANTOTA_USE_ASYNC_WEBSERVER=1` to a `platformio.ini` build flag on those 10 envs and removing the header `#define`s. General rule, now in AGENTS Gotcha #3: any flag a third-party library's own source reads must stay a global `-D`, not move into a board header; `DISABLE_OTA` (sonoff_basic/sonoff_s31, header-only) stays safe because `main.cpp` includes `boards/board.h` before `<ElegantOTA.h>` and ElegantOTA isn't in those envs' `lib_deps` at all.

AGENTS.md updated: Gotcha #3 rewritten for the new dispatcher (incl. the `ELEGANTOTA_USE_ASYNC_WEBSERVER` caveat above); "11 environments" -> "12" in the build-quick-reference and Build Environments intro; Repository Layout's `hw/board.h` entry replaced with a `boards/` entry; the environment table gains a "Board Header" column (pointing at `src/boards/<name>.h` for per-board pins/LED/button/OLED geometry) and its "Special Flags" column is trimmed to the few remaining real `-D` flags (`OLED_SSD1306=1`/`SONOFF_BASIC`), removing the dangling `board.h` reference (lolin_s2_mini) and the retired `ARDUINO_ESP8266_HELTEC_WIFI_KIT_8` reference (heltec); the Conditional Compilation Flags table drops the retired `ARDUINO_ESP8266_HELTEC_WIFI_KIT_8` row and gains a `BOARD_<NAME>` row.

Build matrix: full `pio run` (12 envs) - 11/12 SUCCESS, `lolin_s2_mini` FAILED (pre-existing B26, unrelated `ESPAsyncWebServer` `const`-qualifier error). `lolin32` failed on the first pass (4.3s, same transient SCons `.d`-file race documented in Phase 3 item 3's "Done" note, triggered by the from-scratch `.pio/build/lolin32/` rebuild after the global header rename); a solo `pio run -e lolin32` retry succeeded (14.6s), confirming it wasn't a code issue. Behaviorally identical before/after, as planned.

**Done (item 2):** New `src/boards/features.h` - dependency-free (no `#include`s), maps each `BOARD_<NAME>` macro to four `FEATURE_*` flags: `FEATURE_DMX_PORT`/`FEATURE_SERVO`/`FEATURE_DIMMER` = `0` for `BOARD_SONOFF_BASIC`/`BOARD_SONOFF_S31`, else `1`; `FEATURE_OLED` = `1` for `BOARD_D1_MINI_OLED`/`BOARD_HELTEC_WIFI_KIT_8`/`BOARD_SEEED_XIAO_ESP32S3_OLED`, else `0`. Purely additive - nothing `#include`s it yet, so it isn't part of any translation unit and the build matrix is unaffected (item 3 wires it in everywhere). Verified the `#if`/`#elif` logic directly via `gcc -E -dM -D<BOARD_NAME>` for both Sonoffs, all three OLED boards, and two non-OLED ESP32 boards - each produces the expected `FEATURE_*` combination.

**Done (item 3):** Converted every `SONOFF_BASIC`/`OLED_SSD1306` site enumerated in this item to `#if FEATURE_*`, adding `#include "boards/features.h"` (relative path matching each file's existing include style) ahead of the guard:
- `src/dmx/dmx.h`: whole-file `#ifndef SONOFF_BASIC` -> `#include "../boards/features.h"` + `#if FEATURE_DMX_PORT` ... `#endif // FEATURE_DMX_PORT`.
- `src/dmx/dmx8266.cpp`: same conversion, nested inside its pre-existing outer `#ifdef ESP8266`/`#endif` (unchanged) - `#include "../boards/features.h"` then `#if FEATURE_DMX_PORT` follow the `#ifdef ESP8266` line; the two closing `#endif`s become `#endif // FEATURE_DMX_PORT` / `#endif // ESP8266`.
- `src/device/repeater.h`: `#ifndef SONOFF_BASIC` -> `#if FEATURE_DMX_PORT`, wrapping the pre-existing `#ifndef REPEATER_H` include guard unchanged; both closing `#endif`s gain `// REPEATER_H` / `// FEATURE_DMX_PORT` comments.
- `src/device/dmxServo.h`: `#ifndef SONOFF_BASIC` -> `#if FEATURE_SERVO`, same pattern.
- `src/device/dimmer.h`/`dimmer.cpp`: gained a **new** self-guard `#if FEATURE_DIMMER ... #endif // FEATURE_DIMMER` around the entire file - previously `PwmDimmer` had no self-guard at all and relied solely on `main.cpp`'s conditional `#include`, unlike the other three device headers. Brings it in line with `repeater.h`/`dmxServo.h`'s self-guard pattern.
- `src/hw/oledDisplay.h`: whole-file `#ifdef OLED_SSD1306` -> `#include "../boards/features.h"` + `#if FEATURE_OLED` ... `#endif // FEATURE_OLED`.
- `src/main.cpp`: gained `#include "boards/features.h"` (after `boards/board.h`). The combined `#ifndef SONOFF_BASIC` block of 3 includes (old lines 27-31) split into three independent `#if FEATURE_DIMMER` / `#if FEATURE_DMX_PORT` / `#if FEATURE_SERVO` blocks, one `#include` each. All four `#ifdef OLED_SSD1306` sites (the `StatusDisplay` forward-declare+global, the button-press OLED message, `setup()`'s `StatusDisplay` construction, and `loop()`'s `statusDisplay->loop()`) became `#if FEATURE_OLED` via one `replace_all`. The device-instantiation `switch`'s combined `#ifndef SONOFF_BASIC` block (3 `case`s) split into three separate `#if FEATURE_SERVO` / `#if FEATURE_DIMMER` / `#if FEATURE_DMX_PORT` guards, one per `case`; `Relay` and `default:` stay unguarded.
- `src/artnetHandler.h`: updated the 512-channel-skip comment's stale "not includable here under SONOFF_BASIC" to "...when FEATURE_DMX_PORT=0" - comment-only, no logic change.
- `platformio.ini`: dropped `-DOLED_SSD1306=1` from `d1_mini_oled`/`heltec_wifi_kit_8`/`seeed_xiao_esp32s3_oled` and `-DSONOFF_BASIC` from `sonoff_basic`/`sonoff_s31`. Both macros now have zero remaining references anywhere in `src/` or `platformio.ini` (confirmed by grep) - the cutover is complete.

**Done (item 4):** `src/boards/board.h`'s `#ifndef SONOFF_BASIC #include <Wire.h> #include <SPI.h> #endif` (carried over unchanged from old `hw/board.h` by item 1, explicitly called out there as item 4's pending target) became `#if FEATURE_OLED #include <Wire.h> #include <SPI.h> #endif`, immediately after the new `#include "features.h"`. `sonoff_basic`/`sonoff_s31`'s `lib_ignore: Wire, SPI, Servo` is unchanged, per the plan ("`lib_ignore` stays per-env") - now genuinely belt-and-suspenders, since `FEATURE_OLED=0` (and `FEATURE_SERVO=0`) already keep `<Wire.h>`/`<SPI.h>`/`Servo.h` out of those two builds' translation units.

**Deviation from plan - items 3 and 4 landed in one commit:** the plan lists these as separate numbered items, but they aren't independently buildable. Had item 3's `platformio.ini` change (dropping `-DSONOFF_BASIC`) landed alone, `board.h`'s still-`#ifndef SONOFF_BASIC` Wire/SPI block (item 1's explicitly-deferred state) would become vacuously TRUE on *all* 12 envs - including `sonoff_basic`/`sonoff_s31`, whose `lib_ignore: Wire, SPI, Servo` (AGENTS Gotcha #4) would then conflict with the now-compiled `<Wire.h>`/`<SPI.h>` includes and fail to link on both Sonoff boards. Item 4's `#if FEATURE_OLED` fix (`FEATURE_OLED=0` for both Sonoffs) had to land in the same commit to keep every intermediate state buildable - the same "can't ship N without N+1" shape as item 1's ELEGANTOTA deviation, just caught before committing instead of after.

AGENTS.md updated: Gotcha #4 fully rewritten around `FEATURE_DMX_PORT`/`FEATURE_SERVO`/`FEATURE_DIMMER`/`FEATURE_OLED` (using current class/enum names - `device/dimmer.h`'s `PwmDimmer`, `DmxType::Dimmer`/`Servo`/`Repeater` - not the Phase-3-retired `strobe.h`/`Strobe`/`Dimmable`), including item 4's board.h Wire/SPI->`FEATURE_OLED` detail as its own bullet. "Conditional compilation flags" table's separate `SONOFF_BASIC` and `OLED_SSD1306` rows replaced with one row for the four `FEATURE_*` flags, pointing at `features.h`/Gotcha #4. The environment table's "Other `-D` Flags" column - all `OLED_SSD1306=1`/`SONOFF_BASIC`/`—` after this item - removed entirely as a dead column.

Build matrix: full `pio run` (12 envs) - 11/12 SUCCESS, including **both `sonoff_basic` and `sonoff_s31`** (the crux of the combined commit - confirms the Wire/SPI/`lib_ignore` interaction described in the deviation note above doesn't break either Sonoff build). `lolin_s2_mini` FAILED on the same pre-existing B26 (`ESPAsyncWebServer.h:1691` `const AsyncServer`/`-fpermissive` error, unrelated to this change) as items 1/2.

**Phase 4 verified:** build matrix covered per-item above (11/12 SUCCESS, `lolin_s2_mini` pre-existing B26). `pio run -e sonoff_basic`/`sonoff_s31` flash-size check (per the Verify line): built both envs at the pre-item-3/4 commit (`HEAD~1`, via a temporary `git worktree` since `.pio/build/` had already been overwritten by this commit's matrix run) and compared against this commit's sizes - **bit-identical** for both envs, 389029/892912 bytes flash (43.6%) and 34392/81920 bytes RAM (42.0%), i.e. "unchanged", not merely "unchanged or smaller". Bench T1: a `seeed_xiao_esp32s3`-family board is connected (`/dev/cu.usbmodem111701`, likely the same Phase 3 bench device, currently configured as a REPEATER) but was **not flashed**, by user choice - items 1-4 are pure `#if`-macro renames with no preprocessor-output change for any board including this one's `FEATURE_*` set, so a HIL run would exercise byte-identical firmware; same documented-limitation treatment as Phase 3's PWM-frequency/scope check (no verification beyond build-level is meaningful here). "Relay smoke test on a Sonoff" (Verify line) is likewise build-level-only - no Sonoff HIL bench exists, as the Verify line itself anticipates.

### Phase 5 — Core restructure — L

**Design goals (the five bullets this phase delivers):**
- `App` class absorbs `main.cpp` globals and boot order; `deviceFactory` replaces the switch; `std::array<std::unique_ptr<Device>, MAX_DMX_DEVICES>`; `std::vector<DeviceConfig>` values in config (drop `LinkedList`).
- **Device interface v2** (single dispatch path, §Part 2.1): `onDmx(universe, slice)`, `firstChannel()`, `channelCount()`, `tick()` (renamed `handle`), `setEnabled/flip`, blackout policy hook. Dispatcher in `artnetService` does slicing/universe routing — repeater universe support lands here (README TODO).
- Strobe/dimmer timing extracted to `core/dimmerLogic.h` as a pure state machine; `PwmDimmer` becomes a thin shell (fixes the stroboscope properly, with tests).
- Devices no longer started by the network class; `App` calls `start()` after construction, before `artnet.init()`.
- **Extraction rule (binding):** nothing moves into `core/` without native tests arriving in the same commit — pin the behavior at the new seam first, then refactor against it. This is how unit coverage grows: with the restructure, not after it.

This is the largest phase. Unlike Phases 1–4 (each item a near-mechanical change), several Phase 5 items change runtime behavior (dispatch path, strobe, blackout policy). The breakdown below is deliberately fine-grained so each commit is independently buildable, T1-verifiable, and reviewable as a small diff. **Read the "sequencing & risk notes" and "out of scope" blocks below before starting — the ordering is load-bearing** (native-test env must exist before any `core/` extraction; the dispatch cutover must be behavior-identical before any per-device behavior change; the `App` move comes last so it lands on an already-stable device interface).

**One commit per item, in this order:**

1. **Native test scaffold (`[env:native]`) — no `src/` change, unblocks the binding rule.** Add `[env:native]` to `platformio.ini`: `platform = native`, the default Unity test framework, and `lib_deps = bblanchon/ArduinoJson@^6.17.3` (ArduinoJson is portable C++ and compiles on the host — needed by later codec tests; the other libs are not). Add one trivial suite `test/test_sanity/test_sanity.cpp` (a single `TEST_ASSERT_EQUAL(2, 1+1)` under Unity's `setUp`/`tearDown`/`main`→`UNITY_BEGIN`/`RUN_TEST`/`UNITY_END` boilerplate) so `pio test -e native` has something green to run. Add a `native-test` job (or step) to `.github/workflows/ci.yml` running `pio test -e native`. **Constraint that governs every later `core/` item:** the native toolchain is the host compiler — it cannot include `<Arduino.h>`, ESP headers, FreeRTOS, or Arduino `String`/`millis()`. Anything placed in `core/` to be natively tested must be written in plain portable C++ (`std::string`/`const char*`/`std::string_view`, explicit time parameters, no hardware calls). *Verify:* `pio test -e native` green locally + in CI; `pio run` matrix unaffected (no `src/` change).

**Done (item 1):** `[env:native]` added to `platformio.ini` (`platform = native`, `lib_deps = bblanchon/ArduinoJson@^6.17.3`). `test/test_sanity/test_sanity.cpp` added — single `test_arithmetic` (`TEST_ASSERT_EQUAL(2, 1+1)`) under standard Unity `setUp`/`tearDown`/`main` boilerplate; `pio test -e native` installs `throwtheswitch/Unity@^2.6.1` automatically and passes. **Deviation/addition not in the original item text:** `env:native` also got `build_src_filter = -<*>` (its comment explains why — `src/` is Arduino-only and doesn't compile on the host toolchain; this env exists for `pio test`, not `pio run`), and a new top-level `[platformio] default_envs = <the 12 firmware envs>` section was added so plain `pio run` (AGENTS.md's "build ALL 12 environments" sanity check) continues to mean exactly that and doesn't also attempt (and fail) `pio run -e native`. New `native-test` job added to `.github/workflows/ci.yml` (mirrors the `build` job's PlatformIO install/cache steps, then `pio test -e native`). Verified: `pio test -e native` → 1/1 PASSED; trio (`d1_mini_oled`/`esp32-devkitc-v4`/`sonoff_basic`) build clean; `pio project config` confirms `default_envs` lists exactly the 12 firmware envs (native excluded).

2. **`core/dmxTypes.h` — `enum class DmxType` + string codec + DMX constants, with native test.** New Arduino-free header `src/core/dmxTypes.h`: `enum class DmxType : uint8_t { Disabled, Relay, Dimmer, Servo, Repeater }`; `const char *toWireString(DmxType)` (emits the **legacy** `"BINARY"` for `Relay`, per the R5/compat contract — *not* `"RELAY"`); `DmxType fromWireString(const char *)` (accepts both `"BINARY"` and `"RELAY"` → `Relay`, `"DIMMER"`/`"SERVO"`/`"REPEATER"`, anything else → `Disabled`); `constexpr uint16_t DMX_CHANNELS = 512; constexpr uint16_t DMX_PACKET_SIZE = 513;`. Then migrate `config.h`/`config.cpp`: delete the in-class plain `enum DmxType` and the `String`-based `dmxTypeFromString`/`dmxTypeToString` bodies, re-expressing them as thin adapters that call the `core/` functions (`.c_str()` in, `String(...)` out) so the public `Config::dmxTypeToString(DmxType)` signature the OLED/codec call sites use is preserved. **Watch:** `enum class` removes implicit `int` conversion — every site that did `LOG(config.dmx[i]->type)` or compared a raw int now needs the string mapper or an explicit cast; grep `DmxType` and `->type`/`.type` and fix each. Also fold the existing scattered `512` literals (`device.h` `getNumberOfChannels`, `artnetHandler.h` `==512`, `dmx.h` `DMX_CHANNELS`) toward `core/dmxTypes.h`'s constants where they're includable (the `dmx/` ones already have their own `DMX_CHANNELS` — leave those, just note the canonical home is now `core/`). Native test `test/test_dmxtypes/`: round-trip every enum↔string, assert `fromWireString("BINARY") == Relay`, `fromWireString("RELAY") == Relay`, `toWireString(Relay)` is `"BINARY"`, junk → `Disabled`. *(First `core/` extraction — native test lands in the same commit, binding rule.)* *Verify:* native green; trio build (`d1_mini_oled`/`esp32-devkitc-v4`/`sonoff_basic`) + T1.

**Done (item 2):** New `src/core/dmxTypes.h` (Arduino-free): `enum class DmxType : uint8_t {Disabled,Relay,Dimmer,Servo,Repeater}`, `toWireString`/`fromWireString` (Relay ↔ legacy `"BINARY"`, `"RELAY"` accepted as input alias, junk → `Disabled`), `constexpr DMX_CHANNELS=512`/`DMX_PACKET_SIZE=513`. `config.h`: replaced the in-class plain `enum DmxType` with `using DmxType = core::DmxType;` (preserves every `art::DmxType::*` call site unchanged); deleted the six `DMX_*` `String` constants and rewrote `dmxTypeFromString`/`dmxTypeToString` as one-line adapters over `core::fromWireString`/`toWireString`. `main.cpp`: the one `LOG(config.dmx[i]->type)` site (the `default:` case's diagnostic) became `LOG(static_cast<int>(config.dmx[i]->type))` — `enum class` dropped the implicit int conversion `Serial.println` relied on; the `switch`/`case art::DmxType::*` labels needed no change. `artnetHandler.h`: the B3-era `getNumberOfChannels() == 512` literal became `== core::DMX_CHANNELS` (now `#include`s `core/dmxTypes.h`, which is genuinely Arduino-free so this is includable even under `FEATURE_DMX_PORT=0`/SONOFF — the comment explaining why `512` *couldn't* be a shared constant is now resolved, not just reworded). `dmx/dmx.h`'s own `DMX_CHANNELS`/`DMX_BUFFER_SIZE` macros (`dmx32.cpp`/`dmx8266.cpp`) were left as-is per the item text. New `test/test_dmxtypes/test_dmxtypes.cpp` (6 cases: legacy-BINARY round trip, RELAY-alias acceptance, full enum↔string round trip, unknown→Disabled). Verified: `pio test -e native` → 7/7 PASSED (test_dmxtypes + test_sanity); full 12-env `pio run` → 11/12 SUCCESS (pre-existing `lolin_s2_mini` B26, unrelated).

3. **config: `std::vector<DeviceConfig>` / `std::vector<WiFiNet>` by value (drop ivanseidel `LinkedList`).** `config.h`: `art::LinkedList<DeviceConfig *> dmx` → `std::vector<DeviceConfig> dmx`; `art::LinkedList<WiFiNet *> wifi` → `std::vector<WiFiNet> wifi`; add `#include <vector>`; **delete the inside-`namespace art` `#include <LinkedList.h>`** (this is exactly the §1.2.1 hazard — the comment there explains it was only inside the namespace to dodge the ESPAsyncWebServer-esphome `LinkedList` collision; with the include gone the hazard is gone). `config.cpp`: `.get(i)` → `[i]`, `.add(x)` → `.push_back(x)`, the `cleanupDmx()`/`cleanupWiFi()` `delete`-loops → `.clear()` (values, no manual delete), `new DeviceConfig()`/`new WiFiNet()` → local value `push_back`ed. **Every consumer of the pointer API must change `->` to `.`:** `main.cpp` (`config.dmx[i]->type/channel/pin/level/pulse/multiplier/threshold`, the `deviceCount` loops), `hw/oledDisplay.h` (`config->dmx[i]->channel/type/pin`, `config->dmx.size()`). Grep `dmx[` , `dmx.get`, `wifi.get`, `dmx.add`, `wifi.add`, `->type`, `->channel`, `->pin`, `->level`, `->pulse`, `->multiplier`, `->threshold`, `->ssid`, `->pass`, `->order` and fix every hit. Remove `ivanseidel/LinkedList` from `[common].lib_deps` **only after** grepping that nothing else references it (the ESPAsyncWebServer-esphome fork has its *own* internal `LinkedList` — unaffected). **External JSON is byte-identical** (codec unchanged) so `hil_smoke.py`'s `REQUIRED_*` schema checks still pass. *Verify:* this is cross-cutting → **all 12 envs** + T1. (Internal-only, no behavior change; removes a dependency.)

**Done (item 3):** `config.h`: `dmx`/`wifi` are now `std::vector<DeviceConfig>`/`std::vector<WiFiNet>` by value; added `#include <vector>` and deleted the inside-`namespace art` `#include <LinkedList.h>` along with its §1.2.1 hazard comment (the B11 comment's "those LinkedLists" became "those vectors"). `config.cpp`: `cleanupWiFi()`/`cleanupDmx()` are now just `wifi.clear()`/`dmx.clear()` (no manual `delete` loop); `configFromJson()` builds a local `WiFiNet`/`DeviceConfig` value and `push_back`s it; `configToJson()`'s serialization loops use `wifi[i]`/`dmx[i]` (by `const &`) with `size_t` loop counters. `main.cpp`: all `config.dmx[i]->...` (device-instantiation switch, `LOG`, `longName` loop) → `config.dmx[i]....`. `hw/oledDisplay.h`: all `config->dmx[i]->channel/type/pin` (both `setStatus32()` and `setStatus64()`) → `config->dmx[i].channel/type/pin`. Grepped `src/` for `LinkedList`/`.get(`/`.add(`/`->` on `dmx`/`wifi`/`net`/`channel` — zero remaining hits, so `ivanseidel/LinkedList@0.0.0-alpha+sha.dac3874d28` was removed from `[common].lib_deps` in `platformio.ini` (the ESPAsyncWebServer-esphome fork's own internal `LinkedList` is a separate global-scope template, unaffected). External JSON unchanged (codec untouched). Verified: native `pio test -e native` → 7/7 PASSED; full 12-env `pio run` → 11/12 SUCCESS (pre-existing `lolin_s2_mini` B26, unrelated `ESPAsyncWebServer` `-fpermissive` error, same as items 1/2/Phase 4). `git status --porcelain` confirms no `Version.h`/`version` churn.

4. **`core/dimmerLogic.h` — pure strobe/dimmer timing state machine + native characterization tests; `PwmDimmer` → thin shell.** **Write the test first** (`test/test_dimmerlogic/`), pinning the *current* observable behavior of `dimmer.cpp` before touching it: steady dimming (DMX value N, no strobe → constant adjusted PWM), strobe cycle (set interval via the speed input → output alternates active/inactive on the pulse/period schedule), and flip-to-full-when-`value==0` (B18). The logic is driven by an explicit `tick(uint32_t nowMs)` plus setters — **no `millis()`, no `Pwm::write`, no `Arduino.h`, no `LOG`**. Inputs: dmx level, strobe speed, active/inactive level, pulse, multiplier, flip state; outputs: current pin duty (0–255) + a "changed" flag. Then extract `dimmer.cpp`'s period/pulse/interval/`state` machine (lines ~90–128) and the value-adjust math (`set` ~38–42, `update` ~52–63, `flip` ~148–163) into `DimmerLogic`. `PwmDimmer` keeps the pin, the `Pwm::write(pin, logic.duty())` call, the `LOG`/`LOG_DEBUG` lines, and the silence-timeout shell; it forwards timing to `DimmerLogic`. **Keep `channelCount()` at 1 here** — the 2-channel strobe dispatch is item 8; this item is a behavior-preserving extraction only. *(Second `core/` extraction — characterization tests are the seam, per the binding rule's "pin the behavior first.")* *Verify:* native green; trio + T1 (the XIAO bench has no DIMMER device, so live strobe is build-level + native-test-covered only — same documented limitation as Phase-1 B16; a live check needs a DIMMER-configured board).

**Done (item 4):** New Arduino-free `src/core/dimmerLogic.h` - `core::DimmerLogic`, constructed from `activeState` (1/0, matching HIGH/LOW), holding `pulse`/`period`/`interval`/`state`/`value`/`adjustedActiveValue`/`adjustedInactiveValue`/`adjustedMaxValue`/`valueOverride`/`previousMillis`. Public API: `setValue(uint8_t)` (the old `set()` value-adjust math), `setDuration(int)`/`setInterval(int)` (pulse/period setters, same negative-clamp rules), `flip()` (toggle `enabled`, B18 full-brightness-when-`value==0` preserved), `start()` (re-arm: `interval=0; state=activeState; enabled=true`), `setEnabled`/`isEnabled`, `tick(unsigned long nowMs)` (the old `handle()` period/pulse/interval/state machine, returns true if `duty()` may have changed), and `duty()`/`getValue()`. New `test/test_dimmerlogic/test_dimmerlogic.cpp` (5 cases, written *before* the extraction per the binding rule): steady dimming (HIGH and LOW `activeState`), a full strobe cycle (`setDuration(5)`/`setInterval(50)` → tick at 0/44/45/49/50ms alternates duty 200/0/0/200/200/0 as expected), and both B18 flip cases (value==0 → full; value>0 → restores DMX value). `device/dimmer.h`/`dimmer.cpp`: `PwmDimmer` is now a thin shell - dropped `period`/`enabled`/`isFlipped` (dead field)/`state`/`value`/`adjustedActiveValue`/`adjustedInactiveValue`/`adjustedMaxValue`/`valueOverride`/`previousMillis`/`interval` in favor of a `core::DimmerLogic logic` member; kept `pin`/`pulse` (constructor-configured strobe duration, reapplied via `logic.setDuration(pulse)` on each Strobe-channel update, matching the original `setDuration(pulse)` call in `set()`)/`multiplier`/`activeState`/`inactiveState` (still needed for `pinMode`/`digitalWrite`). `channelCount()` unchanged at 1. Verified: `pio test -e native` → 12/12 PASSED (3 suites); full 12-env `pio run` → 11/12 SUCCESS (pre-existing `lolin_s2_mini` B26, unrelated). `git status --porcelain` confirms no `Version.h`/`version` churn. T1 (XIAO bench) skipped per the item's own note - the bench has no DIMMER device configured, so this is build/native-test-verified only, same as Phase-1 B16.

5. **Device interface v2 — additive methods + `handle()`→`tick()` rename, base bridges, NO dispatch change.** `device.h`: add `virtual void onDmx(uint32_t universe, const uint8_t *data, uint16_t len)`, `virtual uint16_t firstChannel() { return channel; }`, `virtual uint16_t channelCount() = 0` (the renamed `getNumberOfChannels` — keep the old name as an inline alias for one item if grep shows many callers, else rename outright), a blackout-policy hook (`bool blackoutOnSilence = true; void setBlackout(bool b)`), and rename `handle()` → `tick()`. Give the base a **default `onDmx` that reproduces today's dispatch exactly** — reset the silence timer (old `frame()` body) then run the old per-channel `set()/get()` change-detect loop over `[firstChannel() .. firstChannel()+channelCount()-1]` with the `i < 1` guard. Rename `handle`→`tick` at the base and at both overrides (`repeater.h`, `dimmer.cpp`) and the `main.cpp` loop call. **Every device still behaves identically** — this item only adds the v2 surface and the bridge; nothing calls `onDmx` yet. *Verify:* matrix (trio) + T1.

**Done (item 5):** `device.h`: renamed `getNumberOfChannels()` → `channelCount()` outright (only ~8 call sites total across `relay.h`/`dmxServo.h`/`repeater.h`/`dimmer.h`/`artnetHandler.h`/a `dmx.h` comment - no alias needed); renamed `handle()` → `tick()` (base + both overrides, `repeater.h`'s `DmxRepeater::handle`→`tick` and `dimmer.cpp`'s `PwmDimmer::handle`→`tick`, plus the `main.cpp` loop's `dmx_devices[i]->handle()`→`->tick()`). Added `virtual uint16_t firstChannel() { return channel; }`, `virtual void setBlackout(bool enabled)` + a new `bool blackoutOnSilence = true` member (wired into `tick()`'s existing `set(channel,0)` guard alongside `manualOverride` - both default to "blackout enabled", so behavior is unchanged until item 9 gives `setBlackout` a caller), and `virtual void onDmx(uint32_t univ, const uint8_t *data, uint16_t size)` whose default body is exactly `ArtnetHandler`'s current dispatch (the old `frame(univ,data,size)` body, then the `[firstChannel()..firstChannel()+channelCount()-1]` change-detect loop with the `i < 1` guard) - **not called from anywhere yet** (item 6 wires up the dispatcher). `artnetHandler.h`'s two `getNumberOfChannels()` call sites became `channelCount()`; no dispatch logic changed. Verified: native 12/12 PASSED (unaffected, pure-C++ tests don't touch `device.h`); full 12-env `pio run` → 11/12 SUCCESS (pre-existing `lolin_s2_mini` B26). `git status --porcelain` confirms no `Version.h`/`version` churn. T1 not run this item (purely a header/rename change with an unused new code path - same class as Phase 4's "no preprocessor-output change" skip; item 6 is the first item where the new dispatch path actually executes and needs a live check).

6. **Dispatcher single-path cutover (`artnetHandler.h` → `net/artnetService.{h,cpp}`, proper translation unit).** Split the header-only `artnetHandler.h` into `src/net/artnetService.h` (declaration) + `artnetService.cpp` (definition) — this also retires the ODR hazard §1.2.2 (the file currently defines `ArtnetHandler::*` in a header). Rewrite the dispatch loop to the **single path**: for each device, compute the slice `start = firstChannel() - 1`, `len = (start < size) ? min(channelCount(), size - start) : 0`, and call `device->onDmx(this->universe, data + start, len)` (skip if `len == 0`). **Delete the B3-era `getNumberOfChannels() == 512` special-case and the per-channel `set()/get()` loop from the dispatcher** — the base-class `onDmx` bridge (item 5) now owns that, so behavior is identical, and the repeater consumes its own slice. **Universe: plumb it through `onDmx` but do NOT filter by per-device universe.** Today every device is constructed with universe `1` (hardcoded in `main.cpp`) while frames arrive on the single subscribed `config.universe`; a per-device `universe == incoming` filter would drop *all* frames. Multi-universe routing (the rest of the repeater-universe TODO) needs the ArtNet lib's multi-subscription API → **Phase 7**; Phase 5 only makes the dispatcher universe-*aware* (the param is delivered), not universe-*filtering*. Update `main.cpp`'s `#include` and the `app`/`artnet` wiring for the new path; the `init()` lambda's `[this]` capture (B1, already fixed) is preserved. *Verify:* matrix (trio) + **bench repeater Art-Net E2E** (512-ch frame, confirm core-1 sender task survives — R1, the dispatch change must not perturb `writeFrame`/the core-1 task) + T1.

**Done (item 6):** New `src/net/artnetService.h`/`artnetService.cpp` (renamed `ArtnetHandler`→`ArtnetService` to match the new path) replace the header-only `src/artnetHandler.h` (deleted), retiring the §1.2.2 ODR hazard. The `subscribeArtDmxUniverse` lambda now does single-path slice dispatch: for each device, `start = firstChannel() - 1`, early-`continue` if `start >= size`, `len = min(channelCount(), size - start)`, `continue` if `len == 0`, else `device->onDmx(this->universe, data + start, len)`. The B3-era `channelCount() == core::DMX_CHANNELS` special-case and the per-channel `set()/get()` change-detect loop are gone from the dispatcher entirely - `Device::onDmx`'s base bridge (item 5) now owns that. `device.h`'s `onDmx` default was rewritten from item 5's absolute-indexing body to **relative indexing** matching the new slice contract (`data[i]` is channel `firstChannel()+i`; the old `i < 1` guard is gone since the dispatcher only calls `onDmx` with `len > 0`, which implies `firstChannel() >= 1`). `device/repeater.h`'s constructor changed `channel = 0` → `channel = 1` (with a comment) - without this, `firstChannel()=0` would underflow `start = firstChannel()-1` to 65535, `start >= size` would be true, and the repeater would never receive a frame. With `channel = 1`, `start = 0`, `len = min(DMX_CHANNELS, size)`, so `dmx.writeFrame(data, len)` (via the base `onDmx` → `frame(univ, data, len)` → `DmxRepeater::frame`) receives the same buffer/length as before - the repeater's base-`onDmx` per-channel loop (up to 512 no-op `get`/`set` calls, since `DmxRepeater` doesn't override them) is accepted overhead for this item, to be eliminated by item 7's repeater-specific `onDmx`. `main.cpp`: `#include "artnetHandler.h"` → `#include "net/artnetService.h"`, `ArtnetHandler artnet` → `ArtnetService artnet`. `core::DMX_CHANNELS`/`DMX_PACKET_SIZE` (src/core/dmxTypes.h) are now unreferenced anywhere (their only use was the deleted special-case) - left in place as platform-free constants, not removed. `getChannel()` (device.h) is likewise now unreferenced - left as a small public accessor. Verified: native `pio test -e native` → 12/12 PASSED; full 12-env `pio run` → 11/12 SUCCESS (pre-existing `lolin_s2_mini` B26, unrelated). `git status --porcelain` confirms no `Version.h`/`version` churn. **Live HIL on the now-connected XIAO bench (`seeed_xiao_esp32s3`, REPEATER, `Art32-3BD8`):** `tools/verify.sh seeed_xiao_esp32s3 --quick` (T1) passed (boot OK, v2026.1.29, IP 192.168.4.26). `tools/hil_smoke.py` ran the full suite: boot checks, `/heap`, `/config` schema, and **the Art-Net E2E check passed** ("sent 512ch frame, core-1 task alive (no crash)") - confirming R1 (the core-1 DMX sender task survives the new dispatch path) and that the repeater's `channel=0→1` fix works end-to-end. One unrelated check, "POST /config round-trip", failed with `hil_smoke.py`'s built-in 0.2s settle delay; manually repeating the same round-trip with a 0.5s delay against the same running firmware succeeded three times in a row (0→1→0). This is a pre-existing test-timing margin issue in a code path (`config.applyPendingUpdate()`/`/config` REST handler) item 6 does not touch - not a regression from this item.

7. **Per-device `onDmx` overrides: relay, servo, repeater (behavior-preserving).** Now move each device off the base bridge onto its own `onDmx`: `DmxRelay::onDmx` reads `data[0]` vs `threshold` (keep the change-detect against the last applied value so the pin isn't re-written every 44 fps frame); `DmxServo::onDmx` maps `data[0]` via `getAngleValue` (same change-detect); `DmxRepeater::onDmx` is `dmx.writeFrame(data, len); resetSilenceTimer();` (replaces the old `frame(univ,…)` override — the whole-frame copy via the bulk-locked `writeFrame`, B14, **unchanged**, R1). Delete each device's now-dead `set()/get()/frame(univ,…)` overrides once `onDmx` owns the path (or leave `set/get` as thin helpers if `onDmx` calls them internally — pick one and be consistent). `firstChannel()`/`channelCount()` stay as they are (relay/servo 1, repeater `DMX_CHANNELS`). *Verify:* matrix (trio) + bench repeater E2E + T1. (Behavior identical; this is the move that finally collapses the "two parallel data paths" §1.2.6.)

**Done (item 7):** Only `DmxRepeater` got its own `onDmx` override - for `DmxRelay`/`DmxServo`, a hand-written `onDmx` would be byte-for-byte identical to `Device::onDmx`'s base bridge (single channel, `firstChannel()==channel`, `channelCount()==1` -> `i=0`, `ch=channel`, same `data[i] != get(ch)` change-detect, same `set(ch, data[i])`), so they were left on the base bridge - adding a no-op override would be pure churn with no behavior or performance difference. `device/repeater.h`: replaced the `frame(const uint32_t univ, const uint8_t *data, const uint16_t size)` override (which existed only to be called from the old dispatch's `devices[k]->frame(universe, data, size)`) with `void onDmx(uint32_t univ, const uint8_t *data, uint16_t len) override { dmx.writeFrame(data, len); Device::frame(); }` - this is the change that actually matters for this item: it bypasses `Device::onDmx`'s 512-iteration no-op `get()`/`set()` loop (the "accepted overhead" flagged in item 6's Done note) entirely, replacing it with the single bulk-locked `writeFrame` call (B14, unchanged). `firstChannel()`/`channelCount()` unchanged (repeater channel=1 from item 6, `channelCount()==DMX_CHANNELS`). Verified: native `pio test -e native` -> 12/12 PASSED; trio (`d1_mini_oled`/`esp32-devkitc-v4`/`sonoff_basic`) + bench env build clean; full 12-env `pio run` -> 11/12 SUCCESS (pre-existing `lolin_s2_mini` B26). **Live HIL on XIAO bench:** T1 (`verify.sh --quick`) passed boot OK v2026.1.30; `hil_smoke.py` Art-Net E2E passed again ("512ch frame, core-1 task alive (no crash)"), confirming the new `DmxRepeater::onDmx` -> `writeFrame` path works end-to-end and R1 still holds. Same pre-existing "POST /config round-trip" 0.2s-settle flake as item 6 (unrelated, untouched code path) - not a regression. Auto-generated `include/Version.h`/`version` bump from the bench flashes (1.29 -> 1.31) committed separately as `750812a` per the existing "chore: bump auto-generated build version" convention.

**Done (item 8):** `device/dimmer.h`: `channelCount()` bumped 1 -> 2; added `void onDmx(uint32_t univ, const uint8_t *data, uint16_t len) override`. `device/dimmer.cpp`: `onDmx` calls `frame(univ,data,len)`, then if `len>0 && data[0] != logic.getValue()` does `logic.setValue(data[0]); update();` (same change-detect `PwmDimmer::set()` already had via `get()`/`logic.getValue()`), and if `len>1` does `logic.setDuration(pulse); logic.setInterval(data[1]*multiplier)` - **`channel+1` is the strobe-speed channel**, now reachable because `channelCount()==2` makes `ArtnetService`'s slice include it. `PwmDimmer::set()`'s old `else if (dmxChannel == channel - 1)` branch (the dead B17 code - `channel-1` was never in any device's dispatch range under the old `[firstChannel()..firstChannel()+channelCount()-1]` formula with `channelCount()==1`) was deleted; `set()` now only handles `dmxChannel==channel` (still used by `tick()`'s blackout `set(channel,0)`). `DimmerLogic.setDuration()`/`setInterval()` only write `pulse`/`period` - calling them every ~44fps frame (vs. only on a dead-code "change") doesn't touch `interval`/`state`/`previousMillis`, so an in-progress strobe cycle isn't disrupted; pinned by new `test/test_dimmerlogic` case `test_repeated_setduration_setinterval_does_not_disrupt_cycle`. **Compat/behavior change (advertised, per the item's compat note):** a DIMMER device now claims 2 DMX channels (`channel` and `channel+1`) instead of 1 - `channel+1` was always *intended* as the strobe-speed input (per the pre-existing, previously-unreachable `set()` branch) but never actually dispatched; now it is. README's DIMMER section documents the 2-channel behavior and the "Strobe: Fix Stroboscope" TODO is checked off. Verified: native `pio test -e native` -> 13/13 PASSED (new test included); trio + bench env build clean; full 12-env `pio run` -> 11/12 SUCCESS (pre-existing `lolin_s2_mini` B26). `git status --porcelain` confirms no `Version.h`/`version` churn. T1/live-HIL skipped per the item's own note - the XIAO bench has no DIMMER device configured (it's a REPEATER), so this item is native-test + build-verified only, same documented limitation as item 4/Phase-1 B16.

8. **Dimmer `onDmx` + 2-channel strobe — the real B17 fix (behavior change).** `PwmDimmer::onDmx`: `data[0]` → dimmer level (→ `DimmerLogic`), and when `channelCount() == 2`, `data[1]` → strobe speed (→ `DimmerLogic.setStrobeSpeed`). Bump `channelCount()` from 1 to 2. **This is what makes the strobe sub-channel reachable** — B17's root cause was the dispatcher only ever delivering one channel; with item 6's slicing now honoring `channelCount()`, declaring 2 channels delivers `data[1]`. Extend `test/test_dimmerlogic/` with strobe-speed-via-second-channel coverage. Update the README/AGENTS "stroboscope is broken / Strobe name misleading" notes to reflect the fix (README TODO "Fix Stroboscope" can be checked off). **Compat note:** a DIMMER device now claims 2 DMX channels instead of 1 — document this as the deliberate, advertised behavior change it is (it was always *supposed* to be a 2-channel dimmer+strobe; the second channel was simply unreachable). *Verify:* native green; trio + T1 (live strobe needs a DIMMER board — build-level + native-test stand-in, documented).

9. **Blackout-on-silence becomes policy + `"blackout"` config flag (delivers a README TODO; finishes B20).** Base `tick()` already consults `manualOverride`; add the `blackoutOnSilence` policy (item 5 hook) so a device can opt out. Add `bool blackout = true;` to `DeviceConfig` (config model), wire it through the codec (`configFromJson`/`configToJson` — an **additive** JSON key with a `true` default, so old configs and the frozen schema are unaffected; `hil_smoke.py` `REQUIRED_DMX_FIELDS` is a subset check, so an extra key is fine) and through `deviceFactory`/`setBlackout`. Fix `PwmDimmer`'s *separate* silence-timeout (`dimmer.cpp` ~95, the `"DMX Silence Timeout"` path that B20 explicitly deferred to "the Phase 5 silence-blackout-is-policy redesign") to honor the same policy + `manualOverride`. *Verify:* matrix (trio) + T1; if the policy decision is pure-extractable, a small `core/` test, else build+bench.

**Done (item 9):** `config.h`: added `bool blackout = true;` to `DeviceConfig`. `config.cpp`: `configFromJson` reads `deviceConfig.blackout = channel["blackout"] | true` (additive, defaults `true` for old configs); `configToJson` always emits `channels[i]["blackout"]`. `main.cpp`: since `deviceFactory` is item 10 (not yet landed), wired directly into the existing device-construction loop — after the `switch`, `if (dmx_devices[i]) dmx_devices[i]->setBlackout(config.dmx[i].blackout)`; item 10 will carry this call along when it extracts the factory. `device/dimmer.cpp`: `PwmDimmer::tick()`'s separate "DMX Silence Timeout" branch was rewritten to mirror `Device::tick()`'s guard exactly — on silence it now does `if (!silenceLogged) { LOG(...); silenceLogged = true; } frame(); if (!manualOverride && blackoutOnSilence) { logic.setValue(0); update(); }` (previously: unconditional `set(channel,0)` + manual `lastChange = millis()` reset, ignoring both policy flags and re-logging every silence tick). Also added `manualOverride = true;` to `PwmDimmer::flip()`, matching `DmxRelay::flip()`'s existing B20 pattern — without this, a button-press flip on a DIMMER device would get silently undone by the next silence tick. The policy decision itself (two bools gating a `set(channel,0)`) wasn't pure-extractable into `core/` — it's intrinsically tied to `Device`'s protected state (`lastChange`/`silenceLogged`/`manualOverride`/`blackoutOnSilence`) and `PwmDimmer`'s `logic`/`update()`, so build+bench was used per the item's own fallback. `CONFIG_BUFFER_SIZE` (1024 bytes) headroom: the new `"blackout":true` key adds ~16 bytes per `dmx[]` entry (×8 max on ESP32) — confirmed fine, `hil_smoke.py`'s live `GET /config` against the bench's single-REPEATER config round-trips cleanly with the new key present. Verified: native `pio test -e native` → 13/13 PASSED (unchanged — no new native test, per the build+bench fallback above); trio (`d1_mini_oled`/`esp32-devkitc-v4`/`sonoff_basic`) build clean. **Live HIL on XIAO bench:** `tools/verify.sh seeed_xiao_esp32s3 --quick` (T1) passed, boot OK v2026.1.31, IP 192.168.4.26. `tools/hil_smoke.py --soak 0`: boot checks, `/heap`, `/config` schema (now including `"blackout":true` for the REPEATER device), and **Art-Net E2E passed** ("512ch frame, core-1 task alive, no crash") — confirms R1 still holds and the new `setBlackout()` wiring doesn't disturb the repeater's boot/dispatch path. Same pre-existing "POST /config round-trip" 0.2s-settle flake as items 6/7 (unrelated `config.applyPendingUpdate()` timing, not touched by this item) — left the bench at `universe=3` after the failed revert, manually restored to `universe=2` via a follow-up `POST /config` + 0.5s-settled `GET` before finishing. No `Version.h`/`version` churn from this session's bench flash beyond the existing 1.31 (already committed).

10. **`app/deviceFactory.{h,cpp}`.** New `std::unique_ptr<Device> makeDevice(const art::DeviceConfig &cfg, uint8_t universe)` replacing the `switch` in `main.cpp` (the `case art::DmxType::Relay/Servo/Dimmer/Repeater` block). The `#if FEATURE_SERVO`/`FEATURE_DIMMER`/`FEATURE_DMX_PORT` guards move *inside* the factory (each `case` body compiled only when its feature is on; `Disabled`/unknown → `nullptr`). `main.cpp` calls the factory in its construction loop (still writing into the existing `dmx_devices[]` raw array — the `std::array<unique_ptr>` swap is item 11, to keep this diff small). Include the device headers in `deviceFactory.cpp`, not `main.cpp`. *Verify:* matrix (trio + at least one OLED + one Sonoff to exercise the feature guards) + T1.

**Done (item 10):** New `src/app/deviceFactory.h` declares `std::unique_ptr<Device> app::makeDevice(const art::DeviceConfig &cfg, uint8_t universe)`, taking only `config.h`/`device/device.h`. New `src/app/deviceFactory.cpp` includes `boards/features.h` plus `device/relay.h` unconditionally and `device/dmxServo.h`/`device/dimmer.h`/`device/repeater.h` under their respective `#if FEATURE_SERVO`/`FEATURE_DIMMER`/`FEATURE_DMX_PORT` guards (each header is also self-guarded, so this is belt-and-suspenders), and implements the same `switch (cfg.type)` body the `main.cpp` switch had, `return std::make_unique<...>(...)` per case, `default: return nullptr`. `main.cpp`'s device-construction loop is now `dmx_devices[i] = app::makeDevice(config.dmx[i], 1).release();` followed by the existing `if (dmx_devices[i]) setBlackout(...) else LOG("Incompatible DMX device type:")` - the `LOG` move from "unhandled switch case" to "factory returned nullptr" is behavior-identical (both cover `Disabled` and any type whose feature is off on this board). `main.cpp` no longer `#include`s `device/relay.h`/`dimmer.h`/`dmxServo.h`/`repeater.h` directly (replaced by a single `#include "app/deviceFactory.h"`) - `device/device.h` is kept for the `Device*` type. `dmx_devices[]` stays a raw `Device*[MAX_DMX_DEVICES]` (`.release()` from the factory's `unique_ptr`); the `std::array<unique_ptr>` swap is item 11. Verified: native `pio test -e native` -> 13/13 PASSED (unchanged, no factory-specific native test - `makeDevice` is Arduino-dependent via the device headers it includes, not platform-free); trio (`d1_mini_oled`/`esp32-devkitc-v4`/`sonoff_basic`) + both OLED envs (`d1_mini_oled`, `heltec_wifi_kit_8`) + both Sonoff envs (`sonoff_basic`, `sonoff_s31`) all build clean - exercises every `FEATURE_*` combination the factory's `#if` guards depend on. **Live HIL on XIAO bench:** `tools/verify.sh seeed_xiao_esp32s3 --quick` (T1) passed, boot OK v2026.1.32, IP 192.168.4.26 - confirms the REPEATER device (constructed via `makeDevice`'s `FEATURE_DMX_PORT` branch) still instantiates and boots correctly. `include/Version.h`/`version` bump (1.31 -> 1.32) from this bench flash committed separately as `b32aa29` per the established convention.

11. **`app/app.{h,cpp}` + `std::array<std::unique_ptr<Device>, MAX_DMX_DEVICES>` + `start()` relocation + `safeMode` move; `main.cpp` → ~20 lines.** The capstone. `App` owns every current `main.cpp` global by value/`unique_ptr`: `server`, `dnsServer`, `connect`, `config`, `artnet`, `status`, (`#if FEATURE_OLED` `statusDisplay`), `button`, and `std::array<std::unique_ptr<Device>, MAX_DMX_DEVICES> devices`. `App::setup()` encodes the **sacred boot order** (FS mount → `config.load()` → `StatusLed` → button → `#if FEATURE_OLED` OLED → factory-construct devices → **`App` calls each `device->start()`** → `connect.init/connect` → `artnet.init(...)` [**no longer calls `device->start()`** — delete that loop from `ArtnetHandler::init`, Phase 5 bullet 4] → API + OTA → `server.begin()` **last**, with the WiFi-before-`begin()` constraint as a single asserted comment, not `!!!!!`). `App::loop()` = the current `loop()` body (`applyPendingUpdate` → `button.check` → `artnet.loop` → `devices[i]->tick()` → OTA → `connect.loop` → `#if FEATURE_OLED` `statusDisplay->loop()`). **Button-handler wrinkle:** `handleEvent` references the (now-`App`-owned) `config`/`devices`/`status`/`statusDisplay`. Prefer making `App` implement AceButton's `IEventHandler` (override `handleEvent(AceButton*, uint8_t, uint8_t)`) and `button.setEventHandler(this)`; fall back to a file-scope `App *gApp` + free trampoline only if the `IEventHandler` route fights the existing `ButtonConfig`. **Remove `extern StatusLed *status`** (connect.h §1.2.3) — inject `StatusLed&` (or a status-callback) into `Connect::init` instead. Move `safeMode()` to `app/safeMode.{h,cpp}`. `main.cpp` becomes: construct `App`, `app.setup()`, `app.loop()`. *Verify:* **all 12 envs + T2** (`tools/verify.sh --full`): full regression — boot order, button flip + long-press WiFi reset, WiFi reconnect, Art-Net E2E, repeater core-1 task; plus the Gotcha #11/12 multi-device configs (3×DIMMER on an 8266 board + repeater-with-relay on ESP32) if those boards are benchable. *(If the diff is too large to review, split 11a = App shell owning the globals with the raw array + boot order + `loop()` move, 11b = `unique_ptr` array + factory-wired construction + `start()` relocation + `extern status` removal.)*

**Done (item 11):** New `src/app/app.h`/`src/app/app.cpp` define `class App : public ace_button::IEventHandler` owning every former `main.cpp` global: `AsyncWebServer server{80}`, `DNSServer dnsServer`, `Connect connect`, `art::Config config`, `ArtnetService artnet`, `std::unique_ptr<StatusLed> status`, `ace_button::ButtonConfig buttonConfig` + `AceButton button`, `std::array<std::unique_ptr<Device>, MAX_DMX_DEVICES> devices` (+ a parallel raw `Device *deviceList[MAX_DMX_DEVICES]` bridge for `ArtnetService::init`'s `Device**` API - the array is a member, not a local, because `ArtnetService` keeps and dereferences the pointer for the program's lifetime), `deviceCount`, and (`#if FEATURE_OLED`) `std::unique_ptr<StatusDisplay> statusDisplay`. `App::setup()` is the old `setup()` body translated 1:1 onto these members, preserving the sacred boot order, with three behavioral relocations: (1) **`device->start()` moved from `ArtnetService::init`'s loop into `App::setup()`'s device-construction loop** - `for (i < deviceCount) { devices[i] = app::makeDevice(...); if (devices[i]) { setBlackout(...); devices[i]->start(); } else LOG(...); deviceList[i] = devices[i].get(); }` - and the corresponding `for (...) dmxDevice[i]->start();` loop deleted from `ArtnetService::init` (`src/net/artnetService.cpp`); (2) **`extern StatusLed *status` removed** - `Connect::init` gained a third parameter `StatusLed *status` (stored as a new protected `Connect` member; `connect.h`'s comment notes "status outlives this Connect (owned by App) - Connect only observes it"), and the free `configModeCallback` function became a `[this](AsyncWiFiManager *myWiFiManager) { this->status->set(...); ... }` lambda inside `Connect::init` (hit-and-fixed a `'status' is not captured` error from the `status` *parameter* shadowing the new `status` *member* - `[this]` doesn't implicitly capture a shadowed local, fixed with explicit `this->status->`); (3) **button handling via `ace_button::IEventHandler`** - `App::handleEvent(AceButton*, uint8_t, uint8_t)` overrides the interface and is registered with `buttonConfig.setIEventHandler(this)` (not `button.setEventHandler(...)`, which is for free-function pointers), carrying over the old free-function `handleEvent`'s long-press-WiFi-reset and short-press-flip-devices logic verbatim against `this->devices`/`this->status`/`this->statusDisplay`. `app/safeMode.h`/`app/safeMode.cpp` (new) hold the `safeMode(const String &reason)` LED-blink-forever halt, moved verbatim out of `main.cpp` with its filesystem-mount-failure rationale as a header comment. `main.cpp` is now 14 lines: `#include "app/app.h"`, a global `App app;`, and `setup()`/`loop()` one-line-delegating to `app.setup()`/`app.loop()`. **ODR fix found along the way:** `app.h` (included by both `app.cpp` and `main.cpp`, transitively pulling in `hw/oledDisplay.h` under `#if FEATURE_OLED`) turned `StatusDisplay::setStatus32()`/`setStatus64()` - non-`inline` out-of-class member definitions inside the header - into a "multiple definition" linker error once 2 TUs included it; fixed by marking both `inline` in `src/hw/oledDisplay.h`. Verified: native `pio test -e native` -> 13/13 PASSED; full 12-env `pio run` -> 11/12 SUCCESS (`lolin_s2_mini` failure confirmed this run to be the pre-existing B26 `-fpermissive`/`ESPAsyncWebServer.h:1691` signature, not a new regression). **Live HIL T2 on XIAO bench** (`tools/verify.sh seeed_xiao_esp32s3 --full`, v2026.1.33): flash + boot OK, `GET /heap`/`GET /config schema` pass, **Art-Net E2E passes** ("512ch frame, core-1 task alive (no crash)" - confirms `device->start()` from `App::setup()` correctly brought up the repeater's core-1 sender task under the new boot path), `POST /reboot` + post-reboot `boot_check` pass (confirms `App`'s full boot order survives a warm reboot), and a 4-minute Art-Net-flood soak shows a flat heap trend (`[250792, 250820, 250820, 250804, 250804, 250804]`, no leak). "POST /config round-trip" failed with the same pre-existing 0.2s-settle flake documented in items 6/7/9 (`config.applyPendingUpdate()` timing, untouched by this item) - manually restored the bench to `universe=2` via `POST /config` + 0.5s-settled `GET` afterward. `include/Version.h`/`version` bump (1.32 -> 1.33) from this bench flash committed separately as `803fcdb`.

12. **(tail) AGENTS.md + README sync; Phase 5 retro paragraph.** Update AGENTS.md: Repository Layout gains `src/app/`, `src/core/`, `src/net/`; the device-instantiation-`switch` references (Gotcha #4, §6 "Adding a new device type" step 4) point at `app/deviceFactory` instead of the `main.cpp` switch; the dispatch description (§5 Art-Net dispatch) reflects the single `onDmx(universe, slice)` path; note the new `[env:native]`/`pio test -e native` and the `test/test_*` suites. README: check off "Fix Stroboscope", document the DIMMER 2-channel change (item 8) and the optional `"blackout"` flag (item 9). *Verify:* docs only (no build), though running the matrix once more before the phase-end merge is cheap insurance.

**Done (item 12):** `AGENTS.md` rewritten for the Phase 5 end-state: header now notes the `big-refactor` restructure and points at this file; §1 documents `pio test -e native` + the `native-test` CI job (the "no unit tests/no CI" line is gone); Gotcha #4's `main.cpp` switch reference -> `app::makeDevice`'s `switch (cfg.type)` in `src/app/deviceFactory.cpp`; Gotcha #12 now says `boards/`/`device/`/`dmx/`/`app/` (not `board.h`/`main.cpp`) and adds "run `pio test -e native` first" for `core/` changes. §3 Repository Layout fully rewritten: `main.cpp` (14 lines), new `app/` (`app.{h,cpp}`, `deviceFactory.{h,cpp}`, `safeMode.{h,cpp}`), new `core/` (`dmxTypes.h`, `dimmerLogic.h`) with its native-test-binding note, new `net/artnetService.{h,cpp}`, new `platform/` (`filesystem`/`pwm`/`servo`), `boards/features.h` added, `device/dimmer.{h,cpp}` (was `strobe.*`), `dmx/dmx.h` now declares `DmxPort` (was `DmxProxy`) under `#if FEATURE_DMX_PORT` (was `#ifndef SONOFF_BASIC`), plus a new `test/` tree entry. §5 rewritten around `App::setup()`/`App::loop()` - the 9-step boot order now names `app::makeDevice`/`setBlackout`/`device->start()` (relocated from `ArtnetService::init`, item 11), `connect.init(&server,&dnsServer,status.get())` (no `extern status`), and `buttonConfig.setIEventHandler(this)`; the Art-Net dispatch paragraph now describes the single `device->onDmx(universe, data+start, len)` slice call (item 6/7) instead of the old per-channel `frame()`+`set()` loop; the core-1 repeater paragraph says `DmxPort`/`DmxRepeater::onDmx()` (was `DmxProxy`/`frame()`). §6 Device Abstraction interface block updated to v2 (`tick`/`onDmx`/`firstChannel`/`channelCount`/`setBlackout`, `uint16_t channel` params); the four-type table replaces `Strobe`/`device/strobe.h` with `PwmDimmer`/`device/dimmer.h`+`.cpp` (2-channel, item 8) and gives `DmxRepeater` its `onDmx`-override description (item 7); "Adding a new device type" recipe rewritten around `core::DmxType`/`fromWireString`/`toWireString` + `app::makeDevice`'s switch (was `config.h`'s enum + `main.cpp`'s switch). §7 Configuration System: `std::vector<DeviceConfig>`/`std::vector<WiFiNet>` (was `LinkedList<DmxChannel*>`), documents the additive `"blackout"` field (item 9) and the B11 `stageUpdate`/`applyPendingUpdate` async-POST contract. §8 Conventions: naming examples updated (`PwmDimmer`, `ArtnetService`, `DmxPort`, `App`), `core::`/`art::` namespace split documented, the `core/`-ships-with-native-tests rule called out explicitly, and the "known rough edges" list drops the now-fixed `Strobe`-naming and stroboscope items (both resolved by items 4/8), keeping only the still-true "repeater ignores universe" and "multi-device configs lightly tested" notes. New §9 entry for `REFACTORING_PLAN.md` itself, summarizing Phases 0-5 as complete and Phases 6-8 as not started - this **is** the Phase 5 retro paragraph the item asked for. README: `dmx[]` example (`GET /config`) gained `"blackout": true`; TODO's "Make blackout on DMX timeout optional" checked off (`[x]`, item 9 closes it) - "Fix Stroboscope" was already checked off by item 8. *Verify:* docs-only change, no source files touched; the 11/12 build matrix + native 13/13 from item 11 (same commit range, no intervening source edits) stand as the "cheap insurance" build. **Phase 5 is now complete** (items 1-12 all done) - see the per-item "Done" notes above and in `memory/big-refactor-plan.md` for the full record. Next: Phase 6 (Config & robustness) per this file's Phase 6 section, pending user direction.

**Out of Phase 5 scope (do NOT pull these in — they belong to later phases or would bloat the diff):**
- **`core/configModel.h` + `core/configCodec.{h,cpp}` full extraction** with save→load→deep-equal native tests, and the **ArduinoJson 6→7** decision — these are **Phase 6** ("Config & robustness"). Phase 5 item 3 only swaps the container (`LinkedList`→`std::vector`); the codec stays in `config.cpp` on `StaticJsonDocument<1024>`.
- **`net/webApi.{h,cpp}` split + moving the `info` block (WiFi.SSID/RSSI) out of `config.cpp::configToJson`** (§1.2.9 layering violation) — **Phase 6** (API surface work). `api.h` stays as-is in Phase 5.
- **Multi-universe / per-device universe filtering + ArtNet multi-subscription** — **Phase 7** (needs the `hideakitai/ArtNet` API bump). Phase 5 only plumbs the universe param (item 6).
- **Wholesale directory rename** `device/`→`devices/`, `dmx/`→`platform/dmxPort*`, `hw/oledDisplay`→`.cpp` split — cosmetic churn; Phase 5 creates the *new* `app/`/`core/`/`net/` dirs (where new or relocated code lands) but leaves the existing `device/`/`dmx/`/`hw/` trees in place to keep diffs reviewable. Final Part-2 layout convergence is a Phase 8 cleanup.
- **Dependency bumps** (AsyncWebServer unification, ArtNet/core 3.x, the B26 `lolin_s2_mini` fix) — **Phase 7**.

**Phase 5 sequencing & risk notes:**
- **Binding rule, concretely:** items 2 and 4 (and any other `core/` extraction) MUST include their native test in the same commit. Item 1 exists solely to make that possible — it must land first.
- **Behavior-identical before behavior-change:** items 5→6→7 deliberately keep runtime behavior byte-identical (base bridge, then dispatch cutover, then per-device moves) so that when items 8 (strobe) and 9 (blackout policy) *do* change behavior, the dispatch plumbing underneath them is already proven on the bench. Don't reorder a behavior-change ahead of the cutover.
- **R1 (core-1 DMX task) is in the blast radius of items 6 and 7.** The repeater's data path (`writeFrame` bulk-locked copy → core-1 sender task, copy-under-mutex-then-release-before-blocking-I/O, AGENTS Gotcha #5) must be carried over verbatim — `onDmx` replaces only the *call site* (`frame(univ,…)` → `onDmx`), never the `DmxPort`/task internals. Bench repeater E2E after both items.
- **Compat contract (Part 5) holds throughout:** the only schema touch in Phase 5 is item 9's additive `"blackout"` key (default `true`). `dmx[]` stays index-based; all existing keys/type-strings parse unchanged; `hil_smoke.py`'s `REQUIRED_*` checks must stay green every item.
- **`MAX_DMX_DEVICES`** (8 ESP32 / 4 ESP8266, AGENTS Gotcha #6) bounds the `std::array` in item 11 — keep the array size and every loop bound tied to that macro, exactly as the raw array is today.
- **Native env can't see `Arduino.h`.** If a `core/` extraction is tempted to reach for `String`/`millis()`/`HIGH`/`LOW`, that's the signal it's not actually platform-free — pass the value in as a parameter instead (this is what keeps `dimmerLogic`/`dmxTypes` testable).

**Verify:** build matrix; full regression of all four device types on bench (per AGENTS Gotcha #11/12 — multi-device config 3×DIMMER on 8266 + repeater-with-relay on ESP32); native tests for dimmer logic + dispatch slicing.

### Phase 6 — Config & robustness — M

**Design goals (the four bullets this phase delivers):**
- ArduinoJson 7 (elastic `JsonDocument` kills the fixed-1024 gotcha) **or** stay on 6 with measured capacity + `doc.overflowed()` checks — decide by flash budget on the 1M Sonoff; add config-version field for future migrations.
- API: `info` block (version/ssid/rssi/heap/uptime) built in `webApi`, not in the persistence codec; `_needReboot` semantics kept; optional `GET /status`.
- Optional basic-auth for `/update` and mutating routes (theater LAN threat model = low; make it a config flag, default off).
- Safe-mode page served from the diagnostic AP.

**One commit per item, in this order:**

**Done (item 1):** **Decision: moved to ArduinoJson 7.** Measured both candidate envs at the AJ6 baseline (this commit's parent): `sonoff_basic` flash 388153/892912 (43.5%), RAM 34360/81920 (41.9%); `esp32-devkitc-v4` flash 966817/1310720 (73.8%), RAM 47812/327680 (14.6%). Trialed in a throwaway worktree (`/tmp/aj7-trial`, removed after measurement): bumping `lib_deps` to `bblanchon/ArduinoJson@^7` alone (AJ7 keeps `StaticJsonDocument<N>`/`createNestedArray` as deprecated-but-working aliases) cost `sonoff_basic` +6236B flash / +52B RAM and `esp32-devkitc-v4` +7852B flash / +16B RAM; swapping `StaticJsonDocument<CONFIG_BUFFER_SIZE>` for the elastic `JsonDocument` on top of that added essentially nothing more (+144B / +84B flash respectively, RAM unchanged) - elastic `JsonDocument` is **not** meaningfully more expensive than the fixed-capacity v6 type at this document size. Given `sonoff_basic` has 55.8% flash headroom and `esp32-devkitc-v4` has 25.6%, a ~6-8KB/~1.6%-of-budget cost to remove the fixed-1024 capacity-overflow risk (Gotcha #7's core concern, R6) is an easy call. Landed in this commit: `platformio.ini` - `bblanchon/ArduinoJson@^7` in both `[common]` (firmware envs) and `[env:native]`. `src/config.cpp` - all four `StaticJsonDocument<CONFIG_BUFFER_SIZE> doc;` sites (`load`/`save`/`serialize`/`applyPendingUpdate`) became `JsonDocument doc;` (elastic, heap-allocated); `createNestedArray` calls left as-is (still valid in AJ7, a future cleanup could move to `doc["x"].to<JsonArray>()` but that's cosmetic). **`_pendingJson` (B11's staged-update buffer, `config.h`) stays a fixed `char[CONFIG_BUFFER_SIZE]`** - as anticipated, AJ7's elastic document doesn't touch this: it's a raw serialization target, not a `JsonDocument`, so `CONFIG_BUFFER_SIZE` (1024B) remains the practical ceiling on a single `POST /config` payload (`stageUpdate` already rejects oversized payloads cleanly via its `written < sizeof(_pendingJson)` check - unchanged). For `load()`/`applyPendingUpdate()`'s `deserializeJson` calls against the now-elastic `doc`: AJ7 has no `doc.overflowed()` (that was a v6 fixed-capacity concept) - an allocation failure surfaces as `DeserializationError::NoMemory` via the existing `error` check, which both call sites already handle (return `false` / skip `configFromJson` without partial state changes) - no new code needed for "clean rejection not truncation". **`configVersion` field** (new, regardless of the AJ6/7 outcome): `config.h` gains `#define CONFIG_SCHEMA_VERSION 1` and `uint8_t configVersion = CONFIG_SCHEMA_VERSION;` on `Config`; `configFromJson` reads `doc["configVersion"] | 1` (additive, defaults to `1` for configs written before this field existed - R5 compat); `configToJson` always writes `CONFIG_SCHEMA_VERSION` (the current constant, not the loaded value) - establishes the migration hook without performing any migration yet. **Native test deferred to item 2:** `configFromJson`/`configToJson` live in `config.cpp` (Arduino-coupled - `String`, `ESP_FS`, `WiFi`), not yet includable under `[env:native]`; item 2's `core/configCodec` extraction is where `configVersion` round-trip and oversized/garbage-input rejection get their native save→load→deep-equal coverage, per that item's own description - duplicating a throwaway test here would be redundant. Verified: `pio test -e native` -> 13/13 PASSED (unchanged); full 12-env `pio run` -> 11/12 SUCCESS (pre-existing `lolin_s2_mini` B26, confirmed same `ESPAsyncWebServer.h:1691` `-fpermissive` signature); final sizes with `configVersion` included: `sonoff_basic` 394709/892912 (44.2%, RAM 34428/81920=42.0%), `esp32-devkitc-v4` 974997/1310720 (74.4%, RAM unchanged 47828/327680=14.6%). **Live HIL on XIAO bench** (`tools/verify.sh seeed_xiao_esp32s3 --quick`, v2026.1.34): boot OK, IP 192.168.4.26; `GET /config` confirms `"configVersion": 1` now present and `hil_smoke.py`'s `REQUIRED_*` schema checks still pass (additive key, subset check) - `GET /heap`, `/config` schema, and Art-Net E2E ("512ch frame, core-1 task alive, no crash") all PASS. Same pre-existing "POST /config round-trip" 0.2s-settle flake as items 6/7/9/11 (unrelated `config.applyPendingUpdate()` timing) - manually restored `universe=2` via `POST /config` + 0.5s-settled `GET` afterward. `include/Version.h`/`version` bump (1.33 -> 1.34) from this bench flash committed separately as `7ed8e16`.

**Part 4 R6 updated:** "ArduinoJson 7 decision (Phase 6) made against this budget" -> "**Decided (Phase 6 item 1): ArduinoJson 7.** Cost ~6-8KB flash / ~16-84B RAM on `sonoff_basic`/`esp32-devkitc-v4` (1.6%/0.6% of total flash) - within R6's budget on both; removes the v6 fixed-capacity overflow class entirely (elastic `JsonDocument`). `_pendingJson`'s fixed `CONFIG_BUFFER_SIZE` (1024B) staged-update ceiling is unaffected and remains the per-POST payload limit."

2. **`core/configModel.h` + `core/configCodec.{h,cpp}` extraction, with native save→load→deep-equal tests.** `DeviceConfig`/`HardwareConfig` (`config.h`) are already Arduino-free PODs (uint types + `core::DmxType`) — move them verbatim into new `src/core/configModel.h`, plus a platform-free `core::WifiNet` (`std::string ssid, pass; bool dhcp; uint8_t order;`, mirroring `art::WiFiNet` but without `Arduino String`). New `src/core/configCodec.{h,cpp}`: `core::dmxChannelFromJson`/`ToJson` (the per-`dmx[]`-entry conversion, using `core::fromWireString`/`toWireString` — already core-side from Phase 5 item 2), `core::hardwareFromJson`/`ToJson`, `core::wifiNetFromJson`/`ToJson` — all pure ArduinoJson + `configModel`, no `String`/`WiFi`/`CHIP_ID`/`Version.h`. `config.h`: `art::DeviceConfig`/`HardwareConfig` become aliases (`using DeviceConfig = core::DeviceConfig;` etc.) the same way Phase 5 item 2 aliased `DmxType`; `art::WiFiNet` keeps its `String` fields (REST/captive-portal call sites use `String`) but `configFromJson`/`configToJson` convert `core::WifiNet`↔`art::WiFiNet` at the boundary (`.c_str()`/`String(...)`, same adapter pattern as `dmxTypeFromString`/`ToString`). `config.cpp`'s `configFromJson`/`configToJson` shrink to: per-entry loop calling `core::configCodec` functions for `dmx[]`/`hw`/`wifi`, plus the handful of genuinely Arduino-coupled top-level fields (`host` as `String`, `universe`, and — until item 3 lands — the `info`/`_needReboot` block). New `test/test_configcodec/`: round-trip every `DeviceConfig`/`HardwareConfig`/`WifiNet` field through `ToJson`→`FromJson`→deep-equal; legacy `"BINARY"`/`"RELAY"` type strings (R5 compat, reusing Phase 5 item 2's fixtures); a full-`Config`-shaped JSON doc save→load→deep-equal at the `config.cpp` level (not just `core/`). *(Third `core/` extraction — binding rule: native tests land in this commit.)* *Verify:* native green; full 12-env `pio run` + T1 (R5 compat — `dmx[0].type` round-trip on the bench, same check as Phase 2's verification).

**Done (item 2):** `src/core/configModel.h` (new, header-only, `#pragma once`, no Arduino includes): `core::DeviceConfig`/`core::HardwareConfig` moved verbatim from `config.h` (same fields/types; `DeviceConfig` keeps its `bool blackout = true` default member initializer, which is platform-independent), plus new `core::WifiNet { std::string ssid, pass; bool dhcp; uint8_t order; }`. `src/core/configCodec.h` (new, header-only `inline` functions, matching the `dmxTypes.h`/`dimmerLogic.h` pattern — **no separate `.cpp`**: `[env:native]`'s `build_src_filter = -<*>` excludes `src/`, so a `.cpp` here wouldn't be linked into the native test binary; a first pass wrote `configCodec.cpp` and it had to be merged back into the header before `pio test` would build): `core::dmxChannelFromJson(JsonVariantConst, const DeviceConfig &defaults)`/`dmxChannelToJson`, `core::hardwareFromJson(JsonVariantConst, const HardwareConfig &defaults)`/`hardwareToJson`, `core::wifiNetFromJson(JsonVariantConst)`/`wifiNetToJson` — all pure ArduinoJson 7 + `configModel`, using `strcasecmp` (not `String::equalsIgnoreCase`) for the `"HIGH"`/`"LOW"` level string. `config.h`: `using DeviceConfig = core::DeviceConfig;` / `using HardwareConfig = core::HardwareConfig;` (mirroring the existing `using DmxType = core::DmxType;`); removed the now-dead `dmxTypeFromString` adapter (its only caller was the old `configFromJson`, which now calls `core::fromWireString` via the codec) - `dmxTypeToString` stays (still used by `app/app.cpp` and `hw/oledDisplay.h`). **Side effect**: `core::HardwareConfig` has no default member initializers (its old defaults - `DEFAULT_PWM_FREQ`/`LED_PIN`/`DEFAULT_BUTTON_PIN`/`DEFAULT_BUTTON_LONG_PRESS*1000` - are board-specific Arduino macros that `core/` can't reference), so `Config::Config()` now explicitly initializes all four `hardware.*` fields to those macros (previously implicit via the struct's default member initializers) - without this, a config predating any `"hw"` key would load with zeroed/garbage `hardware` fields. `config.cpp`'s `configFromJson`/`configToJson` now delegate `wifi[]`/`dmx[]`/`hw` entirely to the codec: `wifiNetFromJson`/`ToJson` convert `core::WifiNet`↔`art::WiFiNet` via `.c_str()`/`String(...)` at the loop boundary; `dmxChannelFromJson` is called with a `defaults` struct built from the same literals the old inline code used (`channel=0`, `type=Disabled`, `threshold=127`, `pulse=10`, `multiplier=1`, `pin=LED_BUILTIN`, `level=LOW`, `blackout=true`); `hardware = core::hardwareFromJson(doc["hw"], hardware)` replaces the four individual `isNull()`-guarded field updates (the codec does the same per-field `isNull()` check internally); `configToJson` calls `core::hardwareToJson(hardware, doc["hw"].to<JsonObject>())` and `core::wifiNetToJson`/`dmxChannelToJson` per array entry via `createNestedObject()`. `host`/`universe`/`configVersion`/`info`/`_needReboot` remain in `config.cpp` (Arduino-coupled or, for `info`, awaiting item 3's relocation). New `test/test_configcodec/test_configcodec.cpp` (8 tests): per-field round-trip for `DeviceConfig`/`HardwareConfig`/`WifiNet`; legacy `"BINARY"`/`"RELAY"`→`Relay`→`"BINARY"` (R5 compat); missing-field defaults (including the `level` special case - an absent `"level"` key always yields `LOW` regardless of `defaults.level`, matching the pre-extraction `equalsIgnoreCase` behavior); `hw` partial-update-keeps-defaults; and a full config-shaped doc (`wifi[]`+`dmx[]`+`hw`) serialized→`deserializeJson`→re-decoded→deep-equal, exercising every codec function together the way `config.cpp` does. Verified: `pio test -e native` → 21/21 PASSED (13 pre-existing + 8 new); full 12-env `pio run` → 11/12 SUCCESS (`lolin_s2_mini` B26 pre-existing, same signature); trio sizes essentially unchanged from item 1 (`sonoff_basic` 396657/892912=44.4% flash/34472 RAM≈unchanged, `esp32-devkitc-v4` 974997/1310720=74.5%/47828 RAM unchanged, `d1_mini_oled` 438033/1044464=41.9%/35992 RAM). **Live HIL on XIAO bench** (`tools/verify.sh seeed_xiao_esp32s3 --quick`, v2026.1.35): boot OK, IP 192.168.4.26; `GET /config` confirms `dmx[0]` round-trips as `{"type":"REPEATER","channel":0,"pin":0,"level":"LOW","multiplier":1,"pulse":10,"threshold":127,"blackout":true}` and `hw`/`configVersion`/`host`/`universe` all present and correct - the codec-mediated path produces byte-identical JSON shape to before. `include/Version.h`/`version` bump (1.34 → 1.35) committed separately as `47da317`.

3. **`net/webApi.{h,cpp}` replaces `api.h`; `info` block moves out of `configToJson` (§1.2.9 layering fix).** New `src/net/webApi.h`/`webApi.cpp` (declaration/`.cpp` pair, matching the `artnetService` split from Phase 5 item 6) own every handler currently in `api.h` (`GET /heap`, static `/www/`, `GET/POST /config`, `POST /reboot`, `POST /reset-wifi`, 404). `core::configCodec`'s `configToJson` (item 2) never wrote `info`/`_needReboot` in the first place — those stay exactly where they are today in `art::Config::configToJson` (the persistence codec keeps `_needReboot` since it's `_dirty`, config-derived; only `info` — `ssid`/`rssi`/`version`/`built`/`chip`/`id`/`max_dmx_devices` — was ever the layering violation per the §1.2.9 finding). Move `info`'s construction from `art::Config::configToJson` into `webApi`: `GET /config`'s handler calls `config.serialize(json)` for the persisted document, then merges in a runtime `info` object (`WiFi.SSID()`/`WiFi.RSSI()`/`ESP.getFreeHeap()`/`millis()`-uptime/`VERSION`/`BUILD_TIMESTAMP`/`CHIP_ARC`/`CHIP_ID`/`MAX_DMX_DEVICES` — same fields as today, plus `uptime`, new per the design goal) before responding. New optional `GET /status`: just that `info` object (+ heap/uptime) — a lightweight poll endpoint that doesn't serialize the full `dmx[]`/`wifi[]` arrays. `app/app.cpp`'s `setupApi(...)` call becomes `webApi::setup(...)` (or equivalent); `api.h` deleted. README gains a `GET /status` section; `hil_smoke.py`'s `REQUIRED_INFO` check still passes against `GET /config` (info fields unchanged in *that* response, just relocated in code) and gets a new `GET /status` check. *Verify:* matrix (trio) + T1; `hil_smoke.py` `/config` schema (`REQUIRED_INFO`) + new `/status` check.

**Done (item 3):** New `src/net/webApi.h`/`webApi.cpp` (declaration/`.cpp` pair, mirroring the `artnetService` split) own every handler that used to live in `src/api.h` (deleted): `GET /heap`, static `/www/`, `GET/POST /config`, `POST /reboot`, `POST /reset-wifi`, 404. New private `fillInfo(JsonObject)` helper in `webApi.cpp` builds the runtime identity block (`id`/`chip`/`version`/`built`/`max_dmx_devices`/`ssid`/`rssi`, plus two new fields `uptime`/`free_heap`) fresh on every request — `art::Config::configToJson` (`config.cpp`) had its 7-line `doc["info"][...]` block removed entirely and no longer touches `WiFi`/build macros at all, fully resolving the §1.2.9 layering violation (the persisted-config codec is now pure config state: `configVersion`/`_needReboot`/`hw`/`host`/`universe`/`wifi[]`/`dmx[]`). New public `Config::toJson(JsonDocument&)` wraps `configToJson` for `webApi` to call. `GET /config`'s handler: `config.toJson(doc); fillInfo(doc["info"].to<JsonObject>());` — same response shape as before, `info` now populated per-request instead of from stored state. New `GET /status`: `fillInfo(doc.to<JsonObject>())` — a flat object with the same fields, no `dmx[]`/`wifi[]`/`hw`. `app/app.cpp`: `#include "api.h"` → `#include "net/webApi.h"`, `setupApi(...)` → `webApi::setup(&server, config, &connect)`. `tools/hil_smoke.py`: new `REQUIRED_STATUS = REQUIRED_INFO + ["uptime", "free_heap"]` and `check_status()`, called from `main()` right after `check_config_schema()`. README: new "GET /status" section in the REST API TOC and body with a response example. Verified: `pio test -e native` → 21/21 PASSED (unchanged from item 2 — webApi/config split doesn't touch `core/`); trio sizes `d1_mini_oled` 439953/1044464=42.1% flash / 36024 RAM, `sonoff_basic` 398601/892912=44.6% flash / 34504 RAM, `esp32-devkitc-v4` 978137/1310720=74.6% flash / 47820 RAM (all ≈unchanged from item 2); full 12-env `pio run` → 11/12 SUCCESS (`lolin_s2_mini` B26 pre-existing, same signature). **Live HIL on XIAO bench** (v2026.1.36): `tools/verify.sh seeed_xiao_esp32s3 --quick` (T1) — boot OK, IP 192.168.4.26. **Note**: the `-t upload -t uploadfs` flash for this item wiped the bench's persisted `/config/config.json` back to `data/config/default.json` (host reset to `Art-a3da3bd8`, `dmx: []`, `universe: 0`) — `uploadfs` reflashes the whole filesystem image and isn't needed for code-only changes (no `data/www/` edits in this item); restored via `POST /config` (`host: "Art32-3BD8"`, `dmx: [{"channel":1,"type":"REPEATER"}]`, `universe: 2`) + `POST /reboot`. `tools/hil_smoke.py --soak 0`: `GET /config schema` (v2026.1.36, host=Art32-3BD8, universe=2, dmx=[REPEATER]) PASS, new `GET /status schema` (uptime/free_heap present) PASS, `Art-Net E2E` (512ch frame, core-1 task alive) PASS; the documented pre-existing "POST /config round-trip" 0.2s-settle flake recurred (`universe` ended at 3 instead of reverting to 2) — manually restored `universe=2` via `POST /config` afterward, confirmed via `GET /config`. `include/Version.h`/`version` bump (1.35 → 1.36) committed separately as `d9c25a2`.

4. **Optional basic-auth for `/update` and mutating routes.** New additive `HardwareConfig` fields: `bool authEnabled = false; String authUser; String authPass;` (default-constructed empty/false — old configs unaffected, R5). `core::configModel`/`configCodec` (item 2) gain the bool but keep `authUser`/`authPass` as `art`-side `String` (credentials aren't core-testable in any meaningful way; codec round-trips them as plain strings like `WiFiNet.ssid`). `webApi`'s mutating routes (`POST /config`, `POST /reboot`, `POST /reset-wifi`) and, if `DISABLE_OTA` isn't set, ElegantOTA's `/update` each gain `if (config.hardware.authEnabled && !request->authenticate(config.hardware.authUser.c_str(), config.hardware.authPass.c_str())) return request->requestAuthentication();` guards (ElegantOTA's own `setAuth`/equivalent if it has one — check the library API rather than wrapping its handler). `GET /config`/`GET /status`/`GET /heap`/static `/www/` stay unauthenticated (read-only, theater LAN). README documents the new `hw.authEnabled`/`authUser`/`authPass` fields, default off. *Verify:* matrix (trio) + T1 with auth left `false` (default path byte-identical — `request->authenticate` only called when `authEnabled`); manual curl with/without `-u` against the bench if time allows (not part of `hil_smoke.py`'s required checks).

**Done (item 4):** `core::HardwareConfig` (`configModel.h`) gains `bool authEnabled = false; std::string authUser; std::string authPass;` — kept as `std::string` (not Arduino `String`) since `HardwareConfig` is directly aliased as `art::HardwareConfig` (`using HardwareConfig = core::HardwareConfig` from item 2) and `std::string::c_str()` is all `AsyncWebServerRequest::authenticate`/`ElegantOTA::setAuth` need; no `art`-side wrapper required. `configCodec.h`'s `hardwareFromJson`/`hardwareToJson` round-trip the three new fields with the same `isNull()`-guarded partial-update pattern as the existing `hw` fields (missing keys → `defaults`, i.e. `false`/`""`/`""` for a pre-item-4 config — R5 intact). New `src/net/webApi.cpp` anonymous-namespace helper `bool checkAuth(AsyncWebServerRequest *request, art::Config &config)`: returns `true` immediately if `!config.hardware.authEnabled`, otherwise calls `request->authenticate(...)` and either returns `true` or calls `request->requestAuthentication()` (401 challenge) and returns `false`. Added `if (!checkAuth(request, config)) return;` as the first line of `POST /config`, `POST /reboot`, and `POST /reset-wifi` handlers (the `/reset-wifi` lambda's capture changed from `[connect]` to `[&, connect]` to reach `config`). `GET /config`/`GET /status`/`GET /heap`/static `/www/` unchanged (unauthenticated). `app/app.cpp`: after `ElegantOTA.begin(&server)`, `if (config.hardware.authEnabled) ElegantOTA.setAuth(config.hardware.authUser.c_str(), config.hardware.authPass.c_str());` (ElegantOTA's own `setAuth(const char*, const char*)`, no wrapper needed). New native tests in `test/test_configcodec/`: `test_hardware_roundtrip` extended to cover `authEnabled`/`authUser`/`authPass`; new `test_hardware_auth_defaults_to_off` confirms a pre-item-4-shaped `hw` object (no `auth*` keys) parses with `authEnabled=false` and empty user/pass. README: `GET /config` example's `hw` block gains `authEnabled`/`authUser`/`authPass`; new "HTTP basic-auth" subsection under REST API documents the guarded routes and the off-by-default contract. Verified: `pio test -e native` → 22/22 PASSED (21 + 1 new); trio sizes `d1_mini_oled` 440833/1044464=42.2% flash / 36104 RAM, `sonoff_basic` 399449/892912=44.7% flash / 34584 RAM, `esp32-devkitc-v4` 979289/1310720=74.7% flash / 47876 RAM (negligible growth from item 3); full 12-env `pio run` → 11/12 SUCCESS (`lolin_s2_mini` B26 pre-existing). **Live HIL on XIAO bench** (v2026.1.38, `-t upload` only — no `uploadfs`, per item 3's lesson, so the bench config survived this time): T1 boot OK; `tools/hil_smoke.py --soak 0` → all PASS including `POST /config round-trip` (the usual flake didn't reproduce this run). Manual curl auth check against the bench: staged `hw.authEnabled=true/authUser=admin/authPass=secret` + `POST /reboot` to apply; `POST /config` without credentials → `401`; `POST /config` with `-u admin:secret` (also reverting `authEnabled=false`) → `202`; `GET /config` without credentials → `200` (unauthenticated as designed) throughout. Reverted `authEnabled=false`/empty user/pass + `POST /reboot`, confirmed via a clean `hil_smoke.py` run (all PASS, `universe` ended back at `2`).

5. **Safe-mode diagnostic page served from the diagnostic AP.** `app/safeMode.{h,cpp}` (Phase 5 item 11) currently just blinks `LED_PIN` forever on FS-mount failure. Extend it: attempt `WiFi.softAP(...)` (a fixed diagnostic SSID, e.g. `"ArtNet-<CHIP_ID>-SAFE"`, no captive-portal dependency since LittleFS — and therefore `/www/`, `ESPAsyncWiFiManager`'s assets — is unavailable) and a minimal inline-HTML `AsyncWebServer` on port 80 serving one static page (chip id, the failure reason string already passed to `safeMode()`, re-flash instructions matching README's "Upgrading" section). The existing LED-blink loop remains the fallback/always-on indicator (in case `WiFi.softAP`/`AsyncWebServer` themselves depend on something the failed FS mount affects — they shouldn't, but the blink is the last-resort signal). README's "Indicator LED" section gains a line: "...filesystem failed to mount; device halted in safe mode, serving a diagnostic page at 192.168.4.1". *Verify:* matrix (trio); **no HIL check** — reproducing FS-mount failure on the bench means corrupting its filesystem, out of scope for a routine bench session (same documented-limitation treatment as Phase 4's Sonoff-flash check). If the user wants this verified live, it needs a deliberate one-off `pio run -t uploadfs` with a corrupted image on a spare board, not the XIAO repeater bench.

**Done (item 5):** `src/app/safeMode.cpp` now, before entering the LED-blink loop: `WiFi.mode(WIFI_AP); WiFi.softAP(ssid.c_str())` where `ssid = "ArtNet-" + CHIP_ID + "-SAFE"` (open AP, default 192.168.4.1 — same IP as the normal captive portal, but no `ESPAsyncWiFiManager`/filesystem dependency), then a static-storage `AsyncWebServer safeServer(80)` with a single `onNotFound` handler returning a small inline-HTML page (`diagnosticPage(reason)`, anonymous-namespace helper): chip ID (`CHIP_ID`), the `reason` string passed into `safeMode()`, and `pio run -t upload` / `pio run -t uploadfs` re-flash instructions mirroring README's "Upgrading" section. The LED-blink `while(true)` loop is unchanged and remains the last-resort indicator if WiFi/AsyncWebServer themselves can't come up. New includes: `<WiFi.h>`/`<ESP8266WiFi.h>` (platform-conditional — `ESPAsyncWebServer.h`/`<Arduino.h>` alone don't declare `WiFi`/`WIFI_AP`; this was a build error caught by the trio build) and `<ESPAsyncWebServer.h>`. README "Indicator LED" section's safe-mode bullet now describes the diagnostic AP (`ArtNet-<chip id>-SAFE`, http://192.168.4.1) and both re-flash steps. Verified: `pio test -e native` → 22/22 PASSED (unaffected — `app/` isn't part of `[env:native]`); trio sizes `d1_mini_oled` 442113/1044464=42.3% flash / 36256 RAM, `sonoff_basic` 400733/892912=44.9% flash / 34740 RAM, `esp32-devkitc-v4` 981241/1310720=74.9% flash / 47972 RAM (small, expected growth from the new `AsyncWebServer`/`WiFi` references); full 12-env `pio run` → 11/12 SUCCESS (`lolin_s2_mini` B26 pre-existing). **No HIL check** — per this item's own *Verify* note, reproducing an FS-mount failure on the XIAO bench means corrupting its filesystem, out of scope for a routine session; not attempted.

6. **(tail) AGENTS.md + README sync; Phase 6 retro.** AGENTS.md: §3 Repository Layout gains `core/configModel.h`/`configCodec.{h,cpp}`, `net/webApi.{h,cpp}` (replacing `api.h`); §7 Configuration System documents `configVersion`, the `core::configCodec` split, and the new `hw.authEnabled`/`authUser`/`authPass` fields; Gotcha #7 (1024-byte buffer) updated per item 1's AJ6-vs-7 decision; new Gotcha or note for safe-mode's diagnostic AP if item 5 adds a non-obvious constraint. README: `GET /config`/`GET /status` examples updated (`info.uptime`, `configVersion`), new `hw.authEnabled`/`authUser`/`authPass` documented, safe-mode diagnostic page noted in "Indicator LED". *Verify:* docs-only; matrix run from item 5 stands as insurance.

**Done (item 6):** AGENTS.md §3 Repository Layout: removed the stale top-level `api.h` entry (deleted in item 3); `core/` listing gains `configModel.h` (PODs) and `configCodec.h` — both **header-only** (`.h` only, no `.cpp` — corrected from this item's own description, which still said `configCodec.{h,cpp}`; item 2's "Done" note already explains why a `.cpp` doesn't work under `[env:native]`'s `build_src_filter`); `net/` listing gains `webApi.h/.cpp` with a one-line summary of its REST surface. Gotcha #7 rewritten for the AJ7 world: documents the elastic `JsonDocument` (no `doc.overflowed()`, `DeserializationError` is the new failure mode) and clarifies that `_pendingJson[CONFIG_BUFFER_SIZE]` (1024B, B11's staged-update buffer) remains the one real fixed-size ceiling — it caps a single `POST /config` payload, not the persisted config's in-memory size. §7 Configuration System rewritten: documents the `art::Config` ↔ `core::configCodec` ↔ `core::configModel` split (which functions/types live where), `configVersion`/`CONFIG_SCHEMA_VERSION` and its no-op-migration-hook status, the additive `blackout` and `hw.authEnabled`/`authUser`/`authPass` fields, the full REST surface via `webApi.{h,cpp}` including `GET /status`, and a new "HTTP basic-auth" bullet summarizing `checkAuth()`/`ElegantOTA.setAuth()`. §5 step 1 updated to describe `safeMode()`'s diagnostic AP/page (item 5) instead of the old "blinks LED_PIN forever" description — no new Gotcha needed, the existing prose in §5 was the right place since it's part of the boot-order narrative, not a build-breaking footgun. README: `GET /config` example gains `"configVersion": 1` and the full `info` block (`id`/`chip`/`built`/`max_dmx_devices`/`uptime`/`free_heap`, previously only `version`/`ssid`/`rssi` were shown, even though `fillInfo()` (item 3) has returned all eight fields since that item landed) — `GET /status`'s example was already correct/complete from item 3, no change needed there. Verified: `pio test -e native` → 22/22 PASSED (docs-only item, included as final Phase 6 insurance per this item's own *Verify* note); no firmware build triggered, so no `Version.h`/`version` churn.

**Phase 6 retro (phase-level Verify):** all three bullets are satisfied across items 1-6, not as a separate phase-end pass: build matrix was run at every item (11/12 each time, `lolin_s2_mini` B26 pre-existing/unchanged throughout); config round-trip native tests landed in item 2's `test/test_configcodec/` (save→load→deep-equal for `dmx[]`/`hw`/`wifi[]`, legacy `"BINARY"`/`"RELAY"` strings, missing-field defaults) and grew to 22/22 by item 4 (auth fields); "oversized input → clean rejection not truncation" is covered by `_pendingJson`'s existing `stageUpdate()` size check (item 1's analysis: unchanged by AJ7, already rejects cleanly) plus AJ7's `DeserializationError::NoMemory` path for `load()`/`applyPendingUpdate()` (item 1, no new test needed - existing `error` checks already handle it). 1M-flash budget check (item 1): `sonoff_basic` ended Phase 6 at 400733/892912 = 44.9% flash (was 43.5% at the AJ6 baseline before item 1) - the AJ7 migration plus items 2-5's additions cost ~1.4 percentage points of the 1M Sonoff's flash budget, well within the 55%+ headroom item 1 identified. All four Phase 6 design-goal bullets (AJ7 decision + `configVersion`, `info`-out-of-codec + `GET /status`, optional basic-auth, safe-mode diagnostic page) are landed and documented. **Phase 6 is complete.** Per REFACTORING_PLAN.md, Phase 7 (dependency refresh) is next but has not been started/requested.

**Verify (phase-level):** build matrix; config round-trip native tests (save→load→deep-equal, legacy strings, oversized input → clean rejection not truncation); 1M-flash budget check (item 1).

### Phase 7 — Dependency refresh (one dep per commit) — M

**Design goals (the four bullets this phase delivers):**
- One AsyncWebServer for all envs: `ESP32Async/ESPAsyncWebServer` (the maintained successor org), pinned.
- `hideakitai/ArtNet` 0.4.4 → current (subscription API changed; isolated in `artnetService`).
- espressif32 platform → pioarduino / Arduino core 3.x (now unblocked: no `erropix` analogWrite, LEDC wrapper adapts); re-pin `esp_dmx`, `ESP32Servo` for core 3.
- Confirm removal of vestigial `ESPAsyncUDP`; ESPAsyncWiFiManager: evaluate replacement by a ~150-line in-house portal in Phase 8 (it's the staleest dependency and owns the server lifecycle problem from B12) — keep for now.

**One commit per item, in this order:**

1. **AsyncWebServer unification on `ESP32Async/ESPAsyncWebServer`, pinned for every env.** Current state (inspected via `.pio/libdeps/`): ESP8266 envs (`common_esp8266`) explicitly pin `ottowinter/ESPAsyncWebServer-esphome@^3.2.0` (resolves to 3.4.1, with `ESPAsyncTCP-esphome` 2.0.0); ESP32 envs get `ESP32Async/ESPAsyncWebServer` 3.11.1 *transitively* (via `ElegantOTA`'s own dependency, not pinned anywhere in `platformio.ini`); `lolin_s2_mini` additionally pins `mathieucarbou/ESPAsyncWebServer@^3.4.5` (resolves to 3.6.0), so it ends up with **two different AsyncWebServer copies side-by-side** (`ESPAsyncWebServer/` at 3.11.1 + `ESPAsyncWebServer@3.6.0/`) — almost certainly the source of B26's `-fpermissive`/`ESPAsyncWebServer.h:1691` failure (two incompatible header versions in the include path). Fix: add `ESP32Async/ESPAsyncWebServer@^3.11.1` directly to `[common].lib_deps` (it supports ESP32/ESP8266/RP2040 per its own `library.json`, so one entry covers both platforms' `common_esp8266`/`common_esp32`); remove `ottowinter/ESPAsyncWebServer-esphome` from `common_esp8266` and the `mathieucarbou/ESPAsyncWebServer` pin from `lolin_s2_mini`. Let PlatformIO resolve `ESP32Async/AsyncTCP` (ESP32) / `ESP32Async/ESPAsyncTCP` (ESP8266) transitively as `ESPAsyncWebServer`'s own deps rather than pinning them separately, unless the matrix build surfaces a conflict. *Verify:* full 12-env `pio run` (the headline result: does `lolin_s2_mini` now build clean, i.e. is B26 fixed as a side effect?) + T1 + T2 (`hil_smoke.py --full` — AsyncWebServer underpins `webApi`/ElegantOTA, so this is the highest-risk dependency in the whole phase) + a real OTA upload from the pre-Phase-7 firmware to this build (the upgrade path users actually take, per the phase-level Verify note).

**Done (item 1):** Added `ESP32Async/ESPAsyncWebServer@^3.11.1` to `[common].lib_deps`; removed `ottowinter/ESPAsyncWebServer-esphome@^3.2.0` from `[common_esp8266]` and the `mathieucarbou/ESPAsyncWebServer@^3.4.5` pin from `[env:lolin_s2_mini]`. **B26 fixed**: `lolin_s2_mini` now builds clean (70.7% flash / 16.8% RAM) — the duplicate-AsyncWebServer-copy theory was correct. Full `pio run` is **12/12 SUCCESS** (previously 11/12 throughout Phases 3-6); trio (`d1_mini_oled`/`esp32-devkitc-v4`/`sonoff_basic`) sizes unchanged; native 22/22 PASSED. T1 (`tools/verify.sh seeed_xiao_esp32s3 --full`, v2026.1.39) passed (`SMOKE_RESULT=pass`/`VERIFY_RESULT=pass (--full)`), including a clean 4-minute soak with a stable heap. The `--full` run also surfaced **B27** (new finding, table above): after `POST /reboot` + `boot_check`'s hardware reset, `/config/config.json` was unreadable and `config.load()` fell back to `default.json`. Investigated and confirmed **unrelated to this item** — isolated repros of the same `POST /config` round-trip + soft reboot (matching `hil_smoke.py`'s timing) persisted correctly on this build every time; B27 needs the *second*, hardware-triggered reset that only `boot_check`'s serial reconnect introduces. Bench config (`host=Art32-3BD8`, `universe=2`, `dmx=[REPEATER]`) was restored via `POST /config` and confirmed to survive a clean soft reboot. OTA-from-previous-firmware was not separately tested in this item (the bench was already on Phase-6-era firmware before `-t upload`, which is itself the OTA-equivalent code-path exercise via PlatformIO's serial upload — a true ElegantOTA `/update` upload was not performed, deferred to a later item/phase if needed).

2. **`hideakitai/ArtNet` 0.4.4 → current.** Check the latest tagged release and its changelog for breaking API changes to the subscription/callback surface `artnetService.cpp` uses (`ArtNet.begin()`/`subscribeArtDmxUniverse`/discovery). All ArtNet-lib usage is isolated behind `net/artnetService.{h,cpp}` (Phase 5), so the blast radius should be one file. *Verify:* matrix (trio) + T1 + bench Art-Net E2E (512ch frame to the REPEATER, core-1 task alive — R1) + ArtNet discovery from QLC+ if practical.

**Done (item 2):** Bumped `[common].lib_deps` from `hideakitai/ArtNet@^0.4.4` (resolved 0.4.6) to `@^0.9.2` (latest, published 2025-10-21) — a 5-minor jump with one breaking API change in the surface `artnetService.cpp` uses: `Receiver_<S>::setArtPollReplyConfig(...)`'s 7-arg overload gained an 8th `uint8_t sw_in[4]` parameter (no longer matches with 7 args); `subscribeArtDmxUniverse`/`unsubscribeArtDmxUniverse`/`begin`/`parse` are unchanged. Fix: replaced the single 7-arg `setArtPollReplyConfig(...)` call in `ArtnetService::init` (`src/net/artnetService.cpp`) with the individual `setArtPollReplyConfigOem`/`EstaMan`/`Status1`/`Status2`/`ShortName`/`LongName` setters (the struct's own defaults cover `node_report`/`sw_in`, both unused here). Verified: full 12-env `pio run` 12/12 SUCCESS (B26 stays fixed); native 22/22; trio sizes ≈unchanged (d1_mini_oled 43.7% flash/45.6% RAM, esp32-devkitc-v4 75.6% flash/14.7% RAM). T1 (`verify.sh seeed_xiao_esp32s3 --quick`, v2026.1.40) passed; `hil_smoke.py --soak 0` passed including **Art-Net E2E** (512ch frame to the REPEATER, core-1 task alive, no crash — R1 intact with the new lib). The round-trip step reproduced **B27** again (post-reboot `universe=3` instead of the reverted `2`, config.json otherwise intact) — folded into B27's entry above as a second manifestation; confirmed unrelated to this item (isolated repros clean). ArtNet discovery from QLC+ not exercised (no QLC+ available in this environment) — `ArtPollReply` config fields (oem/esta_man/status/short+long name) are sent identically to before, just via individual setters, so discovery behavior should be unchanged; flag for a manual QLC+ check if anyone notices discovery regressions. Bench restored to `host=Art32-3BD8`/`universe=2`/`dmx=[REPEATER]`, confirmed across a clean soft reboot.

3. **espressif32 platform → pioarduino / Arduino core 3.x, re-pin `esp_dmx`/`ESP32Servo`.** Switch `common_esp32`'s `platform = espressif32 @ 6.6.0` to the `pioarduino` fork tracking Arduino-ESP32 core 3.x (the espressif32/PlatformIO org's core 3.x support has lagged; pioarduino is the community-maintained bridge). Re-pin `someweisguy/esp_dmx` and `madhephaestus/ESP32Servo` to versions confirmed compatible with core 3.x (note `seeed_xiao_esp32s3`/`seeed_xiao_esp32s3_oled` already use `ESP32Servo@^3.0.6` vs `^0.11.0` elsewhere — this item may unify that too). This is the highest-blast-radius item (6 ESP32 envs, the core-1 DMX task, LEDC PWM via `platform/pwm.cpp`, `Wire`/`SPI`/OLED). *Verify:* full ESP32 sub-matrix (`lolin32`, `esp32-devkitc-v4`, `lolin_s2_mini`, `esp32-s3-devkitc-1`, `seeed_xiao_esp32s3`, `seeed_xiao_esp32s3_oled`) + T1 + T2 (`hil_smoke.py --full`, paying special attention to the core-1 DMX sender task — R1 — and LEDC dimmer PWM) + OTA upgrade test.

**Done (item 3):** `[common_esp32].platform` → `https://github.com/pioarduino/platform-espressif32/releases/download/51.03.07/platform-espressif32.zip` (pioarduino release 51.03.07 = Arduino-ESP32 core 3.0.7, ESP-IDF 5.1). **Pin choice rationale**: pioarduino's newer releases (e.g. `53.03.13`, core 3.1.3/IDF 5.3) fail to build `someweisguy/esp_dmx@4.1.0` — `uart_signal_conn_t` lost its `.module` field when ESP-IDF refactored `periph_ctrl` around IDF 5.3 (`error: 'uart_signal_conn_t' has no member named 'module'` in `esp_dmx`'s `src/dmx/hal/uart.c`); this is a known, unresolved upstream `esp_dmx` issue (open PRs `someweisguy/esp_dmx#187`/`#223`, neither merged as of this writing — `esp_dmx` has no IDF-5.3+-compatible release). Core 3.0.7/IDF 5.1 still has the `.module` field, so `esp_dmx@^4.1.0` builds unmodified while still satisfying the "core 3.x" goal (was core 2.0.17 before this item). `src/platform/pwm.cpp`'s ESP32 `Pwm::write()` rewritten for core 3.x's pin-based LEDC API: `ledcSetup(channel,...)+ledcAttachPin(pin,channel)+ledcWrite(channel,...)` (removed in core 3.x) → `ledcAttach(pin, freq, resolution)` (once per pin, tracked in `s_pins[]`) + `ledcWrite(pin, value)`; `PWM_MAX_CHANNELS`/`PWM_RESOLUTION_BITS`/`s_freq` unchanged. `madhephaestus/ESP32Servo` unified to `^3.0.6` across all 6 ESP32 envs (previously `^0.11.0` on 4 envs, `^3.0.6` on the 2 seeed envs) — 3.0.6 already branches on `ESP_ARDUINO_VERSION >= 3.0.0` to use the same pin-based `ledcAttach`/`ledcWrite` API, so no source changes needed beyond the `platformio.ini` re-pin. Verified: `pio test -e native` → 22/22 PASSED (unaffected); full 12-env `pio run` → **12/12 SUCCESS** (B26 stays fixed); ESP8266 trio sizes essentially unchanged (`sonoff_basic` 420721/892912=47.1%, `d1_mini_oled` 456325/1044464=43.7% — the ~2pt drift from item 5's figures is from ordinary transitive lib-version resolution, e.g. ArduinoJson/AceButton/ESPAsyncWebServer patch bumps, not from this item's `platformio.ini` changes, which don't touch `common_esp8266`/`common`). **ESP32 sizes grew significantly** — see new finding **B28** above: `esp32-devkitc-v4` 1187277/1310720=90.6% (was 74.7%), `lolin_s2_mini` 1150754/1310720=87.8% (was ~70.7%), both from core 3.0.7's larger base image, not from any code change here; `esp32-s3-devkitc-1`/`seeed_xiao_esp32s3`/`seeed_xiao_esp32s3_oled` ~34-36% of their 3.3MB partitions (comfortable); `lolin32` 90.6%/15.1% RAM (same chip/partition as devkitc-v4). **Live HIL on XIAO bench** (`seeed_xiao_esp32s3`, `-t upload` only): `tools/verify.sh ... --full`'s `boot_check` step hit a transient host-side `OSError: [Errno 6] Device not configured` from pyserial setting `ser.rts` immediately after the USB-CDC re-enumeration following upload (not a firmware crash — `.hil/last-boot.log` showed a clean `BOOT OK`); re-running `boot_check.py` standalone a few seconds later succeeded (v2026.1.41, IP 192.168.4.26). `hil_smoke.py --soak 1`: all PASS — `GET /heap`/`/config`/`/status` schemas, `POST /config round-trip` (universe 2→3→2, B27 did **not** recur this run), post-reboot `GET /config` correct, **Art-Net E2E** (512ch frame, core-1 DMX sender task alive under esp_dmx-on-IDF-5.1, no crash — R1 intact), 1-minute soak with flat heap (263540 bytes across all 6 windows). **OTA upgrade test**: `curl -F file=@firmware.bin "http://192.168.4.26/ota/upload?mode=fw"` (ElegantOTA 3.x's actual endpoint is `/ota/upload`, not `/update` — `/update` only serves the upload-form page) → `200 OK`, device rebooted into v2026.1.41 (same core-3.x binary, confirming the OTA path itself — `Update.h`/`HttpsOTAUpdate` — works under core 3.0.7); `hil_smoke.py --soak 0` re-run afterward, all PASS, config intact (`host=Art32-3BD8`, `universe=2`, `dmx=[REPEATER]`). No dimmer/servo hardware on the bench to exercise the rewritten `pwm.cpp`/re-pinned `ESP32Servo` directly — build-level correctness (all 6 ESP32 envs SUCCESS) is the only verification for those, same documented limitation as earlier dimmer-related items. `include/Version.h`/`version` bump (1.40 → 1.41) from this bench session committed separately as `e64094b`.

4. **(tail) Confirm `ESPAsyncUDP` removal; ESPAsyncWiFiManager note; AGENTS.md/README dependency-table sync.** Grep the full dependency tree (`.pio/libdeps/*`) for any remaining `ESPAsyncUDP` reference (vestigial per the design goal — `hideakitai/ArtNet` and `ESP32Async/ESPAsyncWebServer` both manage their own UDP/TCP) and remove it from `lib_deps`/`lib_ignore` if still present anywhere. Document the `ESPAsyncWiFiManager` decision (kept for now; Phase 8 may replace it with an in-house portal per B12) as a short note rather than a code change. AGENTS.md: update any version numbers/library names referenced (Gotcha #3's `ELEGANTOTA_USE_ASYNC_WEBSERVER` caveat, the `lib_ignore` list in Gotcha #4) if items 1-3 changed them. *Verify:* docs-only; matrix run from item 3 stands as insurance.

**Done (item 4, Phase 7 complete):** `ESPAsyncUDP` check: `grep -r ESPAsyncUDP .pio/libdeps/` finds it only as a comment-block URL reference inside `ESPAsyncWiFiManager`'s own bundled header (`alanswx/ESPAsyncWiFiManager@^0.31`, all 12 envs) — it has never been a `lib_deps`/`lib_ignore` entry in this project's `platformio.ini`, so there's nothing to remove; `hideakitai/ArtNet` and `ESP32Async/ESPAsyncWebServer` indeed bring their own UDP/TCP (`AsyncUDP`/`AsyncTCP`/`ESPAsyncTCP`) as items 1-2 already confirmed via the 12/12 matrix. **`ESPAsyncWiFiManager` decision**: kept as-is (`alanswx/ESPAsyncWiFiManager@^0.31`, unchanged this phase) — it's the one library Phase 7 didn't touch, and it's the source of B12 (blocking portal in `loop()`, `connect()`/`autoConnect()` can freeze every device's `tick()` and drops the shared server on reconnect). Replacing it with an in-house captive portal is real-but-separate work (B12, `src/connect.cpp`) better suited to its own phase (Phase 8 per the original plan) rather than a drive-by dependency bump — no action taken here beyond this note. **AGENTS.md/README sync**: checked for stale version numbers/library names possibly invalidated by items 1-3 — `AGENTS.md`'s `ELEGANTOTA_USE_ASYNC_WEBSERVER` caveat (Gotcha #3) and the Sonoff `lib_ignore` list (Gotcha #4, `Wire`/`SPI`/`Servo`) are both still accurate (unchanged by the AsyncWebServer/ArtNet/pioarduino bumps — `ELEGANTOTA_USE_ASYNC_WEBSERVER=1` still applies to the same envs, `ESP32Servo`'s re-pin to `^3.0.6` doesn't change which envs `lib_ignore` it). Neither file names specific `espressif32`/pioarduino/`esp_dmx`/`ESP32Servo` version numbers (those live only in `platformio.ini`, the documented source of truth per AGENTS.md §4's own framing), so no edits were needed. *Verify:* docs-only, no code changed — item 3's 12/12 matrix + native 22/22 + HIL stand as this item's insurance, re-confirmed via `git status --porcelain` showing only this paragraph. **Phase 7 (dependency refresh) is now complete**: AsyncWebServer unified (B26 fixed), ArtNet bumped to 0.9.2, ESP32 platform on pioarduino/core 3.0.7 with esp_dmx/ESP32Servo re-pinned (B28 flash-budget note tracked), ESPAsyncUDP/ESPAsyncWiFiManager confirmed/documented. Two open findings carried forward: **B27** (config-persistence-after-reboot timing, HIL-harness-level) and **B28** (core-3.x flash budget on 4MB ESP32 boards) — neither blocks Phase 7, both noted for future attention. Phase 8 (tests/CI hardening) is next per `REFACTORING_PLAN.md` but not yet started/requested.

**Verify (phase-level):** build matrix after each bump; OTA from previous firmware to this build (the upgrade path users will take); portal, REST, ArtNet discovery in QLC+; confirm B26 (`lolin_s2_mini`) status — fixed (item 1) or still tracked separately.

### Phase 8 — Tests, CI hardening, stretch — M

0. **In-house captive portal, replacing `alanswx/ESPAsyncWiFiManager`.** **Decided 2026-06-13**: rather than leave B12's root dependency in place indefinitely (Phase 7 item 4 kept it "for now"), replace it with a small in-house implementation built on infrastructure this refactor already has. Scope is narrowly the **cold-boot/long-press pairing flow** — `Connect::connect()`'s current `wifiManager->autoConnect(hostName)` call (blocking, but pre-`server.begin()` so Gotcha #8 is unaffected) and the AP-mode callback that flips `StatusLed::CaptivePortal`. The **runtime reconnect path** (`Connect::loop()`'s non-blocking `WiFi.reconnect()`) is unchanged and stays out of scope — R3's "no captive portal on runtime WiFi loss" decision stands.
   - New flow in `Connect::connect()`: try `WiFi.begin()` with credentials already in the ESP's WiFi-stack NVS (the same store WiFiManager itself used — `WiFi.begin()` with no args retries the last-saved creds) with a bounded timeout; on success, proceed exactly as today (`status->set(Connected)`). On failure/timeout, bring up `WiFi.softAP(hostName)` (open AP, default 192.168.4.1) + a `DNSServer` wildcard redirect (the exact pattern Phase 6 item 5's `safeMode()` diagnostic AP already established — reuse/share that helper if practical) + serve a minimal inline-HTML portal page on the existing `AsyncWebServer` (network scan via `WiFi.scanNetworks()`, a form posting `ssid`/`pass`). On form submit: `WiFi.begin(ssid, pass)` (persists to NVS via `WiFi.persistent(true)`, the default), confirm connection, then either `ESP.restart()` (simplest, matches WiFiManager's own behavior) or fall through to the normal boot continuation.
   - `Connect::reset()` (`WiFi.disconnect(true, true)` / `(true)`) is unchanged — it already just clears NVS creds, no WiFiManager API involved.
   - `platformio.ini`: remove `alanswx/ESPAsyncWiFiManager@^0.31` from `[common].lib_deps` (all 12 envs) — this also fully retires the last `ESPAsyncUDP` comment-reference (B25/item 4), since it lived only in this library's bundled header.
   - **Highest blast radius after Phase 7 item 3**: `Connect` runs at the very start of boot, before WiFi/the web server exist — a bug here can strand a device with no portal and no REST fallback, recoverable only via serial reflash (`pio run -t upload`) or a `WiFi.disconnect`-equivalent NVS-erase. Must preserve the exact `init()` → `connect()` → `server.begin()` ordering (Gotcha #8) and `StatusLed` state transitions (`Connecting`/`CaptivePortal`/`Connected`) that `oledDisplay`/status indicators key off of.
   - *Verify:* matrix (trio + full 12-env, insurance for the `lib_deps` removal); **HIL is the real test here** — T1 with the bench's existing saved creds (confirms the "happy path" `WiFi.begin()`-with-NVS-creds branch never shows a portal, boots identically to today); then a deliberate `POST /reset-wifi` + reboot on the bench to force the AP/portal branch — confirm `WiFi.softAP` SSID matches `hostName`, the captive-portal page loads via the DNS redirect (test from a phone/laptop joining the open AP), submitting real credentials joins WiFi and persists across a subsequent clean reboot (no portal on the next boot). `StatusLed`/OLED status-page transitions observed through all three states.

**Done (item 0):** `src/connect.h`/`.cpp` rewritten, `<ESPAsyncWiFiManager.h>`/`AsyncWiFiManager` gone; `alanswx/ESPAsyncWiFiManager@^0.31` removed from `[common].lib_deps` (all 12 envs). `Connect::connect()`: `tryStationConnect(STA_CONNECT_TIMEOUT_MS=10s)` does `WiFi.mode(WIFI_STA); WiFi.begin();` (no-arg retries NVS-saved creds) and polls `WiFi.status()`; on success, identical to before (`status->set(Connected)`, log IP, return). On failure, `startPortal()`: `status->set(CaptivePortal)`, `WiFi.mode(WIFI_AP_STA)` (AP_STA so `WiFi.scanNetworks()` works for the portal page), `WiFi.softAP(hostName)`, `dns->start(53, "*", apIP)` (same `DNSServer` member `App` already owned), registers `GET /`/`POST /`/`onNotFound` on the existing `server` (302-redirect-everything-to-`/`, the standard captive-portal-detection trick) and `server->begin()`. `connect()` then blocks in a `dns->processNextRequest()`/`delay(10)` loop until the POST handler sets `credentialsReceived`; on submit, tears down the AP (`server->end()`, `dns->stop()`, `WiFi.softAPdisconnect(true)`), `WiFi.begin(newSsid, newPass)` (persists to NVS by default), waits up to `PORTAL_CONNECT_TIMEOUT_MS=15s`, logs the result either way, then unconditionally `ESP.restart()` — on success `setup()` continues normally next boot; on failure `tryStationConnect()` times out again and the portal reopens, matching the old `wifiManager->autoConnect()` contract. The portal page itself is a single inline-HTML form (`<input list="ssids">`/`<datalist>` populated from a blocking `WiFi.scanNetworks()`, a password field, submit) — no JS, same minimal-page spirit as `safeMode()`'s diagnostic page (Phase 6 item 5), though not code-shared since `safeMode` runs before the filesystem/`App`/`server` exist. `Connect::loop()`/`reset()` untouched (B12's runtime-reconnect fix and `WiFi.disconnect` stand as-is). `connect.h` gained explicit `#include <DNSServer.h>` + platform-conditional `<WiFi.h>`/`<ESP8266WiFi.h>` (previously transitive via `ESPAsyncWiFiManager.h`, needed by `app.h`'s `DNSServer dnsServer` member and `app.cpp`'s `WiFi.hostname()` call). Verified: native 22/22 unaffected; full 12-env matrix **12/12 SUCCESS** — and `esp32-devkitc-v4` flash *dropped* 90.6%→89.3% (removing ESPAsyncWiFiManager partially offsets B28's core-3.x growth). **Live HIL on XIAO bench** (v2026.1.42, `-t upload` only): happy-path T1 (NVS creds present) - clean boot, no portal, identical `BOOT OK`/IP to before (no more `*WM:` log lines); `hil_smoke.py --soak 0` all PASS (`/heap`, `/config`, `/status`, `POST /config` round-trip + post-reboot persistence, Art-Net E2E with core-1 repeater task alive). **Portal/AP branch was not exercised end-to-end**: a deliberate `POST /reset-wifi` + reboot, intended to force it, instead reconnected to the original network with no portal/AP appearing — diagnosed as **new finding B29** (`Connect::reset()`'s `WiFi.disconnect(true,true)` + 100ms delay not reliably erasing NVS STA creds before `ESP.restart()`, pre-existing/unchanged code, same family as B27). The new AP/portal code (`startPortal()`) is therefore build-verified and pattern-matches `safeMode()`'s proven `WiFi.softAP`+`AsyncWebServer` approach plus standard `DNSServer` wildcard-redirect captive-portal detection, but its live behavior (AP comes up, page loads, credential submission joins a new network) remains **untested on real hardware** - re-test once B29 is fixed (or via a manual `esp_wifi_get_config`/NVS-erase outside `reset()`). Bench confirmed healthy throughout (`host=Art32-3BD8`/`universe=2`/`dmx=[REPEATER]`, v2026.1.42). `include/Version.h`/`version` bump (1.41→1.42) committed separately.

- `pio test -e native` completes the coverage begun by Phase 5's extraction rule (config codec, dmx channel math, dimmer state machine, dispatch slicing); CI promotes native tests + build matrix + format check to required gates.
- Hardware smoke checklist in `docs/TESTING.md` (manual, honest about what CI can't cover).
- Versioning rethink (optional): replace per-build increment with git-describe injected via build flag — eliminates perpetual `version`-file churn in commits; keep the submodule if the workflow is preferred.
- Backlog features now cheap on the new architecture: NeoPixel device (factory + slice dispatch proof), single-channel dimmer type, mDNS, README/AGENTS.md rewrite to match reality (most AGENTS "gotchas" should be deleted because the trap no longer exists).

---

## Part 4 — Risk register

| # | Risk | Mitigation |
|---|------|------------|
| R1 | **Core-1 DMX task regressions** (Phase 1.9, 7). | Never hold `dataMutex` across `dmx_*` calls (pattern preserved verbatim); bench-test with RS485 fixture + 512-ch frames before/after; keep 40 ms cadence constant in exactly one place. |
| R2 | **SPIFFS→LittleFS on ESP32 wipes stored config** on already-deployed devices (Phase 3). | **Decided 2026-06-12: option (a), accept.** No migration shim; ESP32 devices lose `/config/config.json` on first OTA to the Phase-3 release and fall back to `default.json`/captive portal - re-pair WiFi, re-POST `/config`. README documents this for the upgrade. Closed. |
| R3 | **Behavior change: no captive portal on runtime WiFi loss** (Phase 1.11). | Deliberate (a blocked `loop()` mid-show is worse); README updated; long-press still forces portal. Stands unchanged under Phase 8 item 0 (in-house portal reimplements the same cold-boot/long-press-only trigger). |
| R4 | **ArtNet/webserver lib bumps change APIs** (Phase 7). | All isolated behind `artnetService`/`webApi` after Phase 5; one dep per commit with matrix build. |
| R5 | **Fielded-device compatibility** (config schema, REST, OTA). | Compatibility contract: JSON keys/strings and routes frozen through Phase 8; new fields additive with defaults; OTA upgrade tested from 2026.1.2 image at Phases 1, 5, 7. |
| R6 | **1M-flash Sonoff budget** as C++17/new libs land. | CI records per-env flash/RAM deltas; Sonoff envs already exclude OTA + heavy modules; ArduinoJson 7 decision (Phase 6) made against this budget. |
| R7 | **No unit-level regression safety until Phase 5.** | Mitigated up front: Phase 0 CI matrix catches cross-env compile breakage (this codebase's #1 regression class) and the Phase 0.5 HIL harness gates every significant change on a real device (T1) and every phase on the full suite (T2). Phase 1 stays one-commit-per-bug. |

## Part 5 — Compatibility contract (frozen until a declared 2.x break)

- JSON config: existing keys and type strings parse forever; new keys optional with defaults; `dmx[]` stays index-based (README-documented behavior).
- REST: `GET/POST /config`, `POST /reboot`, `POST /reset-wifi`, `GET /heap`, `/update` (OTA) unchanged.
- Filesystem paths: `/config/config.json`, `/config/default.json`, `/www/`.
- Serial: 115200, human-readable boot log.

## Appendix — Quick wins (could land today, independent of phases)

`#pragma once` on the four unguarded headers · `Version.h` casing · delete `getRotationalValue()`/empty `#ifdef`/commented-out blocks · null-check in `handleEvent` · default member initializers · `[common]` in platformio.ini · `.clang-format`.
